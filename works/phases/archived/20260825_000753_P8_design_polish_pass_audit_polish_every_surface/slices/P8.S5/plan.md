# Plan — P8.S5: Apply R9 — landing 관제 현황판 + board implementation

Orchestrator plan, written inline (auto mode) on 2026-08-23, **after** R9's SIGNOFF ("sign off", 2026-08-23). Executor: `slice-executor-high`. Kind `implementation`, risk `high`. Depends on `P8.S4` (done).

## RESPECT THE DESIGN

Build R9 exactly as signed. **The contract is `docs/reference/design/rounds/09-landing-board/output/build-prompt.md`** (read it whole, first), with `result.md` beside it for intent and departures, **`landing/r9-board.css` as the geometry source of truth** (column plan, states, breakpoints — port its declarations, do not re-derive them) and `landing/r9-rows.jsx` for the card's row markup. Precedence: `docs/reference/design/SIGNOFF.md` R9 entry (what it supersedes in R2/R3). Ship every element as designed: layout, states, interactions, copy. Do not drop, simplify, restyle or "improve" anything; where a value is unspecified pick the option closest to the designed intent, never a plainer fallback. The record under `output/` is read-only. A design question the record never settled → one line on `phase.md` `## Operator Questions`, never invented.

**Operator runtime (every browser claim is made here):** `## Operator Runtime` in `docs/current/operations.md` — dev stack `make stack-up` (already running: `make stack-status`), **`http://127.0.0.1:3000` in Chrome desktop** plus the tailnet origin it prints, `next dev`; a 390px viewport; **and additionally the production build** (`cd frontend && npm run build && npm run start` on another port) because this slice adds client-side refresh behaviour and changes the board on the first screen. Reuse the CDP harness P8.S1/P8.S3 documented in their `result.md` (P7.S9's `cdp.mjs`).

**Operator answers that bind this slice** (`phase.md` §R9 interview + §Operator Questions): window 15; refresh = **automatic**; hero H1 unchanged; P8 Q5 = drop (delete the gate-cost/disclaimer constants now); **Q10 interval = 60 s** (design assumption, unchallenged — use it, make it one constant); **Q11 `next_lapse.tie_count`** — add it (derived from data already served; catalogue as done, operator may still veto at the review); **Q12 the account-menu 「의견 보내기」 row is NOT this slice's** (build-prompt §12 says next chrome slice — do not touch `AccountSlot.tsx`).

## What to build (build-prompt sections in brackets)

1. **`frontend/components/landing/copy.ts`** [§9] — add the **14 new constants verbatim** (`TAB_NOTE_KO`, `shownLine{before,middle,after}`, `moreKo(step)`, `remainingKo(n)`, `collapseToFirstKo(step)`, `COLLAPSE_KO`, `REFRESHED_KO`, `LEGEND_DDAY_KO`, `LEGEND_SOON_KO`, `LEGEND_NEAR_KO`, `LEGEND_FAR_KO`, `tieCountKo(n)`), each cited to `rounds/09-landing-board/output/build-prompt.md §9`; `EXPAND_KO` now pairs with `COLLAPSE_KO` (rewrite its comment — the "copy nobody signed" rationale is superseded by R9). **Delete `STAT_REPORTS_KO`.** Delete the gate-cost / disclaimer constants R8 left in `components/chrome/copy.ts` (`GATE_COST_*`, `DISCLAIMER_KO`, and `POSITIONING_KO` / `PROVENANCE_KO` only if nothing else imports them — grep first; P8 Q5 = drop, no relocation). Register the 14 strings in `docs/reference/design/grounding/copy-inventory.md` under an "R9 additions" section (grounding data, not the immutable `output/`).
2. **`Board.module.css`** [§2–§5; port `r9-board.css`] — row grid `76px minmax(180px,1fr) 240px 190px 96px`, `[data-extras="none"]` → `76px minmax(180px,1fr) 300px 96px` + extras hidden; `gap: var(--space-3)`, `min-height:44px`, `align-items:center`, `padding:8px 12px`, `margin-inline:-12px`, dashed bottom; `≤1119`: `72px minmax(120px,1fr) 200px 170px 96px` / no-extras `72px minmax(120px,1fr) 240px 96px` (**never `auto`** for extras); `≤767`: the two-line row (`minmax(0,1fr) auto`, D-day right on line 1, meta on line 2 spanning, `extras:not(:empty)::before{content:"·"}`), controls 44px, strip button full-width under its sentence. Row states: hover `--surface-raised` + corp underline, `:focus-within` same bg + `outline:2px solid var(--focus-ring); outline-offset:-1px`, active `--surface-inset`, changed `box-shadow: inset 2px 0 0 var(--live)` (+ value fade `--dur-base`, none under reduced-motion). Stretched link: `.corp::after{content:"";position:absolute;inset:0}` with `.row{position:relative}`, `.dart{position:relative;z-index:1}`. Tabs hover/focus-visible [§4]; meta line + legend styles (`lg0..lg3` tokens); footer `.more` (center, gap 16, 16/18 padding), `.btn` **36px ≥768 / 44px ≤767** (retire the 32px + 481px rules), `.rest`, `.flat`; strips unchanged band, `.sbody` rule so expanded rows share the board's start line (24px). Keep every other R2/R2.1 rule that R9 did not touch.
3. **`BoardRow.tsx`** [§2–§3] — DOM order kept (chip + corpCell → when + extras → rail); the corp `Link` is the stretched link; a row with no `rcept_no` keeps the `span` (no stretched target — there is nothing to open); **dateless row renders the label only** (no empty date span, no dash) and `StateBadge tbd` in the D-day cell (already so — keep); add `changed` prop/flag for the refresh edge; `extras` cell always rendered (the panel decides visibility via `data-extras`).
4. **`Board.tsx`** [§1, §4, §5] — `WINDOW_STEP = 15` (rewrite its doc comment: operator decision at the R9 gate, P7 Q3 closed); `<ol data-extras>` computed **per panel**: `"none"` when no row in the current tab's ranked list + both strips has an `offering` (① extras), else `"yes"`; the **meta line** under the tabs (`TAB_NOTE_KO` · `shownLine` with `{ranked}=rows.length`, `{shown}=min(shown, rows.length)`, mono numbers) + the **legend** (four spans, `LEGEND_*`); footer = `moreKo(step)` button + `remainingKo(hidden)` + `collapseToFirstKo(WINDOW_STEP)` when `shown > WINDOW_STEP`; none of the three when `hidden === 0` and `shown <= WINDOW_STEP`… precisely: more+rest only while `hidden > 0`, collapse only while `shown > WINDOW_STEP`. Strip toggle label `open ? COLLAPSE_KO : EXPAND_KO` with `aria-expanded`. Tab switch still resets the window; **refresh does not**.
5. **Auto-refresh** [§7] — the board re-reads `GET /board` (through `lib/api.ts`'s `getBoard()` from the browser — same-origin `/api/board` via the rewrite) every **60 s** (`REFRESH_INTERVAL_MS` constant, cited to Q10), paused while `document.hidden` and run once immediately on `visibilitychange` → visible; on success: if `freshness.as_of` changed → swap rows/counts/strips, show `REFRESHED_KO` beside the chip (mono 11, `--live` on `--live-tint`, 2×8) **until the next refresh**; if `as_of` unchanged → no visible change; compute **changed rows** (same `event_id` with a different `countdown.dday`/`countdown.date`/`offering` or newly inside the shown window) and mark them for one cycle; keep tab / `shown` (clamp to the new list length) / open strips / scroll / focus (rows keyed by `event_id`; if the focused row vanished, move focus to the list); on failure: nothing visible, retry next tick; stale result → R2 handling as today. **No spinner, no button, no text besides `갱신됨`.** Keep `page.tsx`'s server fetch as the initial render (the hero/summary/countdown stay server-rendered and must not remount — the refresh lives inside `Board`, which is already a client component). Reduced motion: no fade, edge shown, refresh continues.
6. **`Anchor.tsx` / `Anchor.module.css`** [§6] — stats card → **three `Stat` rows** (label left / value right, `justify-content:space-between`, `padding:9px 0`, dashed separators, last without), value mono 17/600 `nowrap`; the 2×2 grid rules go; `performance_reports` no longer rendered (type stays). Countdown caption: when `next_lapse.tie_count > 1` the caption's corp slot says `tieCountKo(n)` — check what the caption renders today in `Countdown.tsx`; if the caption carries no corp today, apply the rule only where a corp is printed.
7. **`LapseNotice.tsx`** [§6] — when `next_lapse.tie_count > 1` print `tieCountKo(tie_count)` in the `{corp}` slot, else `corp_name` as today; wrap the mono date and the tie phrase in `white-space:nowrap`; the sentence template itself is unchanged.
8. **Backend `next_lapse.tie_count`** [§6 / Q11] — in `src/mijual/web/reads.py` `load_summary`: `pending` is the ordered list whose head is `soonest`; add `tie_count = number of entries sharing soonest[1]` and pass it through `board_summary(...)` into `next_lapse` (find `board_summary` in `src/mijual/web/present/`; add the key only when `next_lapse` exists). Update `frontend/lib/types.ts` `BoardSummary.next_lapse` with `tie_count?: number`. One terse assertion in `tests/test_web_board.py`'s existing summary test (the fixture's tie situation, whatever it is — assert the count is consistent with the fixture; do not grow the suite).
9. **Hero plain-Enter** [§8] — `frontend/components/lookup/SearchRow.tsx` `onKeyDown` Enter: (a) candidates shown and `active < 0` → if the typed text **exactly equals** a candidate's name or code (trimmed, case-insensitive for codes) → navigate to that candidate; else `preventDefault` and `setActive(0)` (no navigation, no submit); (b) `active >= 0` → navigate as today; (c) no candidates shown → plain GET submit as today. The `/stocks` page and its copy are **untouched** (R11). Keep the existing SearchRow behaviour elsewhere (Esc, blur, ↑/↓ wrap, mouse).
10. **390 line breaks** [§8, §10] — `word-break: keep-all` on landing prose (hero subtitle, fact sentence, 소멸주의보 body, strip sentences, meta line) — prefer one rule on the landing's root/shell (`app/shell.css` or the landing modules; note which); `text-wrap: balance` on the hero subtitle; every mono value on the landing `white-space: nowrap` (dates, D-day, 건 counts, 금액, the 소멸주의보 date + tie phrase). Verify the three walk-10 breaks are gone at 390.
11. **Doc impact** (`phase.md` `## Doc impact`, one line each): frontend (R9 supersession: window 15, column plan, stretched-link rows + states, meta line + legend, strip 접기, three-stat card, tie rule, auto-refresh contract + interval, hero Enter rule, control heights, copy additions/deletions incl. `STAT_REPORTS_KO` and the gate-cost/disclaimer constants gone — P8 Q5 closed), api (`/board/summary` `next_lapse.tie_count`), qa (`## Regression Checklist` lines: 15-row window + 더 보기/남은/접기; whole-row click → detail; strip 펼치기↔접기 + aligned rows; three stats, no 읽은 실적보고서 in the DOM; auto-refresh: chip updates, no spinner, tab/window survive; hero 「삼성」+Enter selects first then Enter goes; 390 no split date; plus pytest/smoke counts if they move), experience/product if they describe the board's window or the stats card.

## Verify — fidelity AND function, in the operator's runtime

- `cd frontend && npm run typecheck && npm run build && npm run smoke` green (restore `next-env.d.ts` if the build rewrites it); `.venv/bin/python -m pytest` green; `python3 scripts/workflow.py validate`.
- **Build-prompt §11 완료 확인 — every box**, in `next dev` at `http://127.0.0.1:3000` and the tailnet origin, **and** in the production build, desktop (1512/1280) + 390px: 15 rows + 「15건 더 보기」+「남은 N건」 → click → 30 + 「처음 15건으로 접기」; 전체 tab 488 vs meta 386 both visible and explained; CB tab: no gap between key date and D-day, D-day flush right; board rows and expanded strip rows share x-coordinates (measure with `getBoundingClientRect`); rows ≥44px, values centred, no D-day wrapping; click anywhere on a row → event detail, `↗` → DART, Tab → row focus ring; strip 펼치기 → 접기; 추후결정 row = label only + 「추후결정」 in the D-day cell; legend present and colours match the ladder; three stats, 「읽은 실적보고서」 absent from the DOM; 소멸주의보 and the countdown caption agree, tie → 「N개 종목」 (today's data has a three-way tie on 2026-09-04 — it should read 3개 종목 once `tie_count` ships); **refresh: leave the page open ≥ 2 intervals** and show the chip/`갱신됨` behaviour without spinner and without the tab/window/strips/scroll resetting (if the corpus does not change within the window, prove the path by temporarily shortening the interval in dev or by observing the network requests + no visible change — say which); countdown does not remount across a refresh (seconds do not jump); 390: subtitle no one-syllable orphan, no split mono date, strip button full-width 44px; hero 「삼성」 + Enter → first candidate selected, Enter again → navigates; `prefers-reduced-motion`: no fade/blink, refresh continues, edge shown.
- **Functional sweep of the landing** — every visible control does something: hero input/submit + typeahead keyboard path, 4 tabs (hover/focus), row click / `↗`, 더 보기 / 접기 / 처음 15건으로 접기, both strip toggles and their rows, the R8 chrome around it still works.
- Re-run the whole qa `## Regression Checklist` (cumulative), then append this phase's lines via Doc impact.
- Evidence: terse — the §11 checklist with pass/fail, screenshots desktop + 390 (dev and prod), the refresh observation (network timestamps or chip change), the x-coordinate measurement for row alignment. No large test suites.

## Record

`result.md` (what changed, file list, §11 results, harness used, refresh evidence, any unsettled question); `phase.md`: Doc impact lines, `## Findings & Notes` for what later surfaces inherit (the refresh hook's shape if reusable, the SearchRow Enter rule R11 must keep, the `tie_count` field), `## Operator Questions` only for genuine design gaps (catalogue, do not decide).

## Don't

No commits, no status transitions, no `doc-new-version`, no edits under `docs/reference/design/rounds/*/output/`, no new Korean strings beyond §9, no restyling outside what R9 signed, no `AccountSlot`/chrome changes (Q12 is a later slice), no `/stocks` page changes (R11), no `rm -rf`.
