# Phase P10: Rebrand to 주주의관제탑

_Intent: see [intent.md](intent.md)._

## Objective

Replace the product's user-facing identity — name 미주알 → 주주의관제탑 (uniformly unspaced, no latin mark) and the MIJUAL wordmark → the operator's supplied logo plus a derived white variant for the cosmos-dark chrome — across every operator-visible surface: brand binaries, favicon, document title, chrome and ops copy, the signed Korean strings that name the product, and user-visible docs. Code identifiers (src/mijual, MIJUAL_* env vars, X-Mijual-CSRF) are explicitly out of scope. Lands before P4 so the deck, demo video and submission carry the new name.

## Slices

<!-- slices:begin -->
_Generated by `python3 scripts/workflow.py rebuild` from each slice's `slice.json`; never hand-edit this block._

| Slice | Name | Kind / risk | Status | Outcome | Result |
|---|---|---|---|---|---|
| `P10.DECOMP` | decompose phase | `decomposition / high` | `done` | Cut five high-risk middle slices as bare folders (S1 binaries → S2 chrome/identity → S3 Korean copy → S4 docs → S5 runtime sweep); seeded phase.md with scope, five decisions and four operator questions | [result.md](slices/P10.DECOMP/result.md) |
| `P10.S1` | brand binaries: land the mark, derive the white variant, rewrite the assets README | `implementation / high` | `done` | Landed juju-logo-source.png byte-exact plus juju-wordmark-{black,white}.png (1213x319, alpha-identical) and rewrote the assets README around three provenance classes; the four mijual-* binaries stay for S2 | [result.md](slices/P10.S1/result.md) |
| `P10.S2` | chrome, ops and document identity: wordmark rewiring, retire the mijual-* set, ring vocabulary, both titles, ops mark, favicon decision | `implementation / high` | `done` | Chrome repointed at juju-wordmark-white.png {1213,319} via WORDMARK_*, ring vocabulary gone, four mijual-*.png deleted, both titles + OPS_MARK + alt + © now 주주의관제탑; verified dev and production, no favicon created | [result.md](slices/P10.S2/result.md) |
| `P10.S3` | Korean copy sweep: every rendered string naming the product (name swap only) | `implementation / high` | `todo` | — | — |
| `P10.S4` | docs and repo-visible prose: in-scope product-name occurrences only | `docs / high` | `todo` | — | — |
| `P10.S5` | fidelity sweep in the operator runtime: dev and production, desktop and mobile | `qa / high` | `todo` | — | — |
| `P10.REVIEW` | phase review | `review / high` | `todo` | — | — |
<!-- slices:end -->

## Context

- **Scope: user-facing only.** Code identifiers are out by operator decision — `src/mijual/`,
  every `MIJUAL_*` env var, `X-Mijual-CSRF`, the repo directory name, `package.json`
  `"name": "mijual-frontend"`, and the Claude Design project's own name ("Mijual Design
  System", `docs/current/frontend.md:107`). Do not rename them. **But Korean strings inside
  Python that reach a user are in scope** — `src/mijual/web/app.py:57` `TITLE = "미주알 API"`
  (the OpenAPI title). Comments — Python, TS or CSS — are prose about the product:
  documentation, not product copy, and **no slice owns them** (see the S3/REVIEW note below).
- **The name is uniformly the unspaced `주주의관제탑`.** No spaced variant, no latin mark —
  `MIJUAL` and `MIJUAL OPS` are retired, not romanized.
- **No design round.** The operator delivered a finished mark, so there is no `co-work` slice,
  no `handoff.md`, no `design-cowork`. Apply-only phase. Do not re-open it.
- **Operator runtime is declared** — `docs/current/operations.md` `## Operator Runtime`, no
  `UNFILLED`: `make stack-up`; dev `next dev` at `http://127.0.0.1:3000`; Chrome desktop on
  this Mac plus a mobile viewport; **and additionally** `npm run build && npm run start`.
  **No slice stops to ask for it, and none may verify anywhere else.** Deadline: P4 ships by
  **2026-09-07 10:00**.
- **`make stack-up` currently fails at `db-up`** — host port 5433 is held by
  `changple_web_dev_postgres`, an unrelated project; not caused by this phase. S2's way
  through: same image, same `mijual_mijual-pgdata` volume on **5434** via a scratchpad compose
  fragment using the **`!override`** tag (a plain override *merges* port lists), then
  `DATABASE_URL=...@localhost:5434/mijual` on the API. Browser-facing runtime unaffected;
  restore the container from `compose.yaml` after.

## Decomposition

Five middle slices, cut on one rule: **the slice that produces the binary must not also be the
slice that decides whether it reads.** Full reasoning in `slices/P10.DECOMP/result.md`.

| slice | kind / risk | what it owns |
|---|---|---|
| `P10.S1` | implementation / high | **Done** — binaries, provenance, the natural pair. |
| `P10.S2` | implementation / high | **Done** — rewiring, `RING_*` retirement, the four deletions, both titles, `OPS_MARK`, favicon (nothing shipped). |
| `P10.S3` | implementation / high | Every **rendered** string naming the product across `frontend/` plus `app.py` `TITLE`. **Name swap only** — no sentence rewritten, no Korean invented. |
| `P10.S4` | docs / high | In-scope product-name prose in `docs/current/*.md`, `frontend/README.md`, `frontend/package.json` `description`, `pyproject.toml` `description`, `Makefile:1`. Appends `## Doc impact`; runs **no** `doc-new-version`. |
| `P10.S5` | qa / high | Fidelity sweep across every changed surface in the operator runtime — dev and production, desktop and mobile, reader chrome and `/ops` and `/docs`. The acceptance gate depends on this. |

**S3/S4 stay separate because they fail differently** — S3 touches **signed design strings**
under RESPECT THE DESIGN (swap the name, never the sentence), while S4's opposite risk is
**falsifying history** by rewriting supersession rows that record the retired name on purpose.

## Decisions

- **S2/S3 string split, settled at S2** (`DECOMP` had listed the same sites twice). **S2 owned
  the identity strings bound to the mark and its chrome:** `chrome/copy.ts` `BRAND_ALT_KO` +
  `COPYRIGHT_KO`, both `metadata.title`s, `ops/copy.ts` `OPS_MARK` — all four **done**. **S3
  owns product-name prose everywhere else**, and the two sites S2 deliberately left standing
  are `components/event/copy.ts:114` (실권주 disclaimer) and `src/mijual/web/app.py:57`.
- **The natural pair is `1213x319`, reached by trimming** (`-trim +repage`). `Wordmark.tsx`
  renders height-constrained and passes the intrinsic pair so the browser reserves the right
  box; the untrimmed 2560x1440 would reserve one ~13x too wide.
- **The recolor is settled — S1 ran** `-trim +repage -channel RGB +level-colors white,white
  +channel -define png:color-type=6`; `+level-colors` alone flattens alpha and without the
  `-define` IM writes GrayscaleAlpha. Evidence: the assets README.
- **Provenance has three classes** (README): **A** design-project export (the font, and the
  four now-deleted `mijual-*`), **B** operator delivery (`juju-logo-source.png`), **C**
  repo-generated derivative (the two wordmarks). "Byte-for-byte from the design project" is A
  only — the assertion `Wordmark.tsx` used to make, corrected by S2.
- **A derivation is verified by pixels, not file sha256** — IM stamps `png:tIME`, so a re-run
  gives identical pixels and a different hash. Never "fix" a hash by re-deriving.
- **The black variant is for light surfaces only** — its near-white RGB sits in fully
  transparent pixels and would fringe on cosmos under downscale filtering. The white variant is
  immune (every pixel `#FFFFFF`); S2 saw no fringing at 10x.
- **Constants are `WORDMARK_WHITE` / `WORDMARK_NATURAL`** (S2). Only `RING_` was stale; not
  `JUJU_*` (bakes a filename into the concept), not `BRAND_MARK_*` (implies a symbol mark,
  which is the open favicon question).
- **The phase takes `accept-gate --require`** — it changes what the operator sees on every page.

## Doc impact

_One line per durable-truth change: `- <doc>.md: <what changed> (<slice>)`. Append only; consolidated into versions at the review, never per slice._

- frontend.md: brand binaries replaced — the R2 ring wordmark retires for `juju-wordmark-white.png` (1213x319, 3.80:1), the assets README now records three provenance classes and verifies derivations by pixel signature rather than file sha256 (P10.S1)
- product.md: the symbol mark / favicon gap that R2 closed is **open again** — the new mark is a Korean wordmark with a sparkle cluster and no ring, and it does not reduce to 32px (P10.S1)
- frontend.md: the chrome loads `/assets/juju-wordmark-white.png` at intrinsic `{1213,319}` through `WORDMARK_WHITE` / `WORDMARK_NATURAL` (the `RING_*` vocabulary is gone); the four `mijual-*.png` are deleted, and `docs/current/frontend.md:119,299` still describe the ring (P10.S2)
- operations.md: `make stack-up` cannot bind host port 5433 while another project holds it; the documented way through is the same image + `mijual_mijual-pgdata` volume on 5434 via a `!override` compose fragment plus `DATABASE_URL=...@localhost:5434/mijual` (P10.S2)

## Operator Questions

- **The ops mark's treatment — measured at S2, and it does not hold.** The string is now
  `주주의관제탑 운영` (implemented; the operator was told and did not object but never confirmed
  it in words). `Ops.module.css` `.mark`/`.doorMark` are **untouched** and still set
  `--font-mono` + `letter-spacing: 0.08em`, per R7's "an identifier, so it stays raw and mono".
  What renders (CDP `CSS.getPlatformFontsForNode`, both nodes, dev and production): **IBM Plex
  Mono SemiBold — 1 glyph (the space)**; **Apple SD Gothic Neo SemiBold — 8 glyphs (all the
  Hangul)**. So mono now styles exactly one character. Visible consequences: the 관제탑␣운영 gap
  reads as a double space; the mark's face is the **OS's**, so it differs off macOS; and in the
  bar it sits among Pretendard Korean tab labels and no longer reads as an identifier at all,
  while the `mijual:lock:pipeline free` chip beside it still does. **S2's recommendation, not
  taken:** keep the string, drop `--font-mono` + tracking from those two rules and let
  Pretendard 600 carry it. Signed styling, so the operator decides. (from P10.DECOMP, measured
  by P10.S2)
- **The signed chrome heights — h19/h17 implemented, measured, hierarchy inverted.** Nav
  `<img>` computes to **72.24x19** with a **9.65px** Korean band; footer **64.64x17** with
  **8.63px**. The adjacent nav label is 13.5px and the footer source line 12px, so the mark is
  **0.72x** the type beside it — where the retired ring was **1.07x**. Not illegible (clean at
  retina 2x, no fringing), but the brand is now the smallest text in its own bar. Layout is
  fine: the slot narrows 119.6 → 72.24px, the 24px gap is unchanged, nothing wraps or
  overflows at 1280 or 390. **S2's recommendation, acceptable in one word: `h27` nav / `h24`
  footer** — band 13.7/12.2px, parity with the adjacent type, boxes still narrower than the
  ring's. Raising it deviates from a signed number, so the operator decides at the gate.
  Note also: box-centring puts the glyph band 4.18px below the 52px bar's optical centre (6.14
  at h27), because the ink is bottom-half only. (from P10.DECOMP, measured by P10.S2)
- **The favicon: the mark does not reduce.** At 32px it is illegible mush. **S2 shipped nothing
  and cropped nothing** and confirmed the pre-existing state is intact: no `app/icon.*`, no
  `favicon.ico`, no `manifest`, no `openGraph`, no `icons` key, and zero `link[rel*=icon]` in
  dev or production. The assets README's rule forbids substituting, generating or placeholding
  an image. The operator chooses: (a) ship no favicon, as today; (b) supply a square symbol
  export; or (c) authorize cropping the sparkle cluster as a symbol. (from P10.DECOMP)
- **P4's mail subject still carries the retired name.** The signed R5 spec, quoted in
  `src/mijual/mail.py:14` as `[미주알] {jongmok} — {magamname} D-{n} ({date})`, is unimplemented
  and out of this phase's scope. P4 will render `[미주알]` unless the operator re-signs the
  subject. (from P10.DECOMP)

## Notes for later slices

- **(from P10.S2, for P10.S3)** S2 already changed `chrome/copy.ts:38` `BRAND_ALT_KO` →
  `주주의관제탑` and `:172` `COPYRIGHT_KO` → `© 주주의관제탑`, and both `metadata.title`s and
  `OPS_MARK`. **Your remaining rendered set is exactly two:** `components/event/copy.ts:114`
  (the 실권주 disclaimer) and `src/mijual/web/app.py:57` (`TITLE = "미주알 API"`). **Name swap
  only.** 미주알 and 주주의관제탑 both end in a consonant, so 은/이/을 particles do not change —
  check both sites rather than assuming, and if a sentence reads badly, **append an operator
  question; do not rewrite.** S2 also rewrote the doc comments directly above `BRAND_ALT_KO`
  and `COPYRIGHT_KO` (they asserted the old values and would have been false); it touched no
  other comment.
- **(from P10.S2, for P10.REVIEW)** **Code doc comments are unowned.** `## Context` rules them
  out of scope as documentation, but several now *describe rendered output that changed* — e.g.
  `chrome/Footer.tsx:15` still draws the footer as `워드마크 h17 + "… · © 미주알"`. Defensible as
  a quotation of R2, misleading as a description. A decision for the review, not a silent fix.
- **(from P10.DECOMP, for P10.S4)** The raw grep massively overstates the work. In
  `docs/current/` only **12** Korean `미주알` hits exist (product 4, frontend 4, decisions 1,
  experience 1, operations 1), plus `security.md:396` and `decisions.md:292` (both in scope) and
  `frontend.md:107` "Mijual Design System" (**out**). **Two lines are history and must NOT be
  changed:** `frontend.md:120` and `decisions.md:674` record that the *"MIJUAL + 한글 미주알
  병기"* lockup was superseded. Everything matching `MIJUAL_*`, `mijual.<module>`, `mijual:`,
  `X-Mijual-CSRF`, `src/mijual` is out. Repo prose in scope: `frontend/README.md:1,87`,
  `frontend/package.json:5`, `pyproject.toml:8` `description` (`name = "mijual"` is **out**),
  `Makefile:1`. Append `## Doc impact` lines; **never** run `doc-new-version` here.
- **(from P10.DECOMP + P10.S2, for P10.S5)** Sweep: reader nav + footer, the `/ops` bar **and**
  door, both titles, the 404, the 실권주 disclaimer, `/docs` (OpenAPI title), and a grep proving
  no stale `mijual-*.png` **code** reference — dev **and** production, desktop **and** 390px.
  Three things S2 learned: no type or test catches a wrong asset path (`tsc` was clean over a
  404 URL), so assert `img.complete && img.naturalWidth > 0` per `<img>`;
  `document.fonts.check(…'IBM Plex Mono', '주주의관제탑')` returns **`true`** and is a false
  positive (use `CSS.getPlatformFontsForNode`); and the `/ops` **bar** needs `MIJUAL_OPS_ID` +
  `MIJUAL_OPS_PASSWORD` on the API process, the door does not. Expected non-live grep hits: 13
  assets README, 1 `chrome/copy.ts:25`, 2 generated `docs/current/frontend.md`, 4
  `docs/reference/design/`.

## Now

`P10.S2` is done — **the new mark is live on every page**. `chrome/copy.ts` points at
`/assets/juju-wordmark-white.png` with `{1213,319}` through `WORDMARK_WHITE` /
`WORDMARK_NATURAL`; the `RING_*` vocabulary and the four `mijual-*.png` are gone; `BRAND_ALT_KO`,
`COPYRIGHT_KO`, both `metadata.title`s and `OPS_MARK` all read 주주의관제탑. Verified in the
operator runtime, **dev and production**, desktop and 390px: both `<img>`s load at 72.24x19 and
64.64x17, the retired paths 404, no fringing, and no favicon was created.

Next: **P10.S3** — two rendered strings only (`event/copy.ts:114`, `app.py:57`); its tagged note
says what S2 already did, so it need not re-sweep the chrome.

Open, none blocking, all four sharpened or confirmed by S2: the ops mono treatment (measured —
mono now styles one space), the signed h19/h17 (measured — 0.72x the type beside it, `h27/h24`
recommended), the favicon (nothing shipped, three options stand), and P4's `[미주알]` subject.
`make stack-up` needs the 5434 workaround in `## Context` until port 5433 is free. The phase
takes `accept-gate --require`.
