# Phase P10: Rebrand to 주주의관제탑

_Intent: see [intent.md](intent.md)._

## Objective

Replace the product's user-facing identity — name 미주알 → 주주의관제탑 (uniformly unspaced, no latin mark) and the MIJUAL wordmark → the operator's supplied logo plus a derived white variant for the cosmos-dark chrome — across every operator-visible surface: brand binaries, favicon, document title, chrome and ops copy, the signed Korean strings that name the product, and user-visible docs. Code identifiers (src/mijual, MIJUAL_* env vars, X-Mijual-CSRF) are explicitly out of scope. Lands before P4 so the deck, demo video and submission carry the new name.

## Slices

<!-- slices:begin -->
_Generated by `python3 scripts/workflow.py rebuild` from each slice's `slice.json`; never hand-edit this block._

| Slice | Name | Kind / risk | Status | Outcome | Result |
|---|---|---|---|---|---|
| `P10.DECOMP` | decompose phase | `decomposition / high` | `done` | Cut five high-risk slices as bare folders; seeded phase.md with scope, decisions and four operator questions | [result.md](slices/P10.DECOMP/result.md) |
| `P10.S1` | brand binaries: land the mark, derive the white variant, rewrite the assets README | `implementation / high` | `done` | Landed juju-logo-source.png byte-exact plus juju-wordmark-{black,white}.png (1213x319, alpha-identical); assets README rewritten | [result.md](slices/P10.S1/result.md) |
| `P10.S2` | chrome, ops and document identity: wordmark rewiring, retire the mijual-* set, ring vocabulary, both titles, ops mark, favicon decision | `implementation / high` | `done` | Chrome repointed at juju-wordmark-white.png, ring vocabulary gone, four mijual-*.png deleted, titles/OPS_MARK/alt/© swapped; verified dev + production | [result.md](slices/P10.S2/result.md) |
| `P10.S3` | Korean copy sweep: every rendered string naming the product (name swap only) | `implementation / high` | `done` | 15 live strings swapped incl. 13 agent prompt strings spelling the product 미주얼; live /ask confirms the assistant names 주주의관제탑 | [result.md](slices/P10.S3/result.md) |
| `P10.S4` | docs and repo-visible prose: in-scope product-name occurrences only | `docs / high` | `done` | Renamed 4 repo-prose sites; built the line-level ledger P10.REVIEW executes over 6 docs; nothing under docs/ touched | [result.md](slices/P10.S4/result.md) |
| `P10.S5` | fidelity sweep in the operator runtime: dev and production, desktop and mobile | `qa / high` | `todo` | — | — |
| `P10.REVIEW` | phase review | `review / high` | `todo` | — | — |
<!-- slices:end -->

## Context

- **Scope: user-facing only.** Code identifiers are out by operator decision — `src/mijual/`,
  every `MIJUAL_*`, `X-Mijual-CSRF`, the repo directory, `package.json` `"name"`, the local DB
  credentials, and the design project's own name ("Mijual Design System"). **But any string
  the product says to a user is in**, including Korean inside Python: `app.py:57` `TITLE` and
  the agent's **live prompt strings** (S3 — see Decisions). Comments and docstrings are
  documentation, not product copy, and **no slice owns them** (see the REVIEW note below).
- **The name is uniformly the unspaced `주주의관제탑`.** No spaced variant, **no latin mark** —
  `MIJUAL` / `MIJUAL OPS` are retired, and there is no romanized replacement to substitute.
- **No design round.** The operator delivered a finished mark. Apply-only; do not re-open it.
- **Operator runtime is declared** — `docs/current/operations.md` `## Operator Runtime`, no
  `UNFILLED`: `make stack-up`; dev `next dev` at `http://127.0.0.1:3000`; Chrome desktop on
  this Mac plus a mobile viewport; **and additionally** `npm run build && npm run start`.
  **No slice stops to ask for it, and none may verify anywhere else.** Deadline: P4 ships by
  **2026-09-07 10:00**.
- **`make stack-up` still fails at `db-up`** — host port 5433 is held by
  `changple_web_dev_postgres`, an unrelated project. Way through (S2, re-used by S3): same
  image + same `mijual_mijual-pgdata` volume on **5434** via a scratchpad compose fragment
  using the **`!override`** tag (a plain override *merges* port lists), then
  `DATABASE_URL=…@localhost:5434/mijual` on the API. Browser-facing runtime unaffected; restore
  with `make stack-down` + `docker compose -f compose.yaml up --no-start --force-recreate postgres`.

## Decomposition

Five middle slices, on one rule: **the slice that produces the binary must not also be the
slice that decides whether it reads.** Full reasoning in `slices/P10.DECOMP/result.md`.

| slice | what it owns |
|---|---|
| `P10.S1`–`S4` | **All done** — binaries + provenance; rewiring, `RING_*` retirement, four deletions, both titles, `OPS_MARK`, favicon (nothing shipped); every string the product *renders, serves or speaks*; repo prose + the review's doc ledger. |
| `P10.S5` | Fidelity sweep over every changed surface, dev and production, desktop and mobile. The acceptance gate depends on it. |

**S3/S4 fail differently**: S3 touched **signed design strings** (swap the name, never the
sentence); S4's opposite risk is **falsifying history** by rewriting supersession rows that
record the retired name on purpose.

## Decisions

- **S2/S3 string split, settled at S2.** S2 owned the identity strings bound to the mark
  (`chrome/copy.ts` `BRAND_ALT_KO` + `COPYRIGHT_KO`, both `metadata.title`s, `ops/copy.ts`
  `OPS_MARK`); S3 owned the product-name prose everywhere else. Both **done**.
- **The assistant's own name is user-facing — and it was a third spelling (S3).** The agent's
  system instruction and tool descriptions called the product **미주얼** (얼, not 알), which
  the decomposition's `미주알` grep never saw. The prompt decides the name the model *says* in
  Korean answers, so it is in scope: **13 live prompt strings** swapped (9 `instructions.py`,
  1 `tools.py` `DATA_BOUNDARY`, 3 `declarations.py`). The product had been shipping two
  spellings of its own name; the rename incidentally normalises that. Comments keep 미주얼.
- **The latin mark is reworded, never substituted (S3)** — there is no romanized replacement.
  `instructions.py:52` `미주얼(Mijual)` lost the parenthesized gloss outright;
  `declarations.py:139` used *Mijual* as an **English noun** and became "this product itself",
  echoing the noun its own first sentence already uses.
- **`instructions.py` is a signed static cache prefix** — its order is a cache key (its own
  header, `:26–36`). S3 changed only names, reflowed nothing; the one-time implicit-cache
  invalidation is an accepted cost.
- **Binaries, settled by S1/S2** (detail: `frontend/public/assets/README.md`,
  `P10.S1/result.md`): natural pair **1213x319** via `-trim +repage`; the white variant is a
  repo-generated derivative (`+level-colors white,white` plus `-define png:color-type=6`, or IM
  writes GrayscaleAlpha) **verified by pixels, not sha256** (IM stamps `png:tIME` — never "fix"
  a hash by re-deriving); the black variant is light-surfaces-only; constants `WORDMARK_WHITE`
  / `WORDMARK_NATURAL`.
- **The phase takes `accept-gate --require`** — it changes what the operator sees on every page.

## Doc impact

_One line per durable-truth change: `- <doc>.md: <what changed> (<slice>)`. Append only; consolidated into versions at the review, never per slice._

- frontend.md: brand binaries replaced — the R2 ring wordmark retires for `juju-wordmark-white.png` (1213x319, 3.80:1), the assets README now records three provenance classes and verifies derivations by pixel signature rather than file sha256 (P10.S1)
- product.md: the symbol mark / favicon gap that R2 closed is **open again** — the new mark is a Korean wordmark with a sparkle cluster and no ring, and it does not reduce to 32px (P10.S1)
- frontend.md: the chrome loads `/assets/juju-wordmark-white.png` at intrinsic `{1213,319}` through `WORDMARK_WHITE` / `WORDMARK_NATURAL` (the `RING_*` vocabulary is gone); the four `mijual-*.png` are deleted, and `docs/current/frontend.md:119,299` still describe the ring (P10.S2)
- operations.md: `make stack-up` cannot bind host port 5433 while another project holds it; the documented way through is the same image + `mijual_mijual-pgdata` volume on 5434 via a `!override` compose fragment plus `DATABASE_URL=...@localhost:5434/mijual` (P10.S2)
- backend.md + architecture.md: the agent's system instruction and tool descriptions name the product 주주의관제탑 — 13 live prompt strings across `instructions.py`, `tools.py` `DATA_BOUNDARY` and `declarations.py`; the retired latin gloss `미주얼(Mijual)` is dropped from `_ROLE` and reworded out of `save_feedback`'s description; the static cache prefix is invalidated once (P10.S3)
- api.md: the served OpenAPI title is `주주의관제탑 API` (`src/mijual/web/app.py:57`), so `/docs` and `/openapi.json` carry it (P10.S3)
- security.md: `docs/current/security.md:307` still spells the product 미주얼 inside the lethal-trifecta paragraph — the only 미주얼 in `docs/current/` (P10.S3, for S4)
- product.md: 4 renames (`:32,243,283`; `:267` possessive → reword) — every line and its replacement in `slices/P10.S4/result.md` §Ledger 2.1 (P10.S4)
- frontend.md: 3 renames (incl. `:675`'s `© 미주알`, a build string S2 already changed), 3 now-**false** ring/asset sites (`:119`, `:299-302`, `:658-660`), and `:120` is HISTORY — §Ledger 2.2 (P10.S4)
- experience.md: `:34` possessive rename plus two more now-false "ring wordmark" lines (`:28`, `:102`) — §Ledger 2.3 (P10.S4)
- operations.md: one rename, the Korean clause of the retired `NEXT_PUBLIC_VOCKY_SRC` row (`:369`) only — §Ledger 2.4 (P10.S4)
- decisions.md: `:292` reworded; `:594` verbatim quote and `:649,653-657,665,674-675` dated domain sheet + supersessions are HISTORY — §Ledger 2.5 (P10.S4)
- security.md: the lone `미주얼` (`:307`) plus 2 `Mijual-side` rewords (`:151`, `:396`) — §Ledger 2.6 (P10.S4)
- repo prose (unversioned, landed here): `frontend/README.md:1,87`, `frontend/package.json:5`, `pyproject.toml:8` renamed; both `name` fields left (P10.S4)

## Operator Questions

- **The ops mark's treatment — measured at S2, and it does not hold.** The string is now
  `주주의관제탑 운영` (implemented; the operator was told and did not object but never confirmed
  it in words). `Ops.module.css` `.mark`/`.doorMark` are **untouched** and still set
  `--font-mono` + `letter-spacing: 0.08em`, per R7's "an identifier, so it stays raw and mono".
  What renders (CDP `CSS.getPlatformFontsForNode`, both nodes, dev and production): **IBM Plex
  Mono SemiBold — 1 glyph (the space)**; **Apple SD Gothic Neo SemiBold — 8 glyphs (all the
  Hangul)**. So mono now styles exactly one character. Visible consequences: the 관제탑␣운영 gap
  reads as a double space; the mark's face is the **OS's**, so it differs off macOS; and in the
  bar it sits among Pretendard Korean tab labels and no longer reads as an identifier at all,
  while the `mijual:lock:pipeline free` chip beside it still does. **S2's recommendation, not
  taken:** keep the string, drop `--font-mono` + tracking from those two rules and let
  Pretendard 600 carry it. Signed styling, so the operator decides. (from P10.DECOMP, measured
  by P10.S2)
- **The signed chrome heights — h19/h17 implemented, measured, hierarchy inverted.** Nav
  `<img>` computes to **72.24x19** with a **9.65px** Korean band; footer **64.64x17** with
  **8.63px**. The adjacent nav label is 13.5px and the footer source line 12px, so the mark is
  **0.72x** the type beside it — where the retired ring was **1.07x**. Not illegible (clean at
  retina 2x, no fringing), but the brand is now the smallest text in its own bar. Layout is
  fine: the slot narrows 119.6 → 72.24px, the 24px gap is unchanged, nothing wraps or
  overflows at 1280 or 390. **S2's recommendation, acceptable in one word: `h27` nav / `h24`
  footer** — band 13.7/12.2px, parity with the adjacent type, boxes still narrower than the
  ring's. Raising it deviates from a signed number, so the operator decides at the gate.
  Note also: box-centring puts the glyph band 4.18px below the 52px bar's optical centre (6.14
  at h27), because the ink is bottom-half only. (from P10.DECOMP, measured by P10.S2)
- **The favicon: the mark does not reduce.** At 32px it is illegible mush. **S2 shipped nothing
  and cropped nothing** and confirmed the pre-existing state is intact: no `app/icon.*`, no
  `favicon.ico`, no `manifest`, no `openGraph`, no `icons` key, and zero `link[rel*=icon]` in
  dev or production. The assets README's rule forbids substituting, generating or placeholding
  an image. The operator chooses: (a) ship no favicon, as today; (b) supply a square symbol
  export; or (c) authorize cropping the sparkle cluster as a symbol. (from P10.DECOMP)
- **P4's mail subject still carries the retired name.** The signed R5 spec, quoted in
  `src/mijual/mail.py:14` as `[미주알] {jongmok} — {magamname} D-{n} ({date})`, is unimplemented
  and out of this phase's scope. P4 will render `[미주알]` unless the operator re-signs the
  subject. (from P10.DECOMP)
- **Dev-tooling banners: `DECOMP` put them in scope, S4's plan took them out — left unchanged.**
  `Makefile:1` (`# Mijual dev stack.`), `Makefile:110`, `compose.yaml:1`. But `make stack-status`
  prints `── mijual dev stack ──` to the operator's own terminal, so "mentions it in passing" is
  arguable. Rename the banners too, or leave them? (from P10.S4)

## Notes for later slices

- **(from P10.S2, for P10.REVIEW)** **Code doc comments are unowned.** `## Context` rules them
  out of scope as documentation, but several now *describe rendered output that changed* — e.g.
  `chrome/Footer.tsx:15` still draws the footer as `워드마크 h17 + "… · © 미주알"`, and
  `frontend/lib/api.ts:2` still calls it "the 미주알 API". Defensible as quotations of the
  record, misleading as descriptions. A decision for the review, not a silent fix. S2 and S3
  each fixed only comments that would be **false about the line directly beneath**.
- **(from P10.DECOMP + P10.S2 + P10.S3, for P10.S5)** Sweep: reader nav + footer, the `/ops`
  bar **and** door, both titles, the 404, the 실권주 disclaimer (live data:
  `/events/20260223002079` and `/stocks/00113261`), `/docs` + `/openapi.json`, and a grep
  proving no stale `mijual-*.png` **code** reference — dev **and** production, desktop **and**
  390px. **Plus the assistant's own name:** ask `/ask` `너 이름이 뭐야?` and check the answer
  says 주주의관제탑 (S3 proved it live in dev; production untested). Three things S2 learned:
  no type or test catches a wrong asset path (`tsc` was clean over a 404 URL), so assert
  `img.complete && img.naturalWidth > 0` per `<img>`;
  `document.fonts.check(…'IBM Plex Mono', '주주의관제탑')` returns **`true`** and is a false
  positive (use `CSS.getPlatformFontsForNode`); and the `/ops` **bar** needs `MIJUAL_OPS_ID` +
  `MIJUAL_OPS_PASSWORD` on the API process, the door does not. Expected non-live grep hits: 13
  assets README, 1 `chrome/copy.ts:25`, 2 generated `docs/current/frontend.md`, 4
  `docs/reference/design/`.

## Now

`P10.S4` is done — **but not the job `DECOMP` wrote.** `docs/current/*.md` are generated snapshots
(verified: all **11** are byte-identical to their newest `docs/versions/` file), so no slice can edit
them and only `P10.REVIEW` may version them. S4 therefore renamed the **4 real repo-prose sites**
and wrote the review's **ledger**: `slices/P10.S4/result.md` §Ledger — 6 documents, every line, its
replacement, its reason.

**`P10.REVIEW` reads that ledger before running `doc-new-version`.** It separates four lookalikes:
a name to change; a latin mark to *reword* (`Mijual-side` is not writable English); content now
**false** (the ring wordmark, "five binary assets"); and **history that must not move** — a verbatim
operator quote, a dated domain fact sheet, two supersession rows. §5 lists 6 choices left open.

Next: **P10.S5** — the fidelity sweep in the operator runtime; its tagged note is unconsumed.
Open, none blocking: ops mono treatment, signed h19/h17, favicon, P4's `[미주알]` subject, and now
the dev-tooling banners. `make stack-up` still needs the 5434 workaround.
