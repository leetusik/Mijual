# Result — P10.REVIEW (phase review, gated)

- **status:** `done`
- **summary:** Validated P10 as a whole (pytest **154**, build 15/15, typecheck, smoke **22/22**,
  `validate`, plus the cumulative `## Regression Checklist` — `gates run` twice byte-identical over
  **710** field rows, `estimate report` twice byte-identical, `scheduler once --offline` six stages
  at 0 req / 0 calls, `extract recheck` and `evalset refresh-recall` idempotent, the exposure
  invariant re-derived **0 / 0 / 0**, the four AST import scans and the anonymity / tool-signature
  guards green, secret scan clean), then **opened the running product myself** in the manifest
  runtime — `next dev` **and** a production build, 1280@2 **and** 390@3 — and re-checked every
  headline claim rather than trusting S5's report. Everything holds. Routed **all six** operator
  questions, consolidated the phase into **10 doc versions** from S4's line-level ledger with the
  four history sites left byte-exact, and returned a `pass` with the acceptance walkthrough.
- **files_changed:**
  - `docs/versions/{product/v0009, frontend/v0008, experience/v0008, operations/v0010,
    decisions/v0010, security/v0007, api/v0007, backend/v0007, architecture/v0009, qa/v0009}_*.md`
    (created via `doc-new-version`)
  - `docs/current/*.md` + `docs/index.json` (engine-regenerated by `rebuild-docs`)
  - `works/phases/active/P10/phase.md` (edited, 175 lines / 16,276 bytes — under budget)
  - `works/phases/active/P10/slices/P10.REVIEW/result.md` (this file)
  - **no source file touched** — `git status` shows zero changes under `frontend/`, `src/`,
    `tests/`, `Makefile`, `compose.yaml` or `.env`
- **validation:** every command below passed — §1
  - `.venv/bin/python -m pytest` — **154 passed**, 1 pre-existing starlette warning
  - `cd frontend && npm run build` — **pass**, 16 route entries, static pages **15/15**
  - `cd frontend && npm run typecheck` — **pass**, no output
  - `cd frontend && npm run smoke` — **pass**, `tests 22 / pass 22 / fail 0`
  - `python3 scripts/workflow.py validate` — **pass** (2 pre-existing `P9.S1`/`P9.S1B` unknown-kind
    warnings, history not findings)
  - the whole cumulative `## Regression Checklist` — §1.2
  - real browser, **dev** and **production**, desktop + 390 — §2
  - `python3 scripts/workflow.py rebuild-docs` + `docs` — 11/11 `docs/current` byte-identical to
    their newest version — §3.4
- **deviations:** two, both inside the plan's own invitation to decide S4's flagged calls — §4
- **doc_impact:** n/a (review slice — the phase's "Doc impact" list was *consumed*, and one
  consolidation line appended to it naming the 10 versions)
- **doc_versions:** 10, listed in §3
- **review_verdict:** `pass`
- **walkthrough:** §6
- **explain:** `not written — run /explain for this phase`

---

## 1. Validation — the phase as a whole

### 1.1 The mechanical top

| command | result |
|---|---|
| `.venv/bin/python -m pytest` | **154 passed**, 1 warning (pre-existing `StarletteDeprecationWarning`) |
| `npm run build` | `✓ Compiled successfully`; `Generating static pages (15/15)`; 16 route entries |
| `npm run typecheck` | clean, no output |
| `npm run smoke` | `tests 22 · pass 22 · fail 0` |
| `workflow validate` | `Workflow validation passed.` + the 2 pre-existing P9 warnings |

### 1.2 The cumulative `## Regression Checklist` — the review's own job

S5 deliberately ran only the cheap top of it. I ran the rest. **Everything scriptable, I ran in
full**; the P8/P9 surface blocks are ~80 browser assertions and I ran a **named sample** of the
highest-value ones rather than claiming all 80 — stated plainly so the number is not overread.

| checklist line | how it was run | result |
|---|---|---|
| `gates run` twice → byte-identical, 710 field rows | `cmp` on two full runs | **byte-identical**; `events: 1359 judged, 201 version(s), 710 field row(s)` — the stated 710 |
| the **four AST import scans** | they are pytest tests — `test_web_smoke.py::…no_request_path_module_imports_a_spending_module`, `test_present.py::…derivation_layer…`, `test_agent_tools.py::…agent_package_imports_no_spending_module`, + the HTTP-client scan | green inside the 154 |
| the **anonymity scan** | `test_web_portfolio.py::…sample_is_anonymous_and_carries_no_account_fact` | green |
| the **tool signature check** | `test_agent_tools.py` (`get_portfolio()` takes nothing) | green |
| ops surface carries no unsafe method | in the suite | green |
| **no reader-facing quota / storage-denial copy** | grep for 「남은 질문」/「저장 이력 없음」/「탭을 닫으면」 + `localStorage` over `frontend/components`, `frontend/lib` | **pass** — every hit is a comment or `CONVERT_SESSION_KO`, the sanctioned 보유량 conversion line on a different surface; `localStorage` appears **only** inside comments saying "never `localStorage`" |
| **exposure invariant re-derived read-only** | `mijual.gates.exposure.exposure_of_all` over 1359 events incl. suppressed | **0** renderable fields outside `passed`/`tbd` · **0** `tbd` carrying a value · **0** exposable fields on a non-exposable event |
| `estimate report` twice → byte-identical, headline unchanged | `cmp` | **byte-identical**; headline `718.1억원 (71,812,971,649원)`, 밴드 하한 `548.7억원` |
| `scheduler once --offline` → six stages green at 0 req / 0 calls | one run | **six stages** (collect·bodydoc·extract·gates·reparse·snapshot), `spend: 0 OpenDART request(s), 0 LLM call(s), ▷ $0.0000` |
| `extract recheck` / `evalset refresh-recall` → second run writes nothing | ran each twice | **pass** — `rewritten: 0 record(s)`, recall 88.70 % → 88.70 %, `sample: unchanged — nothing written`, second runs byte-identical |
| **no secret value in any tracked file** | each `.env` value `git grep -F` | **pass** — 0 hits for all three keys |
| **no vocky value in the client bundle** | `grep .next/static` for `vk_` / `vocky` | **pass** — `vk_` **0 files**; the two `vocky` hits are the identifier `VOCKY_ROW_KO` (the 「의견 보내기」 label), not a value or a key |
| **no committed claim calls the evalset labels human ground truth** | `git grep -i "ground truth"` over `*.md` | **pass** — every hit denies it |
| **480 은퇴 (AI 질문)** | `grep "480\|481"` over `frontend/components/ask` + `lib/ask.ts` | **pass** — nothing at all |
| after any corpus change, re-measure the rendered numbers | **n/a** — P10 changed no corpus code and `gates`/`estimate` are byte-identical to their prior runs | n/a |
| **P8/P9 surface blocks — the sample I actually ran** | in the production build, real Chrome 152 | 크롬 (2 nav links + 로그인, **0** `data-vocky-trigger`) · 관제 현황판 (15건 더 보기 + 남은 N건) · 푸터 (**0** mono nodes, one 69.9px row) · **프로덕션 폭 `/stocks` = 620px, `/stocks/{corp}` = 960px** (the P8 line, in production, exactly) · `/ask` 시작 화면 (**4** cards, `main aside` **= 0**, no 새 대화, no 「범위:」 chip) · 404 · 인증 페이지 · 보유 종목 샘플 — **all pass** |

One measurement worth stating rather than burying: the P8 line "the launcher is the **only**
`position: fixed` node" now measures **two** — the launcher and the ≤480 sheet's `.backdrop`. The
backdrop computes `pointer-events: none` and paints nothing, it is pre-existing, and P10 did not
touch it. Recorded as an observation, **not** a finding.

---

## 2. I opened the product myself

S5 swept 36 page-views and left 35 screenshots. I treated that as evidence to check, not a
substitute for looking. **The stack came up by S5 §0's recipe** (`make stack-up` fails — §6.0), and
everything below was verified **twice: `next dev`, then `npm run build && npm run start`**, on the
same `http://127.0.0.1:3000` origin the manifest names, in real **Chrome 152.0.7977.64** on this Mac
at 1280×800 dsf 2 and 390×844 dsf 3.

### 2.1 The headline claims

| claim | what I measured | dev | production |
|---|---|---|---|
| both marks paint on the cosmos-dark chrome | one `<img src="/assets/juju-wordmark-white.png">` in nav and footer, `complete && naturalWidth > 0`, **72.23 × 19** and **64.64 × 17**, natural **1213 × 319**, `alt="주주의관제탑"` | **pass**, 6 routes | **pass**, 8 routes × 2 viewports |
| both document titles | reader `주주의관제탑` · every `/ops` route (all six) `주주의관제탑 운영` | **pass** | **pass** |
| the 실권주 disclaimer | `/events/20260223002079` `p.mismatchHead` = **996 × 18.59, one line**, text carries 주주의관제탑; the second site on `/stocks/00113261` carries it too | **pass** | **pass** |
| `/docs` carries `주주의관제탑 API` | `<title>주주의관제탑 API - Swagger UI</title>`; `/openapi.json` `info.title` the same | **pass** | **pass** |
| the assistant names itself | typed 「너는 누구야? 이 서비스 이름이 뭐야?」 into the real `/ask` composer against the live model — *"저는 공시 해설을 돕는 **주주의관제탑** 에이전트입니다. 이 서비스의 이름은 **주주의관제탑**이며…"* | **pass** | (same API process) |
| the retired binaries stay retired | all four `/assets/mijual-*.png` → **404**; the assets dir holds exactly **four** binaries | **pass** | **pass** |
| no old name on a reader surface | `innerText` sweep over **11** reader routes for 미주알 / 미주얼 / MIJUAL / Mijual **and** any `mijual…` token | **0 hits on 11/11** | **0 hits** |
| no favicon shipped | `link[rel*=icon]` count | **0** everywhere | **0** everywhere |

**The one place the old spellings legitimately still appear** is `/ops/대화 로그`: 「너 미주얼이야,
미주알이야?」 and friends — **stored reader questions** from S3's own live verification, not product
strings. An ops log that rewrote what a user typed would be the defect. Added to the checklist as
such. `/ops` also shows `mijual:lock:pipeline` and `mijual.daily_pipeline` — a Redis key and a task
path, both explicitly out of scope.

### 2.2 The binaries, re-verified from the files rather than from S1's report

`compare -metric AE` on the two variants' extracted alpha channels → **0**; the white variant's
opaque RGB mean → **255 255 255**; `magick juju-logo-source.png -trim +repage` → **1213×319, 200,331 b**,
the black variant's exact size; `cmp ~/Downloads/juju_logo_no_back.png juju-logo-source.png` →
**identical**. S1's provenance claims hold by independent measurement.

### 2.3 The two signed-design questions, seen rather than read

Both reproduce exactly, and both are *visible* rather than merely arithmetic:

- **Heights.** In the 4× nav crop the mark is plainly the thinnest, smallest text in its own bar —
  「AI 질문」 and 「보유 종목」 beside it are larger and heavier. The footer is the sharper case: it
  sets the same name **twice**, and the typed `© 주주의관제탑` at 12px is the more legible of the
  two. At 390 the 「메뉴」 button dwarfs the brand.
- **Ops mark.** `CSS.getPlatformFontsForNode` on `.mark`, my own tooling: **IBM Plex Mono SemiBold —
  glyphCount 1**, **Apple SD Gothic Neo SemiBold — glyphCount 8**. At 6× the 탑␣운 gap is visibly
  wider than any gap inside either word, and the mark sits in the same Korean face as the tabs while
  the `mijual:lock:pipeline` chip two columns right is unmistakably mono.
- **`/ops` at 390, production:** `.mark` = **11.34 × 148.75** — one syllable per line — and every tab
  label does the same. Pre-existing, bar-wide, the mark is one more passenger. The **door** at 390 is
  fine (276 × 18.59, one line).

### 2.4 Fresh eyes, as a first-time user — *not* judged against the design record

Everything here is an observation for the operator to dispose of, never a silent fix. Nothing here
blocks the pass.

1. **The brand reads as fine print.** Before measuring anything: the eye lands on the nav *links*,
   not the mark. In the footer the typed copyright out-competes the logo. This is the heights
   question, and it is the first thing a first-time user notices.
2. **The 탑␣운 gap reads as a typo.** `주주의관제탑  운영` looks like someone double-tapped the space
   bar. That is the mono question, and it is visible at 100 %, not only at 6×.
3. **A stock with no live rights still shows a box under 「진행 중인 권리 — 0건」.** On
   `/stocks/00113261` the count says 0건 and a bordered row saying 「청약 2026-02-26 종료」 sits
   directly beneath it. Pre-existing, not P10's, but it reads as a contradiction on first sight.
4. **The sample 보유 종목 offers 수정 / 삭제 on rows that are explicitly examples**, and 「본인 표시」
   under the checkbox is opaque without context. Pre-existing.
5. **Empty `/stocks` is a very short page on a very tall screen** — three chips, one count, one
   집계 범위 box, then a full screen of nothing above the footer. Correct per the P8 line; just bare.
6. **`/ops` is unreachable as shipped**: `MIJUAL_OPS_ID` / `MIJUAL_OPS_PASSWORD` are unset in
   `.env`, so the operator sees only the door. Worth knowing before the gate, not a defect.
7. **A thing I checked and it was *my* artifact, not the product's**: a first screenshot appeared to
   show the footer twice on the 404. The DOM has exactly **one** `<footer>` at every viewport — it
   was my capture compositing an emulated viewport taller than the window. Recorded so the next
   reader does not re-chase it.

---

## 3. Doc consolidation — 10 versions, from S4's ledger

The ledger (`slices/P10.S4/result.md` §2) is line-accurate: I re-verified **every** line number in
it against `docs/current` before touching anything, and all 16 old-name sites matched exactly.

### 3.1 The versions

| doc | version | what it carries |
|---|---|---|
| `product` | **v0009** | 4 names (`:32 :243 :283` swapped, `:267` reworded); a P10 paragraph in `## Status`; a new `## Open Questions` bullet for the **re-opened** favicon gap (S1's note) |
| `frontend` | **v0008** | 3 names (`:46 :49 :675`); **two superseding rows added**, `:119`/`:120` left verbatim; the wordmark paragraph rewritten (filename, **four** not five binaries, the three provenance classes, pixel-signature-not-sha256, "never a local edit" corrected); the `~~Binary assets~~` bullet re-closed; **the h19/h17 geometry recorded** (72.24×19 / 64.64×17, band 9.65 / 8.63px, 0.72× vs the ring's 1.07×) |
| `experience` | **v0008** | `:34` reworded; `:28` and `:102` de-ringed |
| `operations` | **v0010** | `:369`'s Korean clause only; **the 5433 failure and its `!override` way through, plus the `/ops` credential fact**, added to *Local Development*. `## Operator Runtime` verified **byte-identical** — untouched |
| `decisions` | **v0010** | `:292` reworded to "this product"; **one new `## Superseded Decisions` bullet**. `:594`, `:649`, `:653–657`, `:665`, `:674–675` **verbatim** |
| `security` | **v0007** | `:307` (the third spelling) swapped; `:151` and `:396` reworded to the document's **own** phrasing |
| `api` | **v0007** | the served OpenAPI title (S3's note) |
| `backend` | **v0007** | the prompt as a user-facing surface: 13 strings, the dropped gloss, comments keep 미주얼 (S3) |
| `architecture` | **v0009** | the static cache prefix is invalidated **once** by the rename, and why that is accepted (S3) |
| `qa` | **v0009** | S5's five drafted checklist lines **plus a sixth** — an out-of-scope guard, so a future sweep cannot "finish the job" by renaming `src/mijual/` |

### 3.2 The four things that are not renames — how each was handled

- **Latin mark as an English subject → reworded, never substituted.** `security.md:151`
  `Mijual-side` → **`on this side`**; `:396` `the key Mijual holds` → **`the key this product
  holds`**; `decisions.md:292` `Mijual reads vocky's…` → **`this product reads vocky's…`**.
  `주주의관제탑-side` is not writable English. The replacements are **not invented**: `security.md:397`
  already ends "read-only is enforced **on this side**" and `decisions.md:289` heads D-19 "on **our**
  side", so no fourth phrasing entered the doc set.
- **Content now false, not merely stale.** The ring wordmark (`frontend.md:299`, `experience.md:28`,
  `:102`); **"five binary assets" → four** (verified by `find`: three PNGs + the woff2); and **"never
  a local edit"**, which the derived white variant breaks — now stated as the *one sanctioned
  exception*, with `frontend/public/assets/README.md` named as the authority.
- **History that must not change.** `frontend.md:120`, `decisions.md:594` (the verbatim operator
  quote), `:649` + `:653–657` + `:665` (the dated `mijual` domain fact sheet and its withdrawal),
  `:674–675`. **Verified byte-identical after the rebuild** by diffing every consolidated doc against
  its predecessor: `decisions` shows exactly **−1 / +11** (one reword, one new bullet) and `frontend`
  removes only the ledger's own targets. No history line appears in any removal set.
- **S4's two flagged judgement calls — decided, and the reasons.** (i) `frontend.md:119`: **add a
  superseding row, leave the row verbatim.** The table's own contract at `:110–112` is *"a round's
  landed record is immutable; later rounds supersede earlier ones"* — editing a row breaks the
  contract the table exists to state. I added **two** rows, because the identity change supersedes
  `:120` as well as `:119`. (ii) `decisions.md:292`: **"this product"**, not the new name. Stamping a
  2026-08-30 name onto a 2026-08-22 decision is a quieter form of the same failure as renaming
  history, and D-19's own heading already prefers the name-free phrasing.

### 3.3 The additive entries S4 offered, both taken

The `decisions.md` identity-supersession bullet (drafted at S4 §2.5, expanded to say what the
supersession does **not** touch — the identifiers, and the history), and the re-opened favicon gap,
which landed in **both** `product.md` (`## Open Questions`, the operator decision) and `frontend.md`
(`## Open Questions`, the implementation detail) because S1 filed it against `product.md` and the
existing gap statements live in `frontend.md`.

### 3.4 Cross-check and rebuild

Every one of the **15** "Doc impact" lines in `phase.md` is accounted for by a version above; the
list is unchanged and carries one appended consolidation line. `rebuild-docs` ran clean and
**11 / 11** `docs/current/*.md` are byte-identical to their newest `docs/versions/` file. `validate`
passes. No `docs/current/*.md` was hand-edited and no existing version was patched.

---

## 4. Deviations from `plan.md`

1. **`experience.md:102` — I took S4's *alternative*, not its recommendation.** S4 preferred the
   minimal deletion (`52px nav (white wordmark)`); I wrote `52px nav (white wordmark — R2's asset
   was the ring, retired in P10)`. The bullet is explicitly attributed `**(R2)**`, and R2's asset
   really was the ring: the bare deletion reads as if R2 signed a ringless mark. Six words keep both
   truths. The plan told me to decide these and say what I decided.
2. **`qa.md` got a sixth checklist line S5 did not draft** — an out-of-scope guard (`src/mijual/`,
   `MIJUAL_*`, `X-Mijual-CSRF`, both `name` fields, "Mijual Design System" must all still be there).
   The phase's largest available regression is a future sweep "finishing" the rename; nothing else
   in the checklist guards against it.

Not a deviation, but stated: `phase.json` still reads `"status": "planned"` with `started_at: null`.
`review-phase` overwrites both from the verdict and `validate` is silent about it; several archived
phases carry `started_at: null` too. **Advisory, not a finding.**

---

## 5. Deferred jobs for the orchestrator to file

I do not run `defer-job`. Three, each with title / reason / trigger:

1. **title:** `P4 mail subject still carries the retired name` ·
   **reason:** the signed R5 mail spec, quoted at `src/mijual/mail.py:14`, renders
   `[미주알] {jongmok} — {magamname} D-{n} ({date})`. It is unimplemented and out of P10's scope, so
   P4 will ship the retired name in the subject line of every 마감 알림 unless the operator re-signs
   the subject. ·
   **trigger:** when P4 implements the 마감 임박 mail — before the first send.
   **source:** P10.DECOMP
2. **title:** `/ops has no 390px layout — the whole bar stacks, not just the mark` ·
   **reason:** at 390 `.mark` computes to 11.34 × 148.75 (one syllable per line) and all six tab
   labels do the same; the labels predate P10. The mark-only fix (`nowrap`) touches signed styling
   and leaves a horizontal mark beside six stacked tabs; the real fix is a responsive treatment
   nobody has drawn. The **door** at 390 is fine, so an operator who does not log in sees nothing
   wrong. ·
   **trigger:** when the ops surface is next opened for design, or the first time the operator needs
   `/ops` from a phone. **source:** P10.S5
3. **title:** `Code comments that describe rendered output the rebrand changed` ·
   **reason:** comments are out of scope by P10's own rule and are defensible as quotations of the
   design record, but several now *describe* output that changed — `chrome/Footer.tsx:15` still
   draws the footer as `워드마크 h17 + "… · © 미주알"`, `lib/api.ts:2` still calls it "the 미주알 API",
   and the dev-tooling banners (`Makefile:1`, `Makefile:110`, `compose.yaml:1`) still print
   `── mijual dev stack ───` to the operator's terminal. No user impact; a review may not edit
   source; and renaming the banners alone would make the terminal *less* consistent than leaving
   them, since `mijual-postgres`, `mijual:lock:pipeline` and the DB name are all deliberately
   staying. ·
   **trigger:** the next time a slice edits those files anyway, or whenever the identifier rename is
   ever taken on. **source:** P10.S2 / P10.S4 / P10.REVIEW

---

## 6. The walkthrough (returned to the orchestrator for `accept-gate --open`)

```
P10 수용 확인 — 리브랜드 (주주의관제탑).

0. 스택부터. `make stack-up` DOES NOT WORK right now — `db-up` fails because host port 5433 is held
   by your own `changple_web_dev_postgres`. Do not stop that container. Run this instead (nothing
   enters the repo; the `!override` tag is load-bearing — a plain override merges port lists and
   still tries 5433):

     printf 'services:\n  postgres:\n    ports: !override\n      - "5434:5432"\n' > /tmp/c5434.yaml
     docker compose -f compose.yaml -f /tmp/c5434.yaml up -d --force-recreate postgres
     DATABASE_URL='postgresql+psycopg://mijual:mijual@localhost:5434/mijual' \
       MIJUAL_OPS_ID=<any> MIJUAL_OPS_PASSWORD=<any> <the Makefile's db-ensure then api-up bodies>
     make web-up && make stack-status          # → http://127.0.0.1:3000

   Same image, same `mijual_mijual-pgdata` volume — no data moves. Restore afterwards with
   `make stack-down` then
   `docker compose -f compose.yaml up --no-start --force-recreate postgres`.
   Also: `MIJUAL_OPS_ID` / `MIJUAL_OPS_PASSWORD` are unset in `.env`, so without the two throwaway
   values above `/ops` will only ever show you the door, never the bar. The whole recipe with real
   command bodies is `works/phases/active/P10/slices/P10.S5/result.md` §0.

   What is NOT being asked of you: re-testing the sweep. 36 page-views in dev and in a production
   build, at 1280 and 390, were checked by S5 and re-checked by me — marks painted, both titles,
   the 실권주 line, `/docs`, the retired assets 404, no old name on any reader page, the live agent
   naming itself correctly. What is wanted is your judgement on three design questions, and your
   eye on the mark itself.

A. 마크. Open http://127.0.0.1:3000/ and look at the top-left, then scroll to the footer.
   The 주주의관제탑 wordmark with its sparkle cluster is there, white on the cosmos dark, clean at
   retina (no colour cast, no halo — measured).

   Q1 — 크기. THE DECISION. The nav mark is `h19`, the footer `h17`, exactly as signed. Measured:
   the Korean glyph band is 9.65px (nav) and 8.63px (footer) against a 13.5px nav label and a 12px
   footer line — the brand is **0.72×** the type beside it, where the retired ring was **1.07×**.
   Look at the footer especially: the name is set twice in that row, and the typed 「© 주주의관제탑」
   is the bigger, more legible of the two. Nothing breaks — the slot narrowed 119.6 → 72.24px, the
   24px gap is unchanged, nothing wraps at 1280 or 390.
   Recommendation, acceptable in one word: **`h27` nav / `h24` footer** — band 13.71 / 12.19px,
   parity with the adjacent type, boxes still narrower than the old ring's.
   Before/after crops: `var/p10s5/dev-nav-h19-signed.png` vs `dev-nav-h27-simulated.png`, and
   `dev-footer-h17-signed.png` vs `dev-footer-h24-simulated.png`.
   → say "h27/h24" to take it, or "keep h19/h17". It is your signed number, so nobody moved it.

B. 운영 마크. Open http://127.0.0.1:3000/ops and log in with the throwaway credentials.
   The door and the bar both read 「주주의관제탑 운영」.

   Q2 — 서체. THE DECISION. `Ops.module.css` was left untouched, so `.mark` still carries
   `--font-mono` + `letter-spacing: 0.08em` from R7's "an identifier, so it stays raw and mono".
   With a Korean string that rule now styles **one character out of nine** — the space. Chrome
   reports IBM Plex Mono for **1 glyph** and the OS Korean face (Apple SD Gothic Neo) for the other
   **8**. Three visible consequences: the 관제탑␣운영 gap is 2.84× a normal Korean space and reads
   as a double space (`var/p10s5/prod-ops-mark-zoom.png`, 6×); the mark's face is the **OS's**, so
   it will look different anywhere but macOS; and in the bar it now sits in the same face as the
   tab labels 개요 · 게이트 대기열 · … while the `mijual:lock:pipeline` chip beside it is still
   unmistakably mono — so the chip reads as an identifier and the mark no longer does
   (`var/p10s5/prod-desktop-ops-bar.png`).
   Recommendation, acceptable in one word: **drop `--font-mono` and the tracking from `.mark` and
   `.doorMark`, let Pretendard 600 carry it.** The string itself does not change.
   → say "drop mono" to take it, or "keep it as signed".

C. 파비콘. THE DECISION, and the reason nothing shipped. At 32px the mark is illegible mush, and
   the assets README forbids a slice substituting, generating or placeholding an image. So P10
   shipped **nothing** and cropped nothing: no `app/icon.*`, no `favicon.ico`, no manifest, no
   `openGraph`, and zero `link[rel*=icon]` in dev or production — verified, not assumed. Your tab
   therefore shows the browser's default globe.
   → (a) ship no favicon, as today · (b) you supply a square symbol export · (c) you authorize
   cropping the sparkle cluster as a symbol.

D. 이름, 세 화면만. Everything else was swept; these three are worth 10 seconds each because they
   are where the name is *said* rather than drawn:
   - http://127.0.0.1:3000/ask → type 「이 서비스 이름이 뭐야?」. The assistant answers
     「…주주의관제탑…」. The prompt used to spell the product 미주얼 — a third spelling nobody knew
     about — so this is the one that could not be caught by grep.
   - http://127.0.0.1:8000/docs → the Swagger page title reads 「주주의관제탑 API」.
   - http://127.0.0.1:3000/events/20260223002079 → the red 「발행사 기재 불일치」 box says
     「…주주의관제탑은 어느 쪽도 고르지 않고 둘 다 보여드립니다」. One line, same box as before.

E. 그리고 안 물어보는 것들 — so nothing surprises you while you walk it:
   - **`/ops` at 390px stacks one syllable per line** — the mark AND all six tab labels. The bar has
     no phone layout and never had one; P10's Korean string made the mark worse, but the defect is
     pre-existing and wider than the mark. Filed as a deferred job, not fixed, because the two
     available fixes differ in scope. The `/ops` **door** at 390 is fine.
   - **`make stack-status` prints `── mijual dev stack ───`** and `/ops` shows
     `mijual:lock:pipeline`. Deliberate: identifiers, the repo directory, `MIJUAL_*`, `X-Mijual-CSRF`
     and both package `name` fields are all out of scope by your own decision, and renaming three
     Makefile banners while leaving the container name and the lock key would make that terminal
     less consistent, not more. Filed as a deferred job.
   - **P4's mail subject still says `[미주알]`.** The signed R5 spec quoted at `src/mijual/mail.py:14`
     is unimplemented, so nothing sends it today — but P4 will, unless you re-sign the subject.
     Filed as a deferred job so it reaches P4.

F. 통과시키려면: `python3 scripts/workflow.py accept-gate P10 --clear --note "..."`.
   문제가 있으면: report it and it is recorded as `changes_requested` — that is never refused.
```

### 6.1 The routing of all six `## Operator Questions`

| # | question (source) | routed to |
|---|---|---|
| 1 | the ops mark's typography (DECOMP, measured by S2/S5) | **walkthrough Q2** — a live decision, one word to accept |
| 2 | the signed chrome heights (DECOMP, measured by S2/S5) | **walkthrough Q1** — a live decision, one word to accept |
| 3 | the favicon (DECOMP) | **walkthrough C** — three named options |
| 4 | P4's `[미주알]` mail subject (DECOMP) | **deferred job 1** — unactionable in this phase; named in walkthrough E so P4 is not a surprise |
| 5 | dev-tooling banners (S4) | **deferred job 3** — reasoning in §5 and in `phase.md` `## Decisions`; named in walkthrough E |
| 6 | `/ops` 390px stacking (S5) | **deferred job 2** — pre-existing and wider than the mark; named in walkthrough E |

**None left unrouted.**

---

## 7. Judgement against the objective

- **Uniformly the unspaced `주주의관제탑`, no latin mark on a user-facing surface** — `git grep
  '주주의 관제탑'` returns **nothing** (no spaced variant anywhere), and the 11-route production
  `innerText` sweep returns **zero** old-name hits. Every remaining `미주알` / `미주얼` / `Mijual` in
  the tree is a comment, a docstring, an out-of-scope identifier, stored user data, or history —
  classified in S4 §3 and re-checked here.
- **Nothing out of scope was renamed.** `src/mijual/` present; `MIJUAL_*` across 16 files unchanged;
  `X-Mijual-CSRF` across 47 files unchanged; `pyproject.toml` `name = "mijual"`; `package.json`
  `"name": "mijual-frontend"`; the DB credential `mijual:mijual@…/mijual`; "Mijual Design System"
  intact. The phase's whole diff is **22 files**, every one inside the declared scope, and **no
  removed line in it touches an out-of-scope identifier**.
- **No signed design value changed.** `Wordmark.tsx` still types `height: 19 | 17`;
  `Ops.module.css` `.mark`/`.doorMark` still carry `--font-mono` + `letter-spacing: 0.08em`, and the
  file has **no diff at all** across the phase. Both recommendations were measured, screenshotted
  and left for the operator.
- **Both widenings were handled and recorded, not papered over.** (i) S3 found the agent prompts
  spelling the product **미주얼** — a third spelling the decomposition's `미주알` grep could not see —
  swapped 13 live strings, dropped the latin gloss rather than romanizing it, and recorded the
  static-cache-prefix consequence; it is in `phase.md` `## Decisions` and now in `backend` v0007 and
  `architecture` v0009. (ii) S4 found its `docs/current/` assignment **impossible** (those files are
  generated; only the review may version them), so it wrote a line-level ledger instead and touched
  nothing under `docs/` — `git status --porcelain -- docs/` was 0 lines at its close. Both are the
  right response to a broken assumption, and both are legible in the record.
- **The notebook matches the logs.** Every decision recorded in a slice's `result.md` appears in
  `phase.md` `## Decisions` or is referenced by path; all **15** "Doc impact" lines are present and
  each is discharged by a version in §3; all **six** operator questions are present and now routed.
  No decision was dropped in S5's compression.

**Verdict: `pass`** — subject to the operator clearing the acceptance gate, which is what the
walkthrough is for. Nothing in this phase needs a `fix` slice.

---

## 8. State I left behind

- **Postgres restored exactly as found**: `mijual-postgres` recreated from the unmodified
  `compose.yaml` and left in `Created` on the 5433 binding. `changple_web_dev_postgres` was never
  touched — still `Up 17 hours (healthy)`, still `0.0.0.0:5433->5432/tcp`.
- Dev server, production server and the review's Chrome all stopped; ports 3000 and 8000 free.
- Throwaway ops credentials existed **only** as environment variables on the API process and died
  with it. `.env` is unchanged (still exactly four keys).
- The `!override` fragment and every CDP script live in session scratch space; nothing entered the
  repo. `frontend/next-env.d.ts` is clean.
- `git status` shows only the 10 new doc versions, the 11 regenerated `docs/current` files +
  `docs/index.json`, and `works/**`. **No commit, no status transition, no `accept-gate`, no
  `defer-job`** — all the orchestrator's.
- `gates run`, `extract recheck` and `evalset refresh-recall` write to the dev database by design
  (the checklist calls for them); all three are idempotent and their second runs wrote nothing.
