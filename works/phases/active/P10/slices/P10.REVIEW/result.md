# Result — P10.REVIEW (phase review, gated) — **round 3**

- **status:** `done`
- **summary:** Re-reviewed P10 after `P10.F1` (R18 §①③④) and `P10.F2` (R18 §②②b). Re-ran the
  phase's whole validation (pytest **154**, typecheck, build **18/18** static pages · 19 route
  entries, smoke **22/22**, `validate` clean, `gates run` ×2 byte-identical over **710** field rows
  at **618/4/10/78**, `estimate report` ×2 byte-identical, `scheduler once --offline` six stages at
  0 req / 0 calls, `extract recheck` and `evalset refresh-recall` idempotent at **88.70 %**, the
  exposure invariant re-derived **0 / 0 / 0** over **1,359** events, the secret scan clean), then
  **opened the product myself** in the manifest runtime — `make stack-up` / `next dev` on
  `127.0.0.1:3010` **and** `npm run build && npm run start` on the same origin, real Chrome 152 at
  1280 and a true 390@3 — and re-measured every corrected value rather than accepting it. **All 16
  reader page-views and all 12 `/ops` page-views were identical between dev and production on every
  measured field.** The phase's defining pattern shaped where the effort went: **five signed values
  in P10 turned out wrong, three of them checks that could not fail**, so every check I ran here was
  first shown capable of reporting failure — and that discipline found a **fourth** broken guard, in
  this phase's *own* round-1 doc consolidation (§4.1). Ran the **full functional sweep** (**119
  controls**, **zero dead**), typed a real question into the live widget in both runtimes and
  watched the agent name itself 주주의관제탑, re-walked round 2's nine fresh-eyes findings (**eight
  stand, one is fixed**), re-ran the whole cumulative `## Regression Checklist` and appended round
  3's block while correcting the two lines R18 falsified, and routed all **nine** operator questions.
  Consolidated round 3 into **4 doc versions**. `pass`, with a walkthrough for the re-opened gate.
- **files_changed:**
  - `docs/versions/frontend/v0010_*.md` · `docs/versions/qa/v0011_*.md` ·
    `docs/versions/decisions/v0012_*.md` · `docs/versions/product/v0011_*.md` (created via
    `doc-new-version`)
  - `docs/current/{frontend,qa,decisions,product}.md` + `docs/index.json` (engine-regenerated by
    `rebuild-docs`)
  - `works/phases/active/P10/phase.md` (edited — 176 lines / **16,035 B**, under budget)
  - `works/phases/active/P10/slices/P10.REVIEW/result.md` (this file, rewritten for round 3)
  - **no source file touched** — `git status --porcelain -- frontend/ src/ tests/ Makefile
    compose.yaml .env docs/reference/` is **0 lines**
  - untracked evidence (`var/` is gitignored): `var/p10rev3/{dev,prod,eyes}/`
- **validation:** every command below passed — §1
  - `.venv/bin/python -m pytest` — **154 passed** (72+72+10 dots), 1 pre-existing starlette warning
  - `cd frontend && npm run typecheck` — pass, no output
  - `cd frontend && npm run build` — `✓ Compiled successfully`, `Generating static pages (18/18)`,
    **19** route entries including `/icon.png`, `/icon1.png`, `/apple-icon.png`
  - `cd frontend && npm run smoke` — `tests 22 · pass 22 · fail 0`
  - `python3 scripts/workflow.py validate` — `Workflow validation passed.` (no warnings)
  - the whole cumulative `## Regression Checklist` — §1.2
  - real browser, **dev** and **production**, 1280 and 390 — §2–§6
  - `python3 scripts/workflow.py rebuild-docs` + `docs` — **11/11** `docs/current` byte-identical to
    their newest version — §8
- **deviations:** two — §9
- **doc_impact:** n/a (review slice — round 3's four "Doc impact" notes were *consumed* and the
  entry annotated `DISCHARGED` in place)
- **doc_versions:** 4 — `frontend` **v0010** · `qa` **v0011** · `decisions` **v0012** ·
  `product` **v0011** (§8). Rounds 1 (ten) and 2 (eight) were **not** re-versioned.
- **review_verdict:** `pass`
- **walkthrough:** §10 (returned to the orchestrator for `accept-gate P10 --open`)
- **explain:** `not written — run /explain for this phase`

---

## 0. What this file is, and where rounds 1 and 2 went

This slice has now run **three** times.

- **Round 1** (2026-08-30) returned `pass` and opened the gate. The operator **did not clear it** —
  the mark shipped at R2's h19/h17 where the Korean glyph band was 0.72× the type beside it, and the
  `/ops` mark's mono rule styled one glyph of nine. Round 1 measured and *reported* both rather than
  fixing them, because both were signed numbers, which is what put the right decisions in front of
  the operator. It consolidated **ten** doc versions. Full file in git at `a003f3d`.
- **Round 2** (2026-08-31) reviewed R17 + `P10.S7` and returned `pass`; it consolidated **eight**
  doc versions and opened the gate a second time. **The operator did not clear that one either** —
  instead they ran a Claude Design session themselves and sent back three fixes (R18). Full file in
  git at `4967487`.
- **Round 3** is this file. It reviews `P10.F1` and `P10.F2`, the two slices that applied R18.

Rounds 1 and 2's eighteen doc versions stand and were **not** re-versioned; re-versioning them would
have been a real error, and §8 shows what was created instead.

**Instrument.** **Aside is not installed on this machine** (`aside` is not on `PATH`, no app bundle
in `/Applications`), so per the doctrine's own fallback I drove **real Chrome 152.0.7977.65 over
CDP** — the same instrument `P10.S7`, `P10.REVIEW` rounds 1–2, `P10.F1` and `P10.F2` used — in the
runtime and access path `docs/current/operations.md` `## Operator Runtime` names: `make stack-up`,
dev at `http://127.0.0.1:3010`, **and** the production build (`npm run build && npm run start`) on
the same origin, at 1280 and at a true 390 (`mobile: true`, dsf 3). `/ops` was reached with throwaway
`MIJUAL_OPS_ID` / `MIJUAL_OPS_PASSWORD` on the API process only; **`.env` was never opened**. Every
number below is read out of a live document or measured with ImageMagick 7.1.2-27 — none is copied
from a slice report. My readings reproduce `P10.F1`'s and `P10.F2`'s **to the decimal**
(`279.484375`, `25.5956`, `154.531`, `439.875`, the 37/34/78/73 tab-paint counts), which is itself
evidence that the instrument is equivalent to theirs.

---

## 1. Validation — the phase as a whole

### 1.1 The mechanical top

| command | result |
|---|---|
| `.venv/bin/python -m pytest` | **154 passed**, 1 warning (pre-existing `StarletteDeprecationWarning`) |
| `npm run typecheck` | clean, no output |
| `npm run build` | `✓ Compiled successfully`; `Generating static pages (18/18)`; **19** route entries |
| `npm run smoke` | `tests 22 · pass 22 · fail 0` |
| `workflow validate` | `Workflow validation passed.` — no warnings, and none on the `phase.md` budget |

### 1.2 The cumulative `## Regression Checklist`

Round 3 touched only frontend chrome and two binaries, so re-running the pipeline half is precisely
the check that it did not move. Everything scriptable, in full; the P8/P9 surface blocks are ~80
browser assertions and I ran a **named sample** rather than claiming all 80.

| line | how it was run | result |
|---|---|---|
| `gates run` ×2 → byte-identical, 710 field rows | `cmp` on two full runs | **byte-identical**; `1359 judged, 201 version(s), 710 field row(s)`; split **passed 618 · tbd 4 · failed 10 · n/a 78** — unchanged from round 2 |
| the four AST import scans · anonymity · tool signature · ops-method guards | they are pytest tests | green inside the 154 |
| no reader-facing quota / storage-denial copy | grep over `frontend/components`, `frontend/lib` | **pass** — every hit is a comment or `CONVERT_SESSION_KO` (the sanctioned 보유량 line on another surface); `localStorage` in the ask surfaces appears only inside comments forbidding it |
| exposure invariant re-derived read-only | `exposure_of_all(session, include_suppressed=True)`, **1,359** events / **418** renderable field views | **0 / 0 / 0** — and the check has teeth: evaluating the same predicates *without* the event gate reports **25**, exactly the gate-passing fields a blocked event withholds |
| `estimate report` ×2 → byte-identical | `cmp` | **byte-identical**; headline `718.1억원 (71,812,971,649원)`, 밴드 하한 `548.7억원` — unchanged since round 1 |
| `scheduler once --offline` → six stages at 0 req / 0 calls | one run | **six** (collect·bodydoc·extract·gates·reparse·snapshot); `0 OpenDART request(s), 0 LLM call(s), ▷ $0.0000` |
| `extract recheck` / `evalset refresh-recall` ×2 | ran each twice, `cmp` | **byte-identical**; `rewritten: 0 record(s)`, recall **88.70 %** (45건), `sample: unchanged — nothing written` |
| no secret value in any tracked file | `git grep -F` each `.env` value | **pass** — 0 hits for `DART_API_KEY`, `GEMINI_API_KEY`, `MIJUAL_VOCKY_API_KEY`. `MIJUAL_VOCKY_API_BASE` matches 7 files: an **endpoint**, not a secret |
| no vocky value in the client bundle | grep `frontend/.next/static` | **pass** — `vk_` / `vocky` **0 files** |
| no committed claim calls the evalset labels human ground truth | `git grep -i "ground truth"` | **pass** — every hit denies it |
| 480 은퇴 (AI 질문) | grep `480\|481` over `components/ask` + `lib/ask.ts` | **pass** — **0** hits |
| after a corpus change, re-measure the rendered numbers | **n/a** — round 3 changed no corpus code, and `gates`/`estimate` are byte-identical to their prior runs | n/a |
| the agent's own two numbers, if a live pass was run | two live turns (dev + production), both meta questions: **0 도구 행, 0 인용 칩**, so there is no stored 인용 원문 or unmarked numeral to check | n/a, stated rather than skipped |

**P8/P9 surface sample, in the production build:** 크롬 (**2** visible bar links, **0**
`data-vocky-trigger`, **0** 「의견」 chips, footer **0** mono nodes / one row) · 관제 현황판
(「15건 더 보기」 + 「남은 362건」; one click → a 접기 control; the four tabs carry `aria-pressed`) ·
**mono 줄바꿈 0건** over 14 page-views at **1512 / 1440 / 1280 / 768 / 767 / 481 / 390** (80 mono
nodes on `/`, 33–37 on a 상세; the first predicate I wrote counted *client rects* and reported 9 at
every width — every one was a numeral and its Korean unit at the **same `top`**, i.e. no break at
all; §4.1's discipline applied to my own check) · **프로덕션 폭 `/stocks` = 620px,
`/stocks/{corp}` = 960px** · `/ask` 시작 화면 (**4** cards, `main aside` **= 0**, no 새 대화, no
「범위:」) · AI 질문 경계 (no launcher at 767/600/390; present at 768; widget **440×620** at
`right/bottom: 24`, `z-index: 40`) · 컴포저 (empty = ghost `disabled`, bg `rgba(0,0,0,0)`, ink
`rgb(109,131,120)` → typed = solid `rgb(15,107,80)`, ink `rgb(234,242,237)` → sending =
「답변 준비 중…」 disabled) · 진행 표시 (exactly **one** `[role=status]` — 「질문을 읽고 있습니다」 —
and **0** at the terminal) · 한 단락 (**0** `<br>`) · `prefers-reduced-motion` (**0** animated
elements document-wide; **247** without it) · 404 (**status 404**, Korean not-found) · 인증 게이팅
(**0** `required`/`pattern`; empty submit renders 「이메일과 비밀번호를 입력해 주세요.」) —
**all pass**.

---

## 2. The corrections to R18, re-measured — not accepted

`P10.F1` corrected three of R18's own verification procedures and `P10.F2` deviated from its diff
once. A slice correcting a signed record is exactly what a review exists to check, so none of it was
taken on the slice's word.

### 2.1 The wordmark, from the binary up

Measured on `frontend/public/assets/juju2-wordmark-white.png` with ImageMagick, and on the
**pre-change** raster recovered from git (`4967487`).

| check | expected | measured | |
|---|---|---|---|
| box | 1247×371 srgba 8 | `1247x371 srgba 4.0 8`, `opaque=False` | ✅ |
| non-transparent px | 78,212 (unchanged) | **78,212** on *both* the shipped and the pre-change file | ✅ |
| fully opaque px | 69,630 (unchanged) | **69,630** on both | ✅ |
| distinct alpha values | 154 (unchanged) | **154** on both | ✅ |
| counter island `50×46+402+226` | 481 | **481** after; **481** before | ✅ |
| counter island `69×15+969+335` | 15 | **15** after; **15** at `+1014+335` before (moved −45) | ✅ |
| 의\|관 ink gap | 25px at `x=519..543` | the zero-alpha column run is exactly **`(519, 543, 25)`** | ✅ |
| empty band | 30 rows, `y=165..194` | full-height zero-alpha row run **`(165, 194, 30)`** | ✅ |
| glyph band | 1087×176 at `y=195`, 75,731 ink px | `1087x176`, crop offset 190 + 5 = **195**, **75,731** | ✅ |
| sparkle | 222×165 at `x=1025`, right-flush | `1025 + 222 = 1247`; **and stronger** — that crop is **`-metric AE` = 0** against `juju2-symbol-white.png` itself, while the same crop one column left gives AE **2.1 × 10⁷** | ✅ |
| every pixel `#FFFFFF` | 1 distinct RGB | **1** — `srgb(255,255,255)` | ✅ |
| top / right / bottom flush | preserved | ink x-range 0…1246, y-range 0…370 | ✅ |

**The 45 cut columns were dead — verified on the pre-change file, independently.** Reading the alpha
channel column by column over the full 371-row height, the zero-alpha column runs on the *pre-change*
raster are

```
[(164,182,19), (347,369,23), (486,493,8), (519,588,70), (757,775,19),
 (895,907,13), (932,970,39), (1137,1141,5), (1172,1201,30)]
```

so `x=519..588` is a **70-column, full-height, zero-alpha band**, and the command removes
`x=530..574` from strictly inside it — 11 columns of clearance left, 14 right. Neighbouring columns
are not zero (`x=517` maxalpha 255, `x=518` 175, `x=589` 152), so the measurement discriminates.

**The alpha-splice hash, with a control.** A fresh `+append` of the same two crops of the source trim
and the shipped derivative both give
`d90e982748259b5356373cb82b5fb9fc20678947eb6df1994554b56ae895df79`; the **pre-change** raster gives
`296508ffbbfcfc0fcbcc67a58348ecf94a32f3989bdac9f049354323cc5d2fbe`. The comparison is therefore a
test, not a tautology — and it is what proves the cut touched no ink.

### 2.2 The aspect — `P10.F1` is right and R18 is wrong, and Chrome agrees in both runtimes

`1247 / 371 = 3.361186`, so `× 27 = 90.752` and `× 24 = 80.668`. R18 signs **3.3603 / 90.7 / 80.6**;
`P10.F1` wrote **3.3612 / 90.75 / 80.67**. (The pre-change 3.4825 was computed correctly, and
`3.4825 − 45/371 = 3.3612` independently.) In the running product, on **all 16 reader page-views per
runtime**:

| | dev | production |
|---|---|---|
| `naturalWidth × naturalHeight` | **1247 × 371** | **1247 × 371** |
| `width` / `height` attributes | `1247` / `371` | same |
| nav `getBoundingClientRect()` | **90.75 × 27** | **90.75 × 27** |
| footer | **80.6563 × 24** | **80.6563 × 24** |

80.6563 is `5162/64` — Chrome quantising 80.668 to 1/64 px, i.e. `P10.F1`'s "Chrome: 80.66" and the
README's arithmetic 80.67 are the same number seen twice. **R18's 80.6 is wrong at the precision it
was signed to.**

### 2.3 The three replaced procedures actually work — each one broken on purpose first

This is the plan's §2.3, and the answer to the standing bias. For each guard I built a deliberately
wrong input and confirmed the guard reports failure on it — and confirmed R18's original form does
**not**.

| guard | on the shipped file | on a deliberately wrong file | R18's own form |
|---|---|---|---|
| **counter islands** (`P10.F1`'s replacement) | 481 / 15 | filled the 「의」 counter → **1442** / 15 | "opaque near-white = **0**" reports **69,630** on the good file and **70,591** on the wrong one — it can never be 0, so it can never fail |
| **alpha-splice hash** | `d90e9827…` = `d90e9827…` | pre-change raster → `296508ff…` ≠ | (R18 specified this one correctly) |
| **tile ink colour** ("0 visible px that are not `#2b8e6c`") | **0** in all three tiles (84 / 37 / 1,117 visible px) | wrong ink `#009268` → **84 of 84**; one stray half-white pixel → **1 of 85** | `%[fx:int(255*u.r)]` prints `0,0,0` on the good tiles **and** on both wrong ones — it reads pixel (0,0), transparent canvas |

The tiles themselves: `32x32` / `16x16` / `180x180`, all `srgba`, all `opaque=false`, ink trims
`24x18+4+7` and `134x100+23+40`, integer margins on both axes. All three are served **byte-identical
to the files on disk** in dev and in production (sha256 on the hashed URLs the DOM actually names);
comparing the served 32 tile against the 16 file gives a mismatch, so that check discriminates too.

### 2.4 The vertical geometry did not move

| | value |
|---|---|
| `INK_OFFSET_PX` in `Wordmark.tsx` | `{ 27: 7, 24: 6 }` — **unchanged** |
| computed transform, nav / footer | `matrix(1, 0, 0, 1, 0, -7)` / `-6`, on all 16 page-views per runtime |
| bar | 52px border-box, `border-bottom: 1px` → 51px content |
| **h27 glyph-band centre in the bar** | **25.5956** — identical to round 2's pre-change 25.596, in **both** runtimes and at **both** viewports |

R18 changed the horizontal only, and the product agrees.

### 2.5 `P10.F2`'s `left`-array equality — and the control that makes it a test

Five nav routes, and I added three more (`/stocks/{corp}`, `/events/{rcept}`, the 404) for eight in
total, at 1280:

```
/ask  /portfolio  /stocks  /auth/login  /  /stocks/00113261  /events/…  /no-such-path
    →  [218.75, 279.484375, 0, 0, 0]   on every one, dev and production
```

`AI 질문` measures **40.7344** whether it is active (600) or not (400) — before the fix it was 40.031
when inactive. The active link is still **600** with a `rgb(255,255,255)` 2px underline; inactive
links still reserve a transparent 2px; `aria-current` still moves; the twin computes to the label
with `visibility: hidden`.

**The control:** injecting `header nav a::after { content: none }` at runtime and re-measuring puts
`/portfolio` straight back to **`[218.75, 278.78125, …]`** while `/ask` stays at `279.484375` —
i.e. exactly the **0.703125px** shove `P10.F1` and `P10.F2` measured, reproduced on demand. **Dev and
production behave identically, control included.**

`/ops`, all six routes, logged in, 1280:

```
shipped    →  [139.6875, 180.53125, 274.84375, 360.828125, 430.296875, 483.5625]  on all six
twin off   →  /ops (정확도·비용 inactive)  [..., 360.515625, 429.984375, 483.25]     ← the 0.3125px shove
           →  /ops/accuracy (active)      unchanged
```

Same in production. The equality is caused by the reservation, not by luck.

### 2.6 The accessibility claim, reproduced independently with its negative control

`Accessibility.getFullAXTree` over both surfaces, then the same dump with the twin switched to the
rejected alternative, then restored:

| | nav (`/ask`) | `/ops` (`/ops/accuracy`), six tabs |
|---|---|---|
| **shipped — `visibility: hidden`** | `AI 질문` · `보유 종목`, **one** `StaticText` each | `개요` · `게이트 대기열` · `정확도·비용` · `대화 로그` · `사용자` · `피드백`, **one** each |
| control — `opacity: 0` | **`AI 질문 AI 질문`** · **`보유 종목 보유 종목`**, two each | **all six doubled** |
| restored | one each | one each |

Identical in production. `visibility: hidden` is load-bearing exactly as `P10.F2` claimed, and the
control is what makes that a measurement rather than a reading of the CSS.

### 2.7 `P10.F2`'s one deviation — `/ops` `.tab` without `white-space: nowrap`

At a true 390 device, `/ops`:

| | shipped | twin disabled | `nowrap` injected |
|---|---|---|---|
| tab strip | **154.531** wide, `right: 213.578`, `top/bottom 21.609 / 151.141` | **identical** | **380.828** wide, `right: **439.875**`, `top/bottom 73.906 / 98.828` |
| each tab width | 12.422 ×6 | 12.422 ×6 | 24.84 / 78.31 / 69.67 / 53.47 / 37.27 / 37.27 |
| the shove | gone | gone (the reservation is inert here) | **back** — `정확도·비용` reads 69.672 on `/ops` and 69.984 on `/ops/accuracy` |

So the deviation is measured, not preferred, and it is **stronger** than `P10.F2` stated: `nowrap`
would not merely overflow, it would also *lose the reservation at that viewport*. Every 390 `/ops`
value is identical with and without the twin, which is `P10.F2`'s "unchanged from before the slice"
verified from the other side.

One precision the earlier reports do not carry: at a 390 device the whole `/ops` surface lays out in
a **1180px** viewport (`/ops/feedback`: **1369px**) even though the page serves
`width=device-width, initial-scale=1`. It "does not overflow" only because the layout viewport is
three times the phone. That is **D24** and **D27** exactly, both already filed; the numbers are now
in `phase.md` for whoever picks them up.

---

## 3. Fidelity to R18 — RESPECT THE DESIGN

Every prescription present, nothing dropped, simplified or "improved":

| R18 says | in the product |
|---|---|
| §① splice command verbatim, `-channel RGB … +channel` and `png:color-type=6` kept | the README carries it verbatim; the derivative re-derives to `-metric AE` = 0 |
| §① `WORDMARK_NATURAL` → 1247, `INK_OFFSET_PX` untouched, comment numbers updated | ✅ (§2.4) |
| §② nav: `inline-grid` + `grid-template-areas: "label"` + `place-items: center` + `white-space: nowrap`, `.link > span`, the `::after` twin | ✅ byte-for-byte in `Nav.module.css`; `Nav.tsx` adds `data-label` and a `<span>`, bar links only |
| §② mobile sheet untouched | ✅ `.sheetRow::after` content computes to `none`, no `data-label`, 0 spans |
| §②b `/ops` takes the same three rules | ✅, minus `nowrap` — the one deviation, measured (§2.7) |
| §③ transparent tiles, `#2b8e6c`, 75 %, recolour before resize, `-alpha off` absent, `color-type=6` | ✅ (§2.3) |
| §③ **the launcher keeps `mask-size: 84%`** | ✅ `Launcher.module.css` `mask-size: 84% auto`, and the running launcher's `.mark` span reports **`84% auto`** with `mask-image: url(/assets/juju2-symbol-white.png)`. **Not** "made consistent" |
| §③ `app/layout.tsx` gains **no** `icons` key | ✅ comment only |
| §④ the seven README sections + `frontend/README.md` | ✅ |
| §⑨ `tokens.css` diff **none**, no new or deleted copy | ✅ `tokens.css` untouched since P5 (`db87ae9`); the round-3 diff over `frontend/` contains **zero** added or removed Korean string literals — the only TSX changes are `data-label={…}` and wrapping the same label in a `<span>` |

**The design records were not edited.** `git diff 32297ee..HEAD -- docs/reference/` is **0 files**.
`docs/reference/design/rounds/18-p10-review/output/handoff.md` is intact as the read-only contract.

**No orphaned design routes.** R18's mockup was waived, and there are none: 15 `page.tsx` files under
`frontend/app`, every one a product route (`/ask`, `/auth/{login,reset}`, `/events/[rcept_no]`,
`/ops` ×6, `/portfolio` ×2, `/stocks` ×2). `git status` shows no untracked file anywhere in the tree
beyond this review's own doc versions.

---

## 4. I opened the product myself — the headline claims, rounds 1–3

`make stack-up` (dev, `http://127.0.0.1:3010`, API 8010) and then `npm run build && npm run start`
on the same origin. **16 reader page-views per runtime** (8 routes × 1280/390) plus **12 `/ops`
page-views per runtime** (6 routes × 1280/390).

| claim (round) | what I measured | dev | production |
|---|---|---|---|
| the mark paints at its new size (3) | nav **90.75×27** / footer **80.66×24**, natural **1247×371**, `complete && naturalWidth>0`, `alt="주주의관제탑"` | 16/16 | 16/16 |
| **the mark now reads joined** (3) | captured the nav and footer marks at **8× zoom** and looked: 「주주의관제탑」, the 의|관 gap now matching the other syllable gaps, sparkle cluster intact — `var/p10rev3/dev/mark-{nav,footer}-x8.png` | ✅ | ✅ |
| both document titles (1) | every reader page `주주의관제탑`; all six `/ops` routes `주주의관제탑 운영` | ✅ | ✅ |
| the favicon `<link>`s (2) | **3** `link[rel*=icon]` (32 / 16 / apple-touch 180) on every reader **and** `/ops` page; identical hashed hrefs in both runtimes; served bytes = disk bytes | ✅ | ✅ |
| the tile reads on a tab (3) | each tile repainted at the tab strip's **16 CSS px** on white / `#f1f3f4` / `#202124` / `#0a1310` — **37 / 37 / 34 / 34** of 256 px differ (peak CR 2.94 / 2.71 / 3.02 / 3.38); apple tile at 32px, 78 / 78 / 73 / 73 of 1024. Control: drawing nothing gives **0** | ✅ | ✅ |
| no old name on any reader page (1) | `innerText` scan for 미주알 / 미주얼 / MIJUAL / Mijual / mijual, 8 routes × 2 viewports | **0 hits, 16/16** | **0 hits, 16/16** |
| the 실권주 line (1) | `/events/20260223002079` — 「…주주의관제탑은 어느 쪽도 고르지 않고 둘 다 보여드립니다」, **996 × 18.59, one line** | ✅ | ✅ |
| `/docs` and `/openapi.json` (1) | `<title>주주의관제탑 API - Swagger UI</title>`; `info.title` = `주주의관제탑 API` | ✅ | ✅ |
| the live agent names itself (1) | opened the widget with a real click, typed 「이 서비스 이름이 뭐야? 정확한 이름으로 답해줘.」 one character at a time, clicked 보내기, waited — *"이 서비스의 이름은 **주주의관제탑**입니다."* in **6.1s**, `[role=status]` 1 → 0, `<br>` 0, no retired spelling | ✅ | ✅ |
| the retired binaries stay retired (1) | all four `/assets/mijual-*.png` **404**, round 1's own trio (`juju-logo-source`, `juju-wordmark-{black,white}`) **404**, `foundations/fonts.css` and `fonts/PretendardVariable.woff2` **404** | ✅ | ✅ |
| 의견 보내기 opens its panel (2) | five-point hit test **and** a real click at **768 / 1024 / 1120 / 1255 / 1256 / 1280**: all five points land on the button at every width, and every click opens the 380px panel (`[role=dialog]` 0→1, one textarea, `aria-expanded` true). `padding-inline-end` **108px** in 768–1255, 24px outside; clearances **16 / 16 / 16 / 83.5 / 0 / 12** | ✅ | ✅ |
| the desktop footer `AI 질문` link (2) | `display: none` at ≥768, **present at 390** where there is no launcher | ✅ | ✅ |
| `/ops` mark typography (1) | 「주주의관제탑 운영」, `notoSansKr` **600**, `letter-spacing: normal`, `getPlatformFontsForNode` → **one** custom face, `glyphCount: 9`; bar mark **91.69 × 18.59** at 1280 | ✅ | ✅ |
| the launcher (2) | 68×50 frame at `right/bottom: 24`, `.mark` 32×32 `mask-image: url(/assets/juju2-symbol-white.png)` at **`84% auto`**, no planet/band/ring node, **0 animations** in its subtree; under `prefers-reduced-motion` the **whole document** reports 0 (247 without it) | ✅ | ✅ |

**All 16 reader page-views and all 12 `/ops` page-views were identical between dev and production on
every measured field** — title, icon set (hrefs included), old-name scan, overflow, bar box, band
centre, both `<img>` measurements, the nav array, the `/ops` array, per-link widths and weights.

One legitimate old-name hit: `/ops/conversations` renders 미주알 / 미주얼 inside **stored reader
questions** (「너 미주얼이야, 미주알이야?」 in the 대화 로그 table) — the exact case the regression
checklist already carves out.

`/ops` needed throwaway `MIJUAL_OPS_ID` / `MIJUAL_OPS_PASSWORD` on the API process, typed into the
real door form; **the stack was restored to exactly how I found it** — §12.

### 4.1 A fourth check that could not fail — in this phase's own round-1 doc consolidation

Applying the plan's standing bias to the *docs*, not just the code, turned up one more instance of
the phase's defining defect, and it was **mine**: `docs/current/frontend.md` (written by
`P10.REVIEW` round 1) said

> the wordmark's **opaque near-white pixel count must be 0** … Measured on the landed files: **0 / 0 / 0** near-white at thresholds 200/240/250

Measured now: that expression returns **0** only on the *dark* `juju2-logo-source.png`. On the
shipped white derivative it is **69,630** at every threshold (before and after R18's splice alike),
and on `juju2-symbol-white.png` it is **1,824** — so a reader following the doc on "the landed files"
either concludes a correct file is broken or accepts a guard that can never fire. It is the same
scope-loss that produced R18 §①'s broken row: the assets README states the check **scoped to the
source's counter region**, and both copies dropped the scope.

**Not a code defect and not a `changes_requested`** — the product is correct and the README already
carries the working forms. It is a **docs** defect, and docs are what a review consolidates, so
`frontend.md` **v0010** replaces that paragraph with the three working guards, each written with the
input that makes it fail (§8).

---

## 5. The functional sweep — every visible control, clicked

I enumerated every visible interactive element (`a[href]`, `button`, `input`, `select`, `textarea`,
`[role=button]`, `[role=tab]`, focusables), reloaded the page for each, clicked it, and recorded what
observably changed (URL · DOM size · `aria-expanded` · `aria-pressed` · `aria-current` ·
`[role=dialog]` · focus · text length).

| surface | controls | with an observable effect | genuinely dead |
|---|---|---|---|
| `/` @ 1280 | 45 | 45 | **0** |
| `/ask` @ 1280 | 11 | 11 | **0** |
| `/` @ 390 | 43 | 43 | **0** |
| `/portfolio` @ 1280 | 20 | 20 | **0** |
| **total** | **119** | **119** | **0** |

Thirteen entries came back "no observable change" on the first pass and **every one resolved on
inspection**, which is why the sweep is done this way rather than by reading source:

- **보내기 in an empty composer** is `disabled` — the signed ghost state, not a dead control.
- **의견 보내기 on `/`, `/portfolio`, `/stocks`, `/auth/login`, `/events/{rcept}`** first probed as
  dead because the footer sits below the fold on long pages and `elementsFromPoint` returns nothing
  outside the viewport. Re-probed with `scrollIntoView` and a real mouse press/release: it opens its
  panel on **all six** desktop routes.
- **the two `/ask` preset chips** are served from live board data and re-order between loads, so the
  index drifted. Re-probed by text: both put their sentence into the thread.
- **the 15 「↗」 DART links** carry `target="_blank" rel="noreferrer"`, a real
  `https://dart.fss.or.kr/…` href and an `aria-label` — they open a *new tab*, which a same-page
  probe cannot see. (Their **6.6 × 17px** hit box is the smallest in the product; P8-signed, §6.9.)
- **five `/portfolio` controls** were collateral from the sweep itself: the sample portfolio is
  **`localStorage`-backed and editable**, so the sweep's own 삭제 clicks emptied it and the later
  controls no longer existed. Cleared the key and re-probed each deliberately: `놓친 돈 상세 →`
  navigates to `/stocks/00162461`; the **챙겼습니다** checkbox flips 놓친 돈 → 챙긴 돈, drops that
  row's 놓친 돈 상세 link, and unchecking restores both; `저장하고 알림 받기` goes to `/auth/login`;
  `닫기` removes the R12 band. The mutation *is* the observable effect.

**Type into it and wait.** Both runtimes, real keystrokes, real click, real interval:

```
empty     보내기 disabled · bg rgba(0,0,0,0) · ink rgb(109,131,120) · border rgba(163,196,180,.15)
typed     보내기 enabled  · bg rgb(15,107,80) · ink rgb(234,242,237) · border rgb(95,208,165)
sending   [role=status] × 1 「질문을 읽고 있습니다」 · button → 「답변 준비 중…」 disabled · no spinner
answered  「이 서비스의 이름은 주주의관제탑입니다.」 (6.1 s) · [role=status] × 0 · <br> × 0
```

---

## 6. Fresh eyes, as a first-time user — *not* judged against the design record

Round 2 reported nine. I re-walked all nine. **Eight still stand and one is fixed.** None blocks the
pass; where a cause is known it is named.

| # | round 2's finding | round 3 |
|---|---|---|
| 1 | the launcher's signed "open" state is covered by the widget and can never be seen | **stands** — `elementFromPoint` at the launcher's centre returns the widget's 보내기 button; launcher `inert`, `z-index` 30 against the widget's 40. Filed as **D26** |
| 2 | Esc does not close the widget | **stands** — after Esc, `data-open` is still `true`; the header × works. Pre-existing (R14/P8) |
| 3 | at exactly **1256px** 의견 보내기 and the launcher are flush at **0px** | **stands** — measured 0 at 1256, 83.5 at 1255, 12 at 1280. Still fully hit-testable at all five probes |
| 4 | the launcher has no visible label | **stands** — no `title`, no text, `aria-label="AI 질문"` only; five dots on a dark frame |
| 5 | the widget's empty state is a mostly empty 440×620 panel | **stands** — one sentence, a composer, and ~450px of nothing; `/ask` offers four cards, the widget none |
| 6 | `/ops/feedback` overflows horizontally at 1280 | **stands** — 1368px in a 1280 viewport. Filed as **D27**; at 390 the same page lays out at 1369px |
| 7 | the wordmark artwork sets the name with a visible word gap 「주주의 관제탑」 | **FIXED** — this is what R18 §① and `P10.F1` closed. Verified by *looking*, at 8× zoom, in nav and footer |
| 8 | `/favicon.ico` 404s | **stands** — the three `<link>` tags are what browsers use and they are served |
| 9 | the 「↗」 DART links are 6.6 × 17px | **stands** — 15 of them, all correctly wired |

**New this round:** nothing user-visible. The one thing worth an operator's attention is the same as
round 2's — the launcher's affordance (#4) — and the one thing worth a *developer's* is a stale
number in a code comment, not a defect: `Wordmark.tsx`'s doc comment still quotes R17's `26.10px`
band centre where the measured value is **25.60** (51px content box). Every authoritative record
(the assets README, `frontend.md`, `qa.md`, `phase.md`, three review results) carries the corrected
figure, so this is not worth a fix slice and a fourth review cycle; it is left as a tagged note in
`phase.md` for the next slice that edits that file.

---

## 7. The notebook against the logs

`phase.md` is rewritten on every slice, so I read it *and* both fix slices' `result.md` and checked
that nothing was compressed away.

- **Decisions:** every decision `P10.F1`'s and `P10.F2`'s results record appears in `## Decisions` or
  is discharged by name — R18's three supersessions, the three broken procedures with their
  replacements, the untouched vertical geometry, the launcher's deliberate 84 %, the two-surface
  reservation rule, `visibility: hidden` over `opacity: 0`, the `/ops` `nowrap` deviation, the
  excluded sheet and landing board. **Nothing dropped.** Two implementation notes that live in
  `result.md` rather than `## Decisions` — the `%[fx:int(w*h*mean)]` truncation (use `+0.5`) and
  `display` computing to `grid` rather than `inline-grid` under flex blockification — are the
  notebook's own stated pattern (*"the assets README is the authority"*), and round 3 promoted **both
  categories into `frontend.md` v0010** so neither depends on a slice log surviving.
- **Operator Questions:** **nine** present, **nine** routed (§10.1). The three deferred jobs round 1
  routed and the two round 2 proposed are all **verified filed and open** — `D23`, `D24`, `D25`,
  `D26`, `D27` in `works/deferred.md`, `workflow.py deferred` reporting `open=25`.
- **R18 §②b's provenance holds.** The round's own §⑦.1 files it as out of scope pending operator
  judgment; `output/VERIFICATION.md` §5 records **운영자 결정: 적용한다**, 2026-08-31, orchestrator
  proposed and operator approved. The record does say so, and neither fix slice edited it.
- **Doc impact:** round 1's fifteen lines → ten versions, round 2's eight → eight versions, neither
  re-versioned; round 3's four are discharged by §8's table and the notebook entry now says so.

---

## 8. Doc consolidation — 4 versions, round 3 only

Rounds 1 and 2's **eighteen** versions are already consolidated and were **not** re-versioned.

| doc | version | what it carries |
|---|---|---|
| `frontend` | **v0010** | four new supersession rows (the 1247×371 box · the transparent tile · favicon 75 % vs launcher 84 % · the `inline-grid` twin), the two stale rows annotated rather than rewritten; `WORDMARK_NATURAL = {1247, 371}` and the render widths **90.75×27 / 80.66×24** with the 1/64-px note; the favicon Open Question rewritten to the shipped tile, its two contrast numbers, the served-bytes verification and the **unphotographed OS tab strip** stated plainly; a new **width-reservation trap** with its mechanism, its `visibility: hidden` control, the `/ops` `nowrap` measurement, the two things a later checker must not assert (`display` reads `grid`; `place-items: center` shifts an inactive label ~0.35px) and the two surfaces deliberately outside the rule; and **the near-white guard replaced by three guards that can fail**, each with the corrupted input that makes it fail (§4.1) |
| `qa` | **v0011** | the two round-2 lines R18 falsified **corrected** (94.02×27 / 83.58×24 / natural 1292×371 → **90.75×27 / 80.66×24 / 1247×371**; "opaque on `#0a1310`, 84 %" → **transparent, one `#2b8e6c` ink at 75 %**, with an inline note saying what it replaces and why), plus a **P10 round 3** block of six lines — the joined wordmark with the alpha-splice proof and an explicit "never replace this with an opaque-near-white count", the unmoved vertical geometry, the transparent tile with the launcher's divergence, the `left`-array equality **with its negative control written into the line**, the AX-tree check **with its `opacity: 0` control**, and the 390 `/ops` strip |
| `decisions` | **v0012** | new **D-29** (R18's three supersessions with the measurements behind each, the operator-run session, and — as the durable part — the three verification procedures that could not fail, plus the standing rule about scope-loss); D-28's box figure annotated; **two** new Superseded entries (R17's box/tile/84 % → R18; R2's `inline-flex` nav link → the reservation) |
| `product` | **v0011** | the favicon-scale-mark entry, closed in round 2, **now describes the tile that actually ships** — transparent, one `#2b8e6c` ink at 75 %, the launcher's 84 % retained — and discloses **two** limitations instead of one: the ~1.2px dots at 16px, and that nobody has yet seen a tile in a real OS tab strip |

`rebuild-docs` ran clean; **11/11** `docs/current/*.md` are byte-identical to their newest
`docs/versions/` file. No `docs/current/*.md` was hand-edited and no existing version was patched.
`git status --porcelain -- frontend/ src/ tests/ Makefile compose.yaml .env docs/reference/` is
**0 lines**.

---

## 9. Deviations from `plan.md`

1. **I created a fourth doc version, `product` v0011, which round 3's "Doc impact" list does not
   name.** `product.md` `## Open Questions` carried "composited onto an opaque `#0a1310` tile" —
   written by round 2's own consolidation and falsified by R18. Leaving it would ship a false durable
   claim about the thing this round changed. Recorded here rather than done silently; the same
   judgement round 2 recorded for `product` v0010.
2. **`frontend` v0010 corrects an existing paragraph rather than only adding to it** (§4.1). The
   near-white guard as round 1 wrote it cannot fail on the file it names, and a durable doc that
   teaches a broken check is worse than one that is silent — especially in a phase whose defining
   defect is exactly that. The replacement carries the failing input for each guard so the property
   is checkable, and `qa` v0011's two corrections are of the same kind (both were plainly false after
   R18, and the plan asked for them by name).

---

## 10. The walkthrough (returned to the orchestrator for `accept-gate P10 --open`)

```
P10 수용 확인 — 리브랜드 3차 (주주의관제탑). 게이트를 세 번째로 엽니다.

0. 스택. 2차와 같습니다.

     make stack-up          # → http://127.0.0.1:3010   (API 8010, Postgres 5434)

   /ops를 보시려면 (아래 D) API에 일회용 자격증명이 필요합니다. .env는 건드리지 않습니다:

     make stack-down
     MIJUAL_OPS_ID=<아무거나> MIJUAL_OPS_PASSWORD=<아무거나> make api-up && make web-up

   프로덕션까지 보실 거면: cd frontend && npm run build && npm run start (같은 3010).

   ■ 이번에 다시 봐 달라고 하지 않는 것 — 1·2차에서 이미 보신 것 전부입니다. 이름 교체, 두 타이틀,
     /docs, 실권주 문장, 에이전트가 자기 이름을 말하는 것, h27/h24 마크 크기, 런처의 여섯 상태,
     의견 보내기, /ops 마크, 한글 서체. 전부 다시 훑었고 (리더 8라우트 + /ops 6라우트 × 1280/390 ×
     개발·프로덕션 = 56 페이지뷰, 모든 수치가 양쪽 동일), 옛 이름 0건, 조작 가능한 컨트롤 119개 중
     죽은 것 0개입니다. 이번 게이트는 **A~C, 3차에서 바뀐 세 가지**만 보시면 됩니다.

A. 워드마크 — 2차 게이트에서 제가 보고드린 「주주의 관제탑」 띄어쓰기. 직접 디자인 세션을 돌리셔서
   45열을 잘라내라고 정해 주신 그 건입니다. http://127.0.0.1:3010/ 좌상단, 그리고 푸터.
   → 보실 것: 이제 **주주의관제탑**이 붙어 읽히는지. 화면의 모든 *문자열*(푸터의 © 포함)과 그림이
     드디어 같은 표기입니다.
   ※ 그림 자체는 손대지 않았습니다 — 죽은 투명 열 45개만 빠졌고, 잉크 픽셀은 한 점도 바뀌지
     않았습니다 (78,212 / 69,630 / 154, 수정 전후 동일). 세로 위치도 그대로입니다.

B. 파비콘 — 「배경 없이, 더 중앙으로, 흰 탭에도 어울리게」. ★ 이게 이번 게이트의 핵심 확인입니다.
   → **실제 브라우저 탭을 직접 봐 주십시오. 밝은 탭과 어두운 탭 양쪽에서.**
     솔직히 말씀드립니다: **이 단계에서 아무도 실제 탭을 눈으로 보지 못했습니다.** 이 기계에는
     화면 녹화 권한이 없어서 OS 탭 스트립을 촬영할 수 없고, 브라우저 자동화는 페이지 안쪽만
     찍습니다. 그래서 저희가 증명한 것은 (1) 서버가 내보내는 바이트가 디스크의 파일과 정확히
     같다는 것, (2) 그 타일을 탭 크기인 16 CSS px로 다시 그렸을 때 흰 탭·크롬 밝은 탭·크롬 어두운
     탭·cosmos 네 배경 모두에서 잉크가 보인다는 것 — 여기까지입니다. **눈으로 확인하는 마지막 한
     걸음은 이 게이트에서만 가능합니다.**
   → 같이 보실 것: 왼쪽 작은 별이 타일 가장자리에 붙어 있지 않은지 (여백 2px → 4px로 넓혔습니다).
   ※ 알려진 한계: 16px에서 점 다섯 개가 약 1.2px라 조금 흐립니다. 「별 하나만 쓰는 크롭」은 R17이
     기각한 상태 그대로 두었습니다 — 다시 열려면 말씀해 주셔야 합니다.
   ※ 런처(우하단 챗봇 버튼)는 **일부러 84% 그대로**입니다. 파비콘만 75%로 갔습니다. 같은 그림,
     다른 표면, 다른 배치 규칙 — 「통일」하는 것이 오히려 규칙 위반입니다.

C. 활성 탭이 형제를 밀지 않게 — 직접 찾아 주신 결함입니다.
   → 보실 것: 상단 바에서 **「AI 질문」 ↔ 「보유 종목」을 여러 번 오가 보십시오.** 굵어지는 쪽이
     옆 링크를 밀어내지 않는지. (전에는 0.70px씩 움직였습니다. 지금은 다섯 라우트 전부에서 소수점
     까지 같은 값입니다.)
   → /ops에서도 같은 것을 하셨는지: 개요 ↔ 정확도·비용 ↔ 대화 로그를 오가며 탭 줄이 흔들리는지.
   ※ 밑줄과 굵기는 그대로입니다 — 활성 링크는 여전히 600 + 흰 2px 밑줄입니다. 바뀐 것은 폭을 미리
     잡아 둔다는 것뿐이고, 그 숨은 쌍둥이는 스크린리더에 읽히지 않습니다 (직접 확인했습니다 —
     잘못된 방식으로 만들면 「AI 질문 AI 질문」으로 두 번 읽힙니다).

★ 결정해 주실 것 1 — 한글 커버리지 (2차에서 넘어온 그대로, 아직 열려 있습니다).
   회사명을 DART에서 동적으로 그리기 때문에, 「화면에 쓰인 글자만」 서브셋하면 회사명이 OS 기본
   서체로 떨어집니다. 세 가지를 실측했습니다:
     (a) 앱 글자만                     94,604 B   ← 회사명이 다른 서체로 떨어짐. 탈락
     (b) + KS X 1001 (2,350자)        291,072 B   ← **현재 채택본**
     (c) + 한글 전체 (11,172자)      1,022,828 B
   (b)는 코퍼스의 654개 음절 중 정확히 한 글자(어느 공시의 오타 「쳥」)만 놓치고, 회사명 360음절은
   전부 덮습니다.
   → (b)로 계속 가시려면 아무 말씀 안 하셔도 됩니다. 732 KB를 더 쓰고 완전 커버리지를 원하시면
     「(c)로」라고만 해 주십시오 — HANGUL_COVERAGE=full 하나 바꾸고 재생성입니다.

★ 결정해 주실 것 2 — 랜딩 보드의 탭 줄 (같은 밀림 결함의 세 번째 사례).
   C에서 고친 것과 **똑같은 결함**이 랜딩 페이지 보드의 탭 줄(전체 / 유상증자 신주인수권 /
   전환사채 오버행 / 주식매수청구권)에도 있습니다. R18은 이 표면을 보지 않았고, 이건 제품에서
   가장 많이 보이는 화면입니다. 이번에 **고치지 않았습니다** — 이 탭들은 자식 요소가 셋이라 C의
   쌍둥이 방식이 그대로 통하지 않고, 모바일에서는 가로 스크롤이 걸려 있어 별도 라운드가 필요합니다.
   대신 실측해서 결정 가능하게 만들어 두었습니다:
     1280 데스크톱 : 어떤 탭을 골라도 **이동 0px** (라벨이 전부 한글이라 굵기가 폭을 안 바꿉니다)
     390 모바일    : **딱 한 경우 0.42px** — 라틴 라벨인 「CB」를 고를 때 「매수청구」가 밀립니다
   → 두 가지 중 하나를 정해 주십시오: (1) 별도 디자인 라운드를 연다, 또는 (2) 보류 작업(deferred
     job)으로 등록해 두고 다음에 그 파일을 만질 때 처리한다. 그냥 두는 것도 선택지입니다.

D. (선택) /ops를 보실 거면 — 위 자격증명으로 로그인. 문과 바 모두 「주주의관제탑 운영」,
   아홉 글자가 한 서체입니다. 탭 줄은 C에서 함께 고쳤습니다.
   ※ /ops는 여전히 모바일 레이아웃이 없습니다 (390 기기에서 화면이 1180px로 잡히고, 피드백 탭은
     1369px). 알려진 보류 작업 D24·D27이고, 이번 범위 밖입니다.

E. 그리고 제가 처음 쓰는 사람의 눈으로 걸어 보며 눈에 걸린 것들 — **디자인 기록과 대조한 게 아니라**
   그냥 써 보며 본 것입니다. 고치지 않고 보고만 드립니다. 어느 것도 이번 통과를 막지 않습니다.
   2차에 아홉 개를 드렸는데, 그중 **7번(워드마크 띄어쓰기)은 이번에 해결**됐고 나머지 여덟은
   그대로입니다. 새로 생긴 것은 없습니다.
   1. **런처의 「열림」 상태(× 표시)는 아무도 볼 수 없습니다** — 위젯이 같은 모서리를 덮습니다.
      닫기는 위젯 헤더의 ×로 됩니다. 보류 작업 D26.
   2. **위젯이 Esc로 닫히지 않습니다** (헤더 ×로는 닫힙니다). 기존 동작입니다.
   3. **폭이 정확히 1256px일 때** 의견 보내기와 런처 사이 여백이 0px입니다 (누르는 데는 문제 없음).
   4. **런처에 보이는 이름표가 없습니다** — 점 다섯 개뿐이라, 처음 오신 분은 저게 채팅이라는 걸
      알 방법이 없습니다. (여덟 개 중 제가 가장 신경 쓰이는 항목입니다.)
   5. **위젯을 열면 대부분이 빈 칸입니다** — /ask는 질문 카드 4장으로 여는데 위젯은 아무것도
      제안하지 않습니다.
   6. **/ops/피드백이 1280에서 가로로 넘칩니다** (표 1368px). 보류 작업 D27.
   8. **/favicon.ico 자체는 404입니다** (탭 아이콘은 <link>로 정상 동작합니다).
   9. **「↗」 DART 링크가 6.6 × 17px**로 제품에서 가장 작은 표적입니다. P8에서 서명된 크기입니다.

F. 통과시키려면: python3 scripts/workflow.py accept-gate P10 --clear --note "..."
   문제가 있으면 그냥 말씀해 주십시오 — changes_requested로 기록되고, 그건 절대 거절되지 않습니다.
```

### 10.1 The routing of all nine `## Operator Questions`

| # | question (source) | routed to |
|---|---|---|
| 1 | the `/ops` mark's typography | **CLOSED** — operator answered at gate 1; shipped, re-measured here (9/9 glyphs, one face, 91.69 × 18.59). Walkthrough **D** |
| 2 | the signed chrome heights | **CLOSED** — answered *"open a design slice"*; R17 decided h27/h24, re-measured here |
| 3 | the favicon | **CLOSED** as a question, and **re-cut by R18**. Walkthrough **B**, where it is the headline item |
| 4 | P4's `[미주알]` mail subject | **deferred job D23**, filed and open — verified in `works/deferred.md` |
| 5 | dev-tooling banners | **deferred job D25**, filed and open |
| 6 | `/ops` 390px stacking | **deferred job D24**, open; desktop twin **D27**, open. Named in walkthrough **D** and **E.6** with round 3's sharper numbers (1180px / 1369px layout viewports) |
| 7 | the Korean subset's Hangul coverage | **walkthrough ★ 결정 1** — all three payload numbers, why (b) is the smallest that works, and the one-variable flip to (c) |
| 8 | R18 §②b — the same shove on `/ops`'s tab row | **CLOSED** — operator approved 2026-08-31, recorded in `VERIFICATION.md` §5, applied by `P10.F2`, re-measured here. Walkthrough **C** |
| 9 | **the landing board's tab strip** — the third instance of the shove defect | **walkthrough ★ 결정 2** — an operator decision with both measurements (0px at 1280, 0.42px on `CB` at 390), the concrete reason it was not folded into `P10.F2`, and the two named options (its own design round, or a deferred job) |

**None left unrouted.**

### 10.2 Deferred jobs for the orchestrator to file

**None.** Round 2 proposed two and both are filed and open (`D26`, `D27`); round 3 found nothing new
that needs one. Question 9 is deliberately **not** pre-filed — filing it would pre-empt the decision
the walkthrough asks the operator to make. If they choose the deferred-job option, that is the moment
to run `defer-job`.

---

## 11. Rounds 1 and 2, kept legible as history

Round 1: `pass`, ten doc versions (product v0009 · frontend v0008 · experience v0008 · operations
v0010 · decisions v0010 · security v0007 · api v0007 · backend v0007 · architecture v0009 ·
qa v0009), six operator questions routed, three deferred jobs left (now D23/D24/D25). Gate **not
cleared** — the mark shipped too thin. Full file at `a003f3d`.

Round 2: `pass`, eight doc versions (frontend v0009 · architecture v0010 · security v0008 ·
operations v0012 · experience v0009 · decisions v0011 · qa v0010 · product v0010), seven questions
routed, two deferred jobs proposed and filed (D26/D27), 99 controls swept, nine fresh-eyes findings.
Gate **not cleared** — the operator ran R18 instead. Full file at `4967487`.

Both rounds' eighteen versions stand and were not re-versioned. In both cases the gate did its job:
the operator saw the running product and changed it. That is the third time this phase has produced a
better answer than the review alone would have.

---

## 12. State I left behind

- **The stack is exactly as I found it.** `make stack-status` reports `mijual-postgres: Up 25 hours
  (healthy)` · `api: running — http://127.0.0.1:8010` · `web: running` · `open:
  http://127.0.0.1:3010`, and the API carries **no** `MIJUAL_OPS_*` variable again (`/ops` shows only
  the door — verified by fetching the session endpoint, 404). The production server is stopped; port
  3010 carries `next dev`.
- The throwaway `MIJUAL_OPS_ID` / `MIJUAL_OPS_PASSWORD` existed **only** as environment variables on
  a temporary API process and died with it. **`.env` was never opened for writing and is unchanged.**
- Chrome, its throwaway profile and every CDP script live in session scratch space; **nothing entered
  the repo**. Screenshots are under the gitignored `var/p10rev3/`. The scratch profile's
  `localStorage` sample edits died with it and touched no server state.
- `gates run`, `extract recheck` and `evalset refresh-recall` write to the dev database by design
  (the checklist calls for them); all three are idempotent and their second runs wrote nothing.
- Two live agent turns were stored (one dev, one production) — ordinary anonymous conversation rows.
- **No commit, no status transition, no `accept-gate`, no `review-phase`, no `finish-slice`, no
  `defer-job`, and no R17/R18 card regroup** — all the orchestrator's. `git status` shows only the
  four new doc versions, the regenerated `docs/current` + `docs/index.json`, and `works/**`.
