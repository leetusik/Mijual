# P12.DECOMP2 — result

- **status:** done
- **summary:** Cut P12.R1's 14 findings into **nine `fix / high` slices** (`P12.F1`–`P12.F9`, orders 3.1–3.9) as bare folders, one per independent cause, with `P12.S2` left last at order 8. Recorded the plan's three orchestrator rulings (R1 F1 ×2, R1 F5) and four deliberate "no slice" verdicts (R1 F11/Q3 — the judgment call, decided **no slice**; F13; F14/Q5; the Q4 hover note) plus the slice → finding → files cut table in `phase.md`, and replaced the nine consumed `for P12.DECOMP2` notes with one shared verification bar for all nine fix slices.
- **files_changed:**
  - `works/phases/active/P12/phase.md`
  - `works/phases/active/P12/slices/P12.DECOMP2/result.md`
  - `works/phases/active/P12/slices/P12.F1/slice.json` … `works/phases/active/P12/slices/P12.F9/slice.json` (nine new bare slice folders)
  - regenerated by the engine: `works/backlog.md`, `works/index.json`, `works/state.json`, `works/events.jsonl`
- **validation:** `python3 scripts/workflow.py validate` → **passed** (exit 0; three pre-existing advisory warnings, all P4/docs-debt, none from P12)
- **deviations:** none — the cut is exactly the nine slices the plan's expected table names, at the orders it names, and the F11 judgment call resolved to the plan's stated default (no slice).
- **doc_impact:** none — this slice changed no durable truth (it created bare slice folders and edited the notebook). The nine fix slices each owe their own `frontend.md` note; the two `qa.md` notes P12 already owes are unchanged.

## What I did

### 1. The nine slices (bare folders, `new-slice` only)

Created with `python3 scripts/workflow.py new-slice --phase P12 --slice P12.F<n> --name "…" --kind fix --risk high --order 3.<n> --depends-on P12.R1`. No `plan.md` was written for any of them; each folder holds only its `slice.json`. Every slice is `high` because every one writes real code across more than one file and owes a real-browser before/after — `low` would route it to the `mid` tier.

The slice → R1-finding → files table is in `phase.md` `## Decisions` (not repeated here). In short: `F1` account-state seeding, `F2` launcher first paint, `F3` `/portfolio`'s two late bands (R1 F2 + F6), `F4` the lookup revisit cells, `F5` the logout flash, `F6` the Family-B ghost across three components (R1 F8 + F10 + F12), `F7` the feedback dialog, `F8` the search-miss line, `F9` the IBM Plex Mono fallback metrics.

Two collapses are worth naming: **R1 F2 and F6 became one slice** (`P12.F3`) because they are the same page, the same family and the same decision made twice ("the server knows" vs "reserve the box"), and **R1 F8, F10 and F12 became one slice** (`P12.F6`) because they are three applications of a single technique the repo already owns (`Nav.module.css`'s `::after { content: attr(data-label); visibility: hidden; height: 0 }` ghost). The plan's escape hatch for `P12.F6` — split it at its turn if a plan cannot verify three components in one dispatch — is recorded in the cut table rather than pre-empted here.

`R1 F1` was deliberately **not** collapsed into one slice: the account half and the launcher half share a symptom (a pop-in 27–293 ms after paint) and a viewport, but nothing else — different files, different mechanisms (a session read on the server vs. a media-query hook's initial value), and different failure modes. Two slices, `P12.F1` and `P12.F2`.

### 2. The rulings, verified against the code before recording

I read the files behind each ruling rather than transcribing the plan:

- **Ruling 1** — `app/layout.tsx` is indeed `async` and already awaits `getSiteContact(...)`; `lib/session.server.ts` and `components/chrome/{SiteChrome,useAccount,Nav}.tsx` all exist as the plan describes, so the `Promise.all` + prop + module-store seeding path is available. Recorded with its file-level mechanism.
- **Ruling 2** — `components/ask/{AskSurface.tsx,useAsk.ts,Launcher.module.css}` all exist; the ruling's substance (a `display: none` element is in no tab order and cannot be activated, so the signed 「≤767px: nothing」 rule survives a pre-hydration CSS guard) is recorded as the reason the fix is not a design change.
- **Ruling 3** — `app/fonts.ts` and `app/shell.css` exist and P4.F5's precedent is on the Noto face; recorded, with Q2's preload alternative explicitly **not** taken.

Q1 and Q2 were left untouched on the `## Operator Questions` list (append-only); the `## Decisions` lines say how each was resolved so the review can route them as "answered by the fix, shown in the walkthrough".

### 3. The F11 judgment call — no slice

The plan left R1 F11 (the board's 갱신됨 chip, 52.55 px) to my judgment with a default of "no slice", to be overturned only if `Board.tsx` / `Board.module.css` offered a mechanism leaving the resting layout byte-identical. I read both. The chip is rendered inside the right-aligned `span.fresh` flex group of a `justify-content: space-between` header:

```
.header { display: flex; flex-wrap: wrap; align-items: baseline; justify-content: space-between; }
.fresh  { display: flex; align-items: center; gap: var(--space-2); }
.updated { font-family: var(--font-mono); padding: 2px 8px; white-space: nowrap; }
```

so Family B's ghost inside `.fresh` reserves width that pushes the 기준시각 stamp ~52 px left **in the resting state** — a visible change to a header the R2/R9 rounds signed, in `frontend/components/landing/`, which carries the P4.F7/F11 "looks and moves exactly as today" constraint. The only mechanism that leaves the resting layout identical is taking `.updated` out of flow, which breaks R9 §7's signed 「the chip and the badge are one group」 relationship and can collide with the title on a wrapping header at 390. Two further reasons stand behind that: the chip is 52 px of a refresh that simultaneously re-ranks 209 rows and grows the page 2 px — the motion the reader actually sees is the re-rank, which is **Q3's product decision** and which no CSS settles — and the trigger only fires when `as_of` moves (twice-daily pipelines), so a fix could not be verified on a genuine refresh at all, only under R1's throwaway `fetch` patch. **Verdict: no slice; Q3 carries it.** Recorded in `## Decisions` beside F13, F14/Q5 and the Q4 note so the review does not read any of the four as an omission.

### 4. Notebook

`phase.md` (see the file itself; nothing restated here):

- `## Decisions` — appended the three rulings, the four "no slice" verdicts, and the final cut as a table (slice → R1 findings → family → files). Nothing superseded, so nothing replaced.
- `## Notes for later slices` — removed **all** ten `for P12.DECOMP2` entries (DECOMP's "how to cut", R1's "four causes not fourteen bugs", the eight F1–F14 finding notes and the "refuted or clean" summary — the finding detail lives in `slices/P12.R1/result.md` and each plan is written from there at its turn); added **one** shared note tagged `**(from P12.DECOMP2, for P12.F1 … P12.F9)**` carrying the verification bar (RESPECT THE DESIGN / `AE = 0` on the resting state, before-and-after in Aside `--account u2` in dev *and* a **fresh** local production build at 1280 + 390 and the cold throttled 412×915 profile where fonts or load are involved, a control for every zero, `typecheck` + `smoke`, no test files, throwaway account, production read-only, and the per-slice `## Doc impact` / `for P12.S2` duty). The `for P12.S2` release note was left exactly as it was.
- `## Operator Questions` — untouched; the cut raised no genuinely new question.
- `## Now` — rewritten (8 lines): the cut, `P12.F1` next, the rulings, the four no-slice verdicts, the shared bar, Q1–Q5 still open, and the freeze arithmetic (nine fix slices + `P12.S2` before 2026-09-07 11:00 KST or the release waits for 09-12).

Notebook size after the edit: **32,188 B**, down from 42,504 B — well inside the ~400 KB budget.

## Validation

| Command | Result |
|---|---|
| `python3 scripts/workflow.py validate` | **passed**, exit 0. Warnings are pre-existing and not P12's: `consolidation_owed=P4`, `stale_docs=product` (P4's unconsolidated notes), `oversized_doc_sections=11` (a docs-phase concern). |
| `ls works/phases/active/P12/slices/P12.F*` | each folder holds `slice.json` and nothing else — bare, as required. |

## Not done, on purpose

No product code, no `docs/` edit, no `plan.md` for any slice (mine included — it was written by the orchestrator), no commit, and no workflow command other than `new-slice`. No `co-work` slice, no OG/Kakao slice, no slice for R1 F13 / F14 or the Q3–Q5 decisions, and nothing rated `low`.
