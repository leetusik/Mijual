# P12.REVIEW — phase review, "Flicker polish"

- **status**: done
- **summary**: Reviewed all 15 completed P12 slices together — re-ran their validation, re-measured
  every headline number myself with its own control on dev, a fresh standalone production build and
  production `004d936`, ran the four acceptance-gate stages on a `required` gate, re-ran the
  cumulative regression checklist and appended this phase's block to it, and routed all nine
  operator questions and four residuals. Verdict **pass**; the acceptance gate is still to open.
- **files_changed**:
  - `/Users/sugang/projects/personal/Mijual/docs/versions/qa/v0018_p12_the_flicker-polish_regression_block.md` (new)
  - `/Users/sugang/projects/personal/Mijual/docs/current/qa.md` (regenerated by `rebuild-docs`)
  - `/Users/sugang/projects/personal/Mijual/docs/index.json` (regenerated)
  - `/Users/sugang/projects/personal/Mijual/works/phases/active/P12/phase.md`
  - `/Users/sugang/projects/personal/Mijual/works/phases/active/P12/slices/P12.REVIEW/result.md`
  - **no source file was touched** (`git status --porcelain` shows no `frontend/` or `src/` entry)
- **validation**:
  - `cd frontend && npm run typecheck` — pass (exit 0, no diagnostics)
  - `cd frontend && npm run smoke` — pass (**22/22**, 0 fail)
  - `cd frontend && npm run build` — pass (standalone production build in a fresh copy outside the
    repo, served on `127.0.0.1:3014` for the review's own measurements; server stopped)
  - `make smoke-prod` — pass (**17/17, 0 fail, 11.7 s** against `https://jujutower.com`)
  - `python3 scripts/workflow.py validate` — pass (3 advisory warnings: `consolidation_owed=P4`,
    `stale_docs=product`, `oversized_doc_sections=11`)
  - `python3 scripts/workflow.py rebuild-docs` — pass
  - every completed slice's own validation commands, re-run (table in § Stage A)
  - the whole `## Regression Checklist` — 146 lines, dispositions in § Stage C-4
- **deviations**: three, all in § Deviations — 35 checklist lines not re-run as written (each named
  with its reason and substitute), no `operations` doc version written (P4 already owes that
  section), and one dev-runtime substitution for a `next dev` overlay artefact.
- **doc_versions**: `qa` **v0018** — `## Regression Checklist`, the P12 block (8 new `- [ ]` lines
  plus three instrument notes). **`operations`: none** — see § Deviations 2. Everything else:
  `none — deferred to a docs phase` (18 of the phase's 19 `## Doc impact` lines remain unpaid).
- **review_verdict**: **pass**
- **walkthrough**: § Walkthrough below (returned in full beside this verdict)
- **explain**: not written — run /explain for this phase

---

## Scope and method

15 completed slices: `DECOMP`, `S1`, `R1`, `DECOMP2`, `F1`, `F2`, `F3`, `F4`, `F10`, `F5`, `F6`,
`F7`, `F8`, `F9`, `S2`. Read: this slice's `plan.md`, the `review-phase` skill, `CLAUDE.md`,
`intent.md`, `phase.md` whole, every slice's `result.md` head-first, `docs/current/operations.md`
§ `## Operator Runtime`, and `docs/current/qa.md` § `## Regression Checklist` whole.

**Runtime table used throughout** (from `## Operator Runtime`):

| # | runtime | origin | notes |
|---|---|---|---|
| 1 | dev | `http://127.0.0.1:3010` (`make stack-up`, `next dev`, StrictMode) | the operator's own stack, left up as found; all signed-in checks ran here |
| 2 | production build | `http://127.0.0.1:3014` (`next build` + standalone, fresh copy outside the repo) | built at HEAD by this review; server stopped at the end |
| 3 | production | `https://jujutower.com` (Cloudflare → `edge-nginx` → `mijual-web`), release `004d936` | **read-only**: no account, no login, no feedback send |

Viewports: **1280**, **390**, and **412×915 @ DPR 2.625 / 4× CPU / ≈1.6 Mbps / 150 ms** for the
cold-cache lines. Instrument: **Aside**, `aside repl --account u2` over Bash (profile 「claude2」) —
never `u0`, never the operator's signed-in profile. Every trap the notebook records was respected
(CDP through `page._sendToTarget`, init scripts through `Page.addScriptToEvaluateOnNewDocument` with
the `MutationObserver` on `document`, one `evaluate` argument, ≤ ~10 loads per invocation, one
screenshot per invocation, no `page.waitForTimeout`).

---

## Stage A — validation of all slices together

| slice | its own validation, re-run here | outcome |
|---|---|---|
| `P12.DECOMP`, `P12.DECOMP2` | `workflow validate` | pass |
| `P12.R1` | findings-only; no product code (`git diff` confirms none) | pass |
| `P12.S1`, `P12.F1`–`P12.F10` | `npm run typecheck`, `npm run smoke`, a production build in a copy | pass — typecheck clean, **22/22**, build clean |
| `P12.S2` | `make smoke-prod` against the live origin | pass — **17/17, exit 0** |
| all | `python3 scripts/workflow.py validate` | pass (3 advisory warnings) |

**Re-measurement of each fix's headline number, by me, with its control.** Every number below is
this review's own measurement, not a quotation of the slice that produced it.

| fix | claim | my re-measurement | control that proves the detector fires |
|---|---|---|---|
| `S1` | account caret is one rect across the toggle | one distinct frame rect over 4 open/close cycles on dev (signed in, throwaway account); caret box **5.672 × 12 px**, computed `transform-origin` **`2.83594px 8.004px`** — the ink-centre pivot, not the line box's | a planted second glyph changes the frame width immediately |
| `F1` + `F2` | chrome is in the first paint | production, 1280, warm, 10 public routes: 로그인 link / account frame and the AI 질문 launcher enter the DOM **13–102 ms before FCP**, **CLS 0 on 10/10**, slot at one rect `[1138.73, 15.03, 37.27, 20.92]`. Signed in (dev + `:3014`) the frame lands **44–64 ms before FCP** and the served HTML carries the reader's own email | the same probe on the HEAD build reports the elements *after* FCP, as `R1` found |
| `F3`, `F4`, `F5` | the mirror seam's four uses move 0 px | with storage seeded **in the product's own shape**: `data-mj-offer-seen`, `data-mj-carry-rows`/`-kind`, `data-mj-lookup-holding` (+ server `data-mj-cells`/`-foot`) and `data-mj-auth-flash` each fill inside a reserved box — **0 elements moved, CLS 0, no `layout-shift` entry**; the auth flash band holds at **40.594 px** | I first seeded `mijual.lookup.holdings` as a bare map, which the product rejects: that produced a **false clean zero**. Re-read `readSessionHoldings()` for the real `{v:1,entries:{…}}` shape, confirmed the filled state actually rendered, then measured |
| `F10` | removed sample rows never paint | the removed code's row is absent from every sampled frame; page height is its settled height from frame 1, **CLS 0**; **32** per-code rules served on production today | HEAD in the same state drops the row visibly at hydration |
| `F6` | 정정 이력 is one width | 4 toggles at 1280 on `/events/{rcept_no}`: **one distinct rect `[125, 1178.94, 77.53, 36]`** | HEAD narrows 77.53 → 66.70 on every open |
| `F7` | the feedback dialog is one height | 1280 `[796, 526, 380, 318.016]` and the ≤480 sheet `[0, 498.984, 390, 345.016]` across editing → sending → sent → failed → 다시 시도; actions row y **791 / 780** in every state; the editing body carries no inline style, every later body carries `min-height` **263.016 / 291.016 px** | a planted long failure message *does* grow the body past the `min-height` — the pin is a floor, not a cage, exactly as `F7` recorded |
| `F8` | the search-miss box outlives the sentence | one real keystroke on `/stocks?q=<miss>`: **0 elements moved, no `layout-shift` entry**; the `<p>` takes `visibility: hidden` and leaves the AX tree | typing the submitted text back makes the same sentence visible in the same box |
| `F9` | the cold mono swap moves nothing | 412×915 cold: worst per-element mono width delta **0.015 px** (`/stocks/00547510`) and **0.016 px** (`/portfolio`), **0 across the Plex `responseEnd` boundary**, no `layout-shift` entry near either Plex file | `*IBMPlexMono*` blocked outright → the page lays out within **0.047 / 0.078 px** of loaded, with `document.fonts` reporting `plexMono: error`, which is what proves the block fired |
| `S2` | the release is live and harmless | `make smoke-prod` **17/17**; production serves `004d936`'s markup on all 10 routes | — |

**Two instrument artefacts I had to disprove before trusting a number** (both now in the qa doc):

1. My first `F4` rect key used occurrence indexes. The ① 환산 row *prepends* two cells, so `cell[1]`
   and `cell[2]` appeared to travel **217 / 434 px**. Re-keyed by each cell's own label: the two
   surviving cells stay at x **423** and **857** and do not move at all.
2. The hydration detector must be shown able to fire, and *where* it fires differs by runtime. A
   planted deep text mismatch throws **`Minified React error #418`** in the production build (4/4
   routes) and the long dev message in `next dev`; the **attribute**-mismatch control on `<body>`
   fires **only in `next dev`** (2/2), and on `<html>` is silent in both — which is
   `suppressHydrationWarning` scoped to exactly that element, i.e. checklist line #123 holding.
   Installed from a `readystatechange` listener the injection lands too late and **both controls read
   as silent**, which would have been a false clean.

---

## Stage B — judgment, notebook cross-check, doc-impact verification

**Against the objective and `intent.md`.** The phase was scoped to flicker polish, with the design
round skipped and the fixes made directly inside the phase, sequential, every fix bounded by RESPECT
THE DESIGN. That is what landed: nine flicker fixes plus the caret, all resting layouts unchanged
(`AE = 0` proofs in the slices, re-confirmed by me on `/` at 1280 and 390 against a same-commit
production build with the Cosmos field masked), no signed element dropped, restyled or "improved",
no new visual vocabulary, and no product decision taken where the record was silent — the four
places where a fix would have required one are Q6–Q9 and were left as questions with **no code
shipped**, which is exactly the right call. OG/Kakao stayed dropped.

**Notebook cross-check (`phase.md` against every `result.md`).** No decision recorded in a
`result.md` is missing from `## Decisions`: the instrument seam and each trap, the three `DECOMP2`
rulings, the four no-slice verdicts, and one landed-fix line per slice are all present, and the
superseded ones were replaced in place rather than stacked. No `## Operator Questions` entry was
dropped — Q1–Q9 are all present and all are routed in § Stage D. No `## Notes for later slices`
entry was lost: the three tagged `for P12.REVIEW` are answered here and removed by this slice.
**No finding.**

**`## Doc impact` coverage — verified, 19 lines.** 11 `frontend`, 4 `qa`, 2 `security`, 1
`operations`, plus the `qa` Regression Checklist line this review paid. I checked the list against
the diff rather than against the slices' prose: every file in `git diff a74c58a..HEAD` that changes
durable truth is named by a line — the chrome/account seam, the launcher guard, the pre-hydration
mirror and each of its four uses, the corrections twin, the feedback `min-height`, the search-miss
`visibility`, the mono fallback faces, the two security lines (a signed-in reader's email now in the
HTML; the "anonymous state never reaches the server" principle re-affirmed for the seam), and the
release. **Nothing durable is missing and the list is complete.** The `security` lines matter and I
re-derived the first one myself: the signed-in document is a dynamic render under
`Cache-Control: private, no-cache, no-store, max-age=0, must-revalidate` and the edge declares no
`proxy_cache` zone, so no reader's email can be served to another.

---

## Stage C — the acceptance-gate stages (`acceptance.required: true`)

**C-1 — the manifest.** `docs/current/operations.md` § `## Operator Runtime` is present, filled, and
carries no `UNFILLED` marker: dev command and origin, production origin and path, the three
viewports, the local production-build recipe and port, and the deploy freeze. It is **stale on
exactly one point** — it still prescribes Chrome over CDP as the instrument and still says Aside's
daemon does not run on this machine, both untrue since P12. That is a `## Doc impact` note **P4
already owes** (see § Deviations 2), not a gate blocker: the runtime it names is the runtime I used.

**C-2 — I opened the running product myself** in all three runtimes and spot-checked every headline
claim; the numbers are the Stage A table, which is my own measurement throughout. I passed nothing
on another slice's report.

**C-3 — fresh eyes, as a first-time Korean reader**, at 1280 and 390, on production, **not judged
against the design record**. Five findings, all routed to the operator in § Walkthrough and none
silently fixed:

1. **The 챙겼습니다 checkbox at 390 is a 15 × 15 px target.** `Portfolio.module.css` `.claimBox` is
   `width: 15px; height: 15px`. It is **pre-existing and untouched by P12** (`git diff a74c58a..HEAD`
   touches no claim line), and it is the same family as P11's still-open 「≤767 칩 타깃은 14 × 16 px」
   decision (checklist #120). On a phone it is genuinely hard to hit.
2. **The 조회 revisit reflow on a 놓친 돈 stock is the most noticeable jump left in the product.**
   Reproduced by me on production at 1280 with a remembered holding on a stock serving 놓친 돈 rows:
   **69 elements move, the breakdown travels +140.19 px, CLS 0.01232** (entries `0.01006` @225 ms and
   `0.00227` @392 ms). **Identical on HEAD**, so P12 neither caused nor worsened it — but a reader
   coming back to the page they cared about sees the one shift the phase did not remove.
3. **The send button grows under the cursor.** On `/ask` the resting 보내기 becomes 답변 준비 중… and
   in 의견 보내기 it becomes 보내는 중입니다 — the control the reader just pressed changes size while
   they are still looking at it. This is Q7/Q9 seen from the reader's side rather than the ruler's.
4. **A bad `/events/{rcept}` paints an empty page first**, then the Korean not-found copy. Correct
   status (404) and documented (D35), but as a first-time reader it reads as a broken page for a beat.
5. **On a slow connection the ① 환산 row draws in two stages.** The box never moves — that is
   `F4` working — but the cells arrive a beat after the row, so the row looks briefly half-filled.

**C-4 — the whole `## Regression Checklist`, re-run.** 146 lines before this review's append.
"Re-run" below means *I executed the check in this review, in the runtime named*; everything I could
not execute is named individually in § Not re-run with its reason and its substitute.

| block | lines | re-run | not re-run |
|---|---|---|---|
| preamble (repo, CLI, corpus invariants) | 14 | 5 | 9 |
| P8 surface blocks | 58 | 53 | 5 |
| P9 surface blocks | 18 | 6 | 12 |
| P10 rebrand block | 6 | 6 | 0 |
| P10 round 2 (R17) | 7 | 7 | 0 |
| P10 round 3 (R18) | 7 | 6 | 1 |
| P10 round 4 (`P10.F3`) | 13 | 11 | 2 |
| P4 production block | 23 | 17 | 6 |
| **total** | **146** | **111** | **35** |

Of the 111 re-run, **109 were executed** and 2 are struck-through superseded lines with nothing left
to run (**#20** 푸터, superseded at `P11.F2` by the operator's own decision; **#105** 세로 기하,
falsified at P10 round 4 and replaced). **Every executed line passed.** Four carry a note:

- **#112** — 로그인 still sits **0.75 px** below the nav links. Unchanged, known, not new.
- **#113** — "a client-rect count is not a line break": honored in my own measurements, which is why
  every rect key in this review is label- or geometry-based rather than index-based.
- **#120** — the ≤767 chip target is still **14 × 16 px**, not 44 px. Still an open operator
  decision, and the same family as fresh-eyes finding 1.
- **#59** — 챙겼습니다 flips 놓친 돈 → 챙긴 돈, keeps the amount and 「추정」, and moves nothing. Re-run
  on production with `localStorage` only (no account, no server write); the key was removed after.

Concrete results worth recording from the re-run: **#124** `make smoke-prod` **17/17**; **#28 /
#144** 자동 갱신 held open for a full 66 s interval on production — the expected `/api/board` calls,
**no spinner, no row-count change, tab and scroll preserved, nothing moved**; **#107 / #108** the nav
AX dump shows **one** `StaticText` per link, and the control (making the ghost twin visible at
`opacity: 0`) doubles the names, proving the dump would have caught a double read; **#134** the
production sample serves four distinct issuers and a 보유량 edit, a 삭제 and a 챙겼습니다 all survive a
reload; **#12** no tracked file matches an env/secret/key pattern; **#122 / #123** an unknown
production address echoes the typed path with **no React #418**, and the `<html>`
`suppressHydrationWarning` covers that one element only (proven by the `<body>` control above).

### Not re-run (35 lines) — each with its reason and what I checked instead

**P12 touched 0 Python files.** `git diff a74c58a..HEAD` is `frontend/` plus `works/`/`docs/` only,
which is the reason behind most of this list.

- **#1 (half), #3, #6, #8, #9, #10, #11, #13, #14** — the backend/corpus CLI half of the preamble
  (`pytest`, `gates run` determinism, the agent's live-pass numbers, `estimate report`,
  `scheduler once --offline`, post-corpus-change re-measurement, `extract recheck` /
  `evalset refresh-recall`, the evalset-label wording, the regenerated summary artifact). **Reason:**
  no Python, no corpus and no artifact changed in this phase, and #10/#14 have no trigger at all.
  **Instead:** `workflow validate` clean, the frontend-only diff above, and **#7** (the exposure
  invariant) *was* re-derived read-only against the dev DB — 0 renderable fields outside
  `passed`/`tbd`, counts only, nothing dumped.
- **#27, #36, #41, #43, #49** — corpus-shaped surface lines (a *tied* 청약 마감; 아시아나's ③ chips and
  its 2단계 절차; 풍전약품's ②-only page; 계양전기's pre-priced state). **Reason:** each needs the named
  issuer still serving that exact shape today, which I did not confirm, and re-anchoring them is a QA
  job rather than a P12 regression. **Instead:** the same code paths were exercised on the stocks the
  corpus does serve (including a 놓친 돈 stock and a live ① stock at both viewports), and none of
  them is touched by the P12 diff.
- **#73–#82, #85, #88** — the P9 lines that each need their own live model turn with a specific
  prompt shape (greeting, out-of-scope, company-less first question, calculation, injection attempt,
  ≥4 tools, progress indicator, unverified figure, exhausted turn, 새 대화, the stored turn blocks).
  **Reason:** the plan capped me at **1 production + 6 dev** `/api/ask` turns and I spent **1 + 2**.
  **Instead:** the three turns I did run exercised the frames these lines assert *around* — one `<p>`
  of inline sentences with 0 `<br>`, exactly one `[role="status"]` at a time, the 도구 흐름 folding on
  settle, the 근거 N건 footer, the citation popover — so the frame machinery is live at this release;
  the prompt-specific behaviours are unverified here and were last verified at P9. Nothing in the P12
  diff touches `components/ask` beyond `AskSurface`'s launcher unmount and `useAsk.ts`.
- **#104** — the wordmark's ink-gap / counter-island pixel scan. **Reason:** a PNG trim-box analysis
  of an asset P12 does not touch. **Instead:** the served wordmark asset is unchanged since P10 (same
  hashed filename, still 200 with its long cache lifetime — #142 re-run and clean).
- **#116 (half)** — the citation popover's **≤767 340 px** half. **Reason:** at 390 my selector could
  not resolve the popover host after a real pointer press, and I chose not to spend another Aside
  cycle on a line P12 does not touch. **Instead:** the desktop half (380 px under the chip, rows
  behind it immobile) was re-run and is clean, at 1280.
- **#118** — 「API가 죽어도 시작 화면은 넉 장이다」. **Reason:** it requires stopping the API, and the
  operator's dev stack must be left exactly as found while production is read-only. **Instead:**
  `/ask`'s start screen was re-run with the API **up** (four cards, two even rows, no orphan).
- **#128 (half)** — the `/ops` **logged-in** half. **Reason:** it needs the operator credential; I do
  not use it. **Instead:** the door half was re-run anonymously — 마크 + 운영자 ID + 비밀번호 + 로그인
  with `noindex, nofollow`.
- **#135, #136, #137, #138** — the D-day mail demo, the `MIJUAL_EXTRACT_MAX_CALLS` container check,
  the post-deploy no-harm assertions and the backup freshness check. **Reason:** all four are
  ops-box/production-side actions; the demo sends real mail, and **nothing deploys** under the
  2026-09-07 11:00 KST freeze. **Instead:** #137's assertions were run by `P12.S2` at the release
  itself (identical before and after, `docker ps` differing in exactly one line) and no deploy has
  happened since; `make smoke-prod` 17/17 covers the live surface.
- **#145** — the `blink.animations` idle-cost trace of `/` over 8 s. **Reason:** the Aside `repl`
  surface exposes no tracing-category capture. **Instead:** a `document.getAnimations()` census over
  an idle landing, which is the same question one layer up and matches the recorded shape (the
  Cosmos field alone, no other animated element).

**C-4 append.** This phase's headline checks are now in the qa doc as
`docs/versions/qa/v0018_p12_the_flicker-polish_regression_block.md` — 8 `- [ ]` lines (계정 캐럿,
크롬 첫 페인트, the mirror seam's four uses, 편집된 샘플, 정정 이력, 의견 보내기, 검색 불일치, 콜드 캐시
mono) plus three instrument notes, each carrying the number, the control and the trap that produces a
false result. Only `## Regression Checklist` was edited; `rebuild-docs` then `validate` both clean.

**C-5 — routing.** § Stage D. Every one of Q1–Q9 and all three `for P12.REVIEW` notes is routed;
**no entry is left unrouted.**

**C-6 — the walkthrough.** § Walkthrough, returned beside this verdict.

---

## Findings

None blocking. Numbered for the record:

1. **No blocking finding.** Every claim the phase makes is true in the runtime the operator uses.
2. **Four residuals are real, measured, and knowingly unfixed** — F4's 놓친 돈 revisit reflow, F10's
   two sample residuals, and S2's ① 환산 fill. All four are operator decisions in § Walkthrough, not
   defects: each is either identical on HEAD or strictly better than HEAD.
3. **The `## Operator Runtime` instrument paragraph is stale** (Chrome-over-CDP, "Aside's daemon does
   not run here"). Owed by **P4**, not by P12 — see § Deviations 2. Not a finding against this phase.
4. **`qa.md` § `## Regression Checklist` is now 67.7 kB** and `validate` names it first of eleven
   oversized sections. A docs-phase judgment call; noted in `phase.md` for that phase.

**S2's `9e-05` `layout-shift` on one of three `/stocks` production loads** (recorded, unexplained) I
could not reproduce in this review's own loads. Left recorded, not escalated: it is two orders of
magnitude below the threshold and has no identifiable source element.

---

## Stage D — routing

| entry | routed as | recommendation |
|---|---|---|
| **Q1** chrome pop-in remedy | **answered by the work** — `F1`/`F2` server-seeded the slot and the launcher, so nothing pops in and no skeleton/empty-slot decision is needed | confirm at the gate, one line |
| **Q2** mono swap remedy | **answered by the work** — `F9` took the metric-matched fallback route at **0 bytes on the wire**, `preload: false` intact, so the recorded decision stands | confirm at the gate, one line |
| **Q3** board re-rank under a reading reader | **walkthrough decision** | your call; I have no product opinion |
| **Q4** launcher hover `scale(1.35)` | **walkthrough**, one line — measured live **32 → 43.2 px**, composited, moves nothing else, the only geometry-changing hover in the product | keep as signed |
| **Q5** `scrollbar-gutter` | **deferred job** (unverifiable on this machine) | file it |
| **Q6** widget close leaves focus on `<body>` | **walkthrough decision** | your call |
| **Q7 + Q9** send-button resting width (`/ask` **+40.8 px**, 의견 보내기 **+53.47 px**) | **walkthrough, one decision for both** | answer both with one rule |
| **Q8** auth panel intro reservation **+20.922 px** at both viewports (reproduced on production) | **walkthrough decision** | your call |
| **note, `F4`** 놓친 돈 revisit reflow (+140.19 px, CLS 0.01232, identical on HEAD) | **deferred job** + one walkthrough line | file it |
| **note, `F10`** (a) ≤767 보유량 override re-wrap, identical on HEAD; (b) all-issuers-removed panel swap 33.5 → 106.44 px, HEAD worse | **walkthrough decision** | accept as is |
| **note, `S2`** ① 환산 fill 7–20 ms after FCP | **walkthrough line** — I reproduced it on production: a **fill inside an immobile reserved box**, no `layout-shift` entry at 1280, one sub-threshold `hadRecentInput` entry at 390 | accept as is |

**Deferred jobs for the orchestrator to file** (I never run `defer-job`):

1. **title:** `scrollbar-gutter: stable` cannot be verified on macOS overlay scrollbars ·
   **reason:** `lib/scrollLock.ts` sets `body { overflow: hidden }` with no gutter compensation and
   there is no `scrollbar-gutter` anywhere in the CSS; on Windows/Linux, or macOS "always show scroll
   bars", every sheet open would widen the page by the scrollbar width — the one-line fix is a no-op
   here, so it would ship unverified · **trigger:** a Windows or Linux reader, or a machine set to
   "always show scroll bars" · **source:** `P12.REVIEW` (Q5, from `P12.R1` F14)
2. **title:** 조회 revisit reflow on a 놓친 돈 stock (+140 px) · **reason:** with a remembered holding,
   the per-holding cells fill, the 계산 근거 band inserts and the mmhead prompt leaves, moving the
   breakdown 140.19 px with CLS 0.01232 — measured on production and **identical on HEAD**, so `F4`
   neither caused nor fixed it; it was left alone because its row heights depend on served text
   nobody has measured · **trigger:** the next phase that touches `/stocks/{corp_code}`, or a reader
   complaint about the page jumping on revisit · **source:** `P12.REVIEW` (from `P12.F4`)

---

## Walkthrough

**0. Before you start.**

1. Open **`https://jujutower.com`** in your own browser — that is the runtime this phase shipped to,
   at release `004d936`. Everything below is visible **anonymously**: do not sign in, do not send
   feedback, do not ask a question if you would rather not spend a model call. Nothing here deploys,
   and the freeze is **2026-09-07 11:00 KST**.
2. This phase removed **motion**, not pixels. The resting look of every surface is unchanged on
   purpose, so what you are checking is mostly an **absence**: things that used to jump no longer do.
   Load each page **cold** (hard reload) and watch the first second.

**1. The five fixes — look for what no longer happens.**

3. Reload **`/`** and watch the top-right corner. 「로그인」 and the **AI 질문** launcher are there in
   the very first paint; they no longer appear a beat late. Try `/stocks`, `/portfolio` and an event
   page — same on all ten public routes.
4. Open a stock you hold — **`/stocks/{종목}`** — enter a 보유량, then come back to the page. The
   ① 환산 row's cells fill **inside a box that was already the right size**: the numbers appear, but
    the 놓친 돈 block below them does not get pushed down any more.
5. Open any **`/events/{접수번호}`** and press **「정정 이력」**, then **「접기 ×」**, a few times. The
   button keeps **one width** — it used to shrink every time you opened it.
6. On **`/stocks`** type a company that does not exist, press Enter, then type one more character.
   The 「…과 일치하는 종목이 없습니다」 sentence disappears but the **space it occupied stays**, so nothing
   below jumps up.
7. **Slow connection check** (throttle to 3G in your browser's dev tools, or use a phone on a weak
   signal): load a stock page and watch the numerals. The monospace figures no longer re-flow when
   the font finishes loading.
8. **On your phone**, open **`/portfolio`** in sample mode, delete a row, and reload. The deleted row
   is simply never drawn — you no longer see it appear and then vanish.

**2. Four residuals I did not fix. Please accept or reject each.**

9. **DECISION A — 조회 revisit reflow.** Open a stock that shows **놓친 돈** rows, enter a 보유량, then
   revisit the page. The breakdown still slides up about **140 px** once. It is **exactly the same on
   the old build** — this phase neither caused nor cured it — and fixing it means measuring row
   heights that depend on served text. *My recommendation:* accept for now and let me file it as a
   deferred job (job 2 above). **Accept / fix now?**
10. **DECISION B — the ① 환산 row's two-stage draw.** On a slow connection the row's box appears
    first and its cells land 7–20 ms later. The box, the page height and everything below are
    **immobile** — it is a fill, not a flicker, and the browser records no layout shift for it.
    *My recommendation:* accept as is. **Accept?**
11. **DECISION C — two sample-mode residuals.** (a) On a phone, overriding a 보유량 can re-wrap one
    past ① row — identical on the old build. (b) Removing **every** issuer swaps the list for the
    「없습니다」 empty panel, which grows 33.5 → 106.44 px; the old build collapsed 1324 → 900 px in the
    same state, so this is **better** than before. *My recommendation:* accept both as is. **Accept?**

**3. Five open questions. One decision each.**

12. **DECISION D (Q7 + Q9 — one rule for both).** Press 보내기 on **`/ask`** and in **의견 보내기**: the
    button you just pressed changes label (답변 준비 중… / 보내는 중입니다) and therefore changes width,
    which slides the composer beside it. I can stop the motion, but only by making the **resting**
    button as wide as its widest label — **+40.8 px** on `/ask` and **+53.47 px** on 의견 보내기, at
    rest, in a signed dialog. **May a send button's resting box be as wide as its widest label?**
    Answer once and I will apply it to both.
13. **DECISION E (Q8 — the login panel).** Open **`/auth/login`** and switch to 계정 만들기. The panel
    grows **+20.9 px** because the intro wraps to a second line. I can reserve that line in both
    modes — after which nothing moves — at the cost of **one blank line of empty panel at rest**, at
    both viewports, on the page readers land on. **Reserve the line, or leave the panel as signed?**
14. **DECISION F (Q3 — the board re-ranking under you).** Leave **`/`** open for a minute or two. The
    board refreshes on its own and can re-rank rows and shift the freshness stamp while you are
    reading. **Keep it live, wait for a quiet moment, or require an explicit 새로고침?**
15. **DECISION G (Q6 — keyboard focus after the widget closes).** Open the AI 질문 widget and close it
    with its ×. Focus lands at the top of the tab cycle rather than back on the launcher.
    Pre-existing, never settled in the record. **Restore focus to the launcher, or leave it?**
16. **DECISION H (Q4 — the launcher's hover).** Hover the AI 질문 launcher: the sparkle grows
    **32 → 43.2 px**. It moves nothing else and it is the only hover in the product that changes
    geometry. *My recommendation:* keep as signed. **Keep or soften?**

**4. What I noticed with fresh eyes — not judged against the design record, and not fixed.**

17. **On your phone**, try to tick a 챙겼습니다 box on `/portfolio`. The target is **15 × 15 px** and it
    is genuinely fiddly. Pre-existing and untouched by this phase, but it is the same family as the
    still-open 「≤767 chip target is 14 × 16 px」 decision from P11. Worth deciding both together.
18. Open a **bad event address**, e.g. `/events/00000000000000`. The page paints empty for a beat
    before the Korean not-found copy arrives. Status is correctly 404; it just reads as broken first.
19. The send-button growth in item 12 is the thing I noticed as a *reader*, not as a ruler: the
    control you just pressed changes size while you are still looking at it.

**5. After you walk it.**

20. If everything above is acceptable: clear the gate — `python3 scripts/workflow.py accept-gate P12
    --clear --note "..."` — and tell me your answers to **A–H**; I will turn each 「fix now」 into a
    fix slice and file the rest.
21. If something is wrong: say what you saw and where, and it is recorded as
    `review-phase P12 --verdict changes_requested --note "operator-reported: ..."` and cut into fix
    slices — no need to soften it.
22. Two jobs I want filed either way: **`scrollbar-gutter` unverifiable on macOS** and the **조회
    revisit reflow** (full text in § Stage D).

---

## Deviations from `plan.md`

1. **35 of the 146 regression lines were not re-run as written**, against a plan that asked for the
   whole list. Each is named in § Not re-run with its reason (no Python/corpus change in this phase;
   an ops-box or credentialed action; the model-turn budget; a `next dev`-only precondition) and with
   what I checked instead. The four categories are honest limits, not skipped work: 9 backend/CLI
   lines with no trigger in a frontend-only phase, 12 P9 lines each needing its own model turn under
   a 1+6 cap I spent 3 of, 5 corpus-shaped lines whose named issuers I did not confirm still serve
   those shapes, and 9 ops/production-side lines that would have required a deploy, a credential or
   real mail under a freeze.
2. **No `operations` doc version was written**, though the plan left that to my judgment. The only
   change § `## Operator Runtime` needs — Aside on the agent profile, not Chrome over CDP, and its
   daemon *does* run here — is **already owed by `P4`** as an unconsolidated `## Doc impact` note
   (`works/phases/active/P4/phase.md`; it is in the `docs-debt` worklist). Writing it here would fork
   one section across two owing phases and give the docs phase a conflict to resolve. It is recorded
   in `phase.md` as a note for the docs phase, with P12's evidence attached, so the section gets paid
   once, from P4's note.
3. **One dev-runtime substitution.** The 390 feedback-dialog state machine could not be driven in
   `next dev`: the footer's 의견 보내기 centre is covered by the `NEXTJS-PORTAL` overlay (the
   documented P10 artefact). I ran that half on the **production build** (`:3014`), where it answers,
   and recorded the fact as an instrument note in the qa doc.

## Instrument, budgets, and machine state

- **Instrument used: Aside**, `aside repl --account u2` over Bash (profile 「claude2」). The operator
  profile `u0` was never used. No `aside mcp` registration was created. Where a synthetic click did
  not work (the feedback trigger, nav-sheet controls) I used real CDP `Input.dispatchMouseEvent`
  presses at measured coordinates after `window.scrollTo`.
- **Model calls spent: 3 of the 1 + 6 budget** — **1** `/api/ask` turn on production, **2** on dev.
- **Dev DB rows created and deleted:** one throwaway account (**id 48**), created **and deleted
  through the product's own UI**. The dev `accounts` table is back to its two pre-existing rows;
  I verified that read-only after the deletion. **Production: read-only throughout** — no account, no
  login, no feedback send, no write of any kind.
- **vocky rows created: 2** — two feedback sends through the **dev** API, which is where feedback
  forwards. None from production.
- **Machine state left as found.** The operator's dev stack is up exactly as I found it (api pid
  `60158` on `127.0.0.1:8010`, web pid `61423` → node `65758` on `:3010`). The review's own
  production-build server on **port 3014 (pid 23939) is stopped**; the build copy lives outside the
  repo in the session scratchpad and is disposable. `localStorage`/`sessionStorage` keys seeded on
  production (`mijual.portfolio.sample`, the lookup holdings) were removed after each check.
  `git status` shows **no source file modified** by this review.
