## Promoted Deferred Context

# Deferred: D15 Take the R7 implementation rules off the /ops door

## Context

## Why Deferred

Four internal rules render as visible 11px text under the login button in production, two describing the security posture (uniform + constant-time failure, credential rotation from env/secrets). R7 record text on a signed round — a copy decision, not a cleanup; not to be fixed silently inside D6.

## Trigger to Promote

Before P4 Ship & Submit, or whenever D6 is promoted

## Notes

