# Result — P11.REVIEW (phase review)

- **status:** done
- **summary:** P11 passes review. Both operator-reported defects are fixed and I verified them in the
  running product myself, in the manifest runtime and in a production build: a sentence resting on
  four 근거 renders on one line with the chips side by side, in all three signed placements, on `/ask`
  and in the widget, at 1280 and 390 — and the six start cards each fire their intended tool row with
  a real answer. All three `## Operator Questions` are routed into the acceptance walkthrough; the
  docs are consolidated into three versions; the gate is the orchestrator's to open.
- **files_changed:**
  - `docs/versions/frontend/v0012_p11_the_ask_becomes_a_conditionally_mounted_absolute_popover_and_the_two-part_rule_for_anything_placed_inside_prose.md` (new)
  - `docs/versions/experience/v0010_p11_the_ask_start_screen_offers_six_cards_one_per_agent_capability_the_seven-tool_list_and_the_never-calculates_line_are_corrected.md` (new)
  - `docs/versions/qa/v0013_p11_the_citation-chip_and_start-card_checklist_rows_are_re-cut_for_the_overlay_popover_and_the_six_cards_and_the_phase_s_own_checks_are_added.md` (new)
  - `docs/current/frontend.md`, `docs/current/experience.md`, `docs/current/qa.md`, `docs/index.json` (regenerated by `rebuild-docs`)
  - `works/phases/active/P11/phase.md` (compressed 177 → **122 lines / 11,086 bytes**)
  - `works/phases/active/P11/slices/P11.REVIEW/result.md`
- **validation:**
  | command | result |
  |---|---|
  | `.venv/bin/python -m pytest` | **pass — 154 passed**, 1 known deprecation warning |
  | `cd frontend && npm run typecheck` | **pass** (`tsc --noEmit`, no output) |
  | `cd frontend && npm run build` | **pass** — 19 route entries, `/ask` static |
  | `cd frontend && npm run smoke` | **pass — 22/22** |
  | `python3 scripts/workflow.py validate` | **pass** (before and after every edit) |
  | `python3 -m mijual.gates run` ×2 | **pass — byte-identical**, 1359 events judged, **710 field rows**, split unchanged (passed 618 / tbd 4 / failed 10 / n/a 78), exposable R1 50 · R2 422 · R3 16 = **488** |
  | exposure invariant (`gates.exposure.exposure_of_all`) | **pass** — 0 renderable fields outside `passed`/`tbd`, 0 `tbd` carrying a value, **418** renderable fields on exposable events (equals `gates run`) |
  | `python3 -m mijual.estimate report --today 20260831` ×2 | **pass — byte-identical**, ▷ 718.1억원 headline unchanged |
  | `python3 -m mijual.scheduler once --offline` | **pass — six stages** (collect · bodydoc · extract · gates · reparse · snapshot) at **0 requests / 0 calls / $0.0000** |
  | `python3 -m mijual.extract recheck` ×2 / `python3 -m mijual.evalset refresh-recall` ×2 | **pass** — recall 88.70 % both times, `rewritten 0`, 「sample: unchanged — nothing written」, working tree unchanged |
  | secret scan over every tracked file (3 secret-ish `.env` values) | **pass — 0 hits** |
  | real browser, **dev** (`make stack-up`, `http://127.0.0.1:3010`) | **pass** — §2, §3 |
  | real browser, **production** (`npm run build && npm run start`, same origin) | **pass — identical to dev on every measured number** — §4 |
- **instrument:** **not Aside.** `command -v aside` → nothing, no `/Applications` entry, no `aside mcp`
  tools in this session — the same finding both implementation slices recorded. The documented
  fallback applies: the same sweep, at the same viewports, in the same manifest runtime, driven
  through the real **Google Chrome** this machine has over the DevTools protocol from a throwaway node
  harness (no Playwright). Every number below was read out of that live DOM.
- **deviations:** none from `plan.md`. Two scope notes: the authenticated `/ops` checklist boxes were
  **not** re-run (they need operator credentials this session does not hold — §5), and the P10 round-3
  wordmark forensics were re-run as **asset statistics** rather than a re-derived splice (§5).
- **doc_impact:** n/a (this is the review; the phase's three notes are consolidated below).
- **doc_versions:** `frontend v0012` · `experience v0010` · `qa v0013` — then `rebuild-docs`. §6.
- **review_verdict:** **pass** — with the acceptance gate still to open (`acceptance.required: true`).
- **walkthrough:** returned to the orchestrator; reproduced in §7.
- **explain:** `not written — run /explain for this phase`.

---

## 1. What I judged, and against what

`intent.md` asked for two things and got both.

**Item 1 — citation chips break the prose.** The confirmed intent named the cause (an always-mounted
`display: grid` panel inside `<p class=prose>`'s inline formatting context) and the fix pattern
(`Citation.tsx`'s R10 conditional-mount absolute popover), and required the fix to hold across three
signed placements, two surfaces, two viewports, while keeping everything R16 signed about the chip.
`P11.S1` did exactly that, and I re-measured it myself rather than reading its report (§2).
Server-side numbering was not touched — `agent/citations.py` and `agent/events.py` were never opened,
and the live answers still number stably per answer.

**Item 2 — the start cards showed one capability of seven.** `P11.S2` replaced the four read-a-filing
questions with six, one per capability, keeping R16's layout and the 「card sentence is the question
sent」 rule, with the count freed by the operator's own instruction. I pressed all six myself (§3).

**Cross-check of `phase.md` against every `result.md`:** no decision recorded in a slice's `result.md`
is missing from `## Decisions` — S1's four deviations are all there (opaque ground, the clip-box
`fit()` + upward flip, the three-close model without `×`, and the 44px chip target raised as a
question rather than invented), and S2's substantive findings are all there (the six sentences and why
each routes, the corrected R3 caveat, the deliberate `SAMPLE_HOLDINGS` avoidance, the bare-접수번호
shape). S2's one deviation — a **comment-only** third file, `AskPage.tsx` L33, which still said 「four
question cards」 — is a log-level detail and is in its `result.md` §6; I verified the edit changes no
code. **No `## Operator Questions` entry was left unrouted** (§7).

## 2. The headline defect, re-measured by me — `/ask`, 1280×800, dev

Pressed card 1 (「HLB 전환사채 공시가 몇 건이나 있나요?」), a live agent turn, no interception:

- **the sentence rests on four 근거 and renders on one line**: chips `1 2 3 4` at
  `x = 530.1 / 547.1 / 564.1 / 581.1`, **all at `y = 249`**, 17px apart; the whole answer's `.prose`
  is **21px high at 20.925px leading — one line** — and carries **0 `<br>`**. The second sentence
  continues on the same line after the chips. This is the operator's `…입니다.[1] ⏎ [2] ⏎ [3]` closed.
- **opening a chip moves nothing**: a document-coordinate snapshot of every `.prose`, `.sentence`,
  `.row`, `.rowLabel`, `.rowValue`, `.answer`, `.data`, `.calc` and chip is **byte-identical before
  and after the click** (`moved: []`), on all three placements.
- **the popover is the R10 anatomy, opaque**: `background-color: rgb(14, 26, 21)`,
  `border-left: 2px rgb(95, 208, 165)`, `border-top: 1px rgba(163, 196, 180, .32)`, `z-index: 40`,
  quote `max-height: 180px`, one popover at a time.
- **the closed popover is unreachable by construction**: `a[href*="dart"]` = 3 with everything shut,
  **4** while one is open, back to 3 after 칩 재탭. That is what `inert` used to buy.
- **all three closes work**: 칩 재탭 (`pops 0`, `aria-expanded` back to false), a press outside
  (`1 → 0`), and **Esc with focus returned to the chip** (`document.activeElement === the chip`);
  Enter on a focused chip opens it.

**The three signed placements, on card 3's answer** (에코프로비엠 ①, which renders a 데이터 블록 *and*
a 계산 블록 in one turn):

| placement | anchor | popover box | quote cap | moved |
|---|---|---|---|---|
| 데이터 행 값 | `.row` (`Blocks-module…row`) | `[274, 355, **732 × 76**]` — flush under the row, block-wide | `180px` | `[]` |
| 계산 입력 | `.row` | `[274, 473.6, **732 × 47**]` | API-tier (link only) | `[]` |
| 프로즈 | `.citationWrap` | `[694.6, 590.5, **380 × 47**]` | API-tier | `[]` |

and the data row's grid tracks stay `283.188px 391.812px 17px` with the popover open — **the value
column cannot collapse**, because an absolutely positioned box sizes no grid track.

**390×844 with touch emulation:** the four chips stay on one line (`x = 286.1 … 337.1`, all `y = 261`),
each **14 × 16 px** (the open question, §7 decision 2); popovers are **340px**, clamped fully inside
the viewport by `fit()` (`translateX(48px)` on a chip that would have started off-screen); no
horizontal overflow (`documentElement.scrollWidth === innerWidth === 390`).

**Widget, 1280:** widget `440 × 620`, thread `438 × 509` (`overflow-y: auto`); opening it shifts
`<main>` by **0px** (byte-identical rect). Chips `1 2 3 4` inline at one `y`; popovers stay **inside
the thread and inside the widget** (clamped by `nearestClip`), and the thread's `scrollHeight` is
**509 before and after every open** — the composer is not pushed. **≤767 has no widget and no
launcher** (verified at 390 and at 767; the launcher appears at **768**), which is R14's own rule.

## 3. The six cards, pressed one at a time by me (dev, 1280)

| # | card | 도구 행 | answer |
|---|---|---|---|
| 1 | HLB 전환사채… | `이벤트 검색 「HLB」 → 4건 · ② 전환사채 ×4` | 「총 4건이 조회됩니다.」 + 4 chips inline |
| 2 | 접수번호 20260730000215… | `이벤트 읽기 → 로젠 · ③ 주식매수청구권` — **and only that one, no 검색 row** | 3 sentences, 「매수예정가격은 2,116원입니다.」 |
| 3 | 에코프로비엠 유상증자… | `이벤트 검색` → `이벤트 읽기` → **`계산 → 배정 신주 · 1,000주 × 0.0910905009 = 91주`** | 배정 신주 91주, one 「입력」 marker beside one cited chip |
| 4 | 내 포트폴리오… | `내 포트폴리오 읽기 → 샘플 포트폴리오 · 4종목 (구성 예시)` | 「현재 포트폴리오는 구성 예시 기준입니다.」 + 대동기어 D-54 |
| 5 | 공시를 원문으로… | `의견 저장 → 운영자 검토 대기열` | 감사 2문장, 근거 0건 — **and a real row landed** |
| 6 | 운영자에게 직접 연락… | `운영자 연락처 → 미정` | 「현재 등록된 운영자 연락처가 없습니다.」 — **nothing invented** |

Six distinct tool rows, six real answers, **no 「미확인」** on any of them (the whole page body was
searched, not just the sentence spans). Start-screen geometry, measured live: six cards, `316px
316px` grid with an 8px gap, **three even rows with no orphan** (y = 333 / 404.2 / 475.3), all
`316 × 63.2`, `min-height: 56px`, nothing clipped; at 390 one column of `358px` (the 의견 card two
lines at 63.2px), no horizontal overflow. `새 대화` is absent on the empty state, as R6 requires.

**Card 5 wrote `conversation_feedback` id 7** (the table was at 6 — ids 4–6 from `P11.S2`'s
verification). That is the capability working, and it is walkthrough decision 3.

## 4. Production

`npm run build` → `npm run start` on the same origin, then the same scripts. **Every measured number
is identical to dev**: the four-chip line at `530.1/547.1/564.1/581.1 @ y 249`; the row popover at
`[274, 355, 732 × 76]` with `180px` cap; the calc chain 검색 → 이벤트 읽기 → 계산 with the same
`1,000주 × 0.0910905009 = 91주`; the 390 chips at `y 261` and the 340px clamped popover; the widget's
`440 × 620` / thread `438 × 509` with `scrollHeight` unchanged on open; the card grid's three even
rows. No console errors and no exceptions in either runtime, on first load or after the reload that
re-renders the chips from the restored thread.

## 5. The cumulative `## Regression Checklist`, re-run

Beyond the deterministic commands in the verdict block, the browser blocks were re-run in the
production build across **8 reader routes** (`/`, `/ask`, `/stocks`, `/stocks/00102618`, `/portfolio`,
`/auth/login`, an event detail, and a 404) at 1280, plus 390 / 767 / 768 passes:

- **brand mark** — every route carries exactly one nav `img` at **90.75 × 27** and one footer at
  **80.66 × 24**, natural **1247 × 371**, `complete`, `transform: translateY(-8px)` / `-6px`,
  `alt="주주의관제탑"` — **including the 404**; the asset itself still measures trim **1247×371**,
  **78,212** non-transparent / **69,630** fully opaque / **154** distinct alphas (the P10 round-3
  numbers, exactly) and is unchanged in git since P10;
- **titles** — every reader page `주주의관제탑`, the `/ops` door `주주의관제탑 운영`, `/openapi.json`
  and `/docs` `주주의관제탑 API`;
- **retired spellings** — 미주알 / 미주얼 / MIJUAL / Mijual appear in **no** reader page's `innerText`;
  `/assets/mijual-*.png` **404**; a repo grep finds the old names only in prose and comments;
- **favicon** — `link[rel*=icon]` = **3** on every reader page and `/ops`, `/icon.png` `/icon1.png`
  `/apple-icon.png` 200 and `favicon.ico` **404**; all three tiles `opaque=false`, trims `24x18+4+7`
  and `134x100+23+40`, and **0** visible pixels that are not `(43,142,108)`;
- **nav width reservation** — `header nav a` lefts are identical **to the decimal**
  (`[218.75, 279.484375, …]`) on all eight routes;
- **fonts / third-party** — `document.fonts` holds `notoSansKr` + `plexMono` and **no Pretendard**,
  exactly **one** `link[rel=preload][as=font]`, and **zero** foreign origins were requested on any
  route in either runtime;
- **widths** — `/stocks` main **620px** and `/stocks/{corp_code}` **960px** in the production build;
- **motion** — under `prefers-reduced-motion: reduce` the whole document reports **0** animations on
  `/` and `/ask`; without it the ambient set is still there (`drift`, `twinkle`, `shoot`, `orbit`);
- **mono line breaks** — 0 splits, measured the P10 round-4 way (comparing the rects' `top`, never
  `getClientRects().length`);
- **AI 질문 blocks** — one `<p>` per answer with inline sentences and **0 `<br>`**; 근거 N건 equals the
  distinct chip count (4건 / 4 chips); the completed footer is 근거 · 접수번호 · KST + DART 원문 ↗ ·
  이벤트 상세 · 내 종목 조회 with no 「다시 질문」; the `/ask` 빈 상태 has 익명 줄 0, 새 대화 0, 「범위:」
  칩 0; `rg "480|481"` over `frontend/components/ask` + `lib/ask.ts` returns **nothing**; no quota or
  storage-denial copy and no `localStorage.` call in the ask surfaces.

**Two boxes I did not re-run, and why** — stated rather than glossed:

1. **The authenticated `/ops` boxes** (the six-tab strip's rects, 대화 로그, `/ops/feedback`, the AX
   tab names) need operator credentials this session does not hold. The `/ops` **door** was checked
   (title, 3 icon links, no reader-chrome link to it). The operator sees `/ops/feedback` in the
   walkthrough anyway, as decision 3.
2. **The P10 round-3 splice forensics** (the alpha-channel hash against a fresh `+append`, the 25-column
   「의」|「관」 ink gap, the two counter islands) were re-run only as the **three asset statistics**
   above plus the trim box. The binary is byte-unchanged since P10 and P11 touched no asset; the
   statistics have teeth enough to catch a swapped file.

## 6. Doc consolidation (pass path)

Three versions, one per durable-truth area in `phase.md`'s `## Doc impact`, then `rebuild-docs`:

- **`frontend v0012`** — the chip's anatomy in full (conditional mount + absolute popover, what it
  replaced and *why a block box in an inline formatting context breaks the paragraph*, the opaque
  ground as the one deliberate R16 deviation with its measured values, the three-close model without
  `×`, and `fit()`'s clip-box clamp + upward flip). It also carries the **pattern the orchestrator
  asked for**: this surface has produced 「a line per sentence」 **twice by two different causes** —
  R14's leading whitespace in the streamed text and P11's block-level box — so **everything inside
  `<p class=prose>` must be phrasing content *and* inline-level**, a two-part rule each half of which
  has now failed once on its own.
- **`experience v0010`** — the six-card set, one card per capability, with why each sentence routes
  where it does and which one has a side effect; **plus the section's two stale P9 lines**: the tool
  list was five where the agent has **seven**, and 「the agent never calculates」 was superseded by
  R16's auditable calculator (struck through and corrected in place, not deleted).
- **`qa v0013`** — L384 and L410 re-cut for the overlay popover (and for `inert`'s retirement: the
  check is now the DART-anchor count rising and falling, because a closed popover is not in the DOM),
  L403 「4장」 → **6장** with the capability claim and the 의견 card's side effect written into it, and
  a new **P11 block** carrying this phase's four headline checks with the numbers above and the
  negative controls that give them teeth.

## 7. The walkthrough returned to the orchestrator, and the routed questions

Reproduced here because the verdict block is ephemeral. Runtime: `make stack-up`, then
**`http://127.0.0.1:3010`** in Chrome (and the tailnet URL from `make stack-status` if the operator
wants a real phone). The stack was left **running in dev**, as the manifest describes.

**A — the defect that started the phase (2 minutes).** Open `/ask`, press **「HLB 전환사채 공시가 몇
건이나 있나요?」**. Watch the answer land: the sentence ends with a period and `1 2 3 4` sit **side by
side after it, on the same line** — no line break before or after any chip. Tap a chip: the quote
panel **floats over** the text; nothing on the page moves. Tap it again (or press Esc, or click
away) to close it. Repeat at a phone width if convenient.

**B — the six cards (5 minutes).** Press 「새 대화」, then the cards **one at a time**, top-left to
bottom-right. Each should show a different 도구 행: 이벤트 검색 · 이벤트 읽기 · **계산** · 내
포트폴리오 읽기 · 의견 저장 · 운영자 연락처. Two to look at closely: card 3 draws a 검증된 계산 block
whose 보유 주식수 is marked 「입력」 (the reader's own number) while the 배정비율 carries a 근거 chip;
card 2's answer is a good second look at chips after a period.

**C — the row and calc placements (1 minute).** In card 3's answer, tap the chip in the 공시에서 읽은
값 row and the one in the 계산 block: the quote opens **under the row, across the block**, and the
value column does not move.

**D — the widget (1 minute).** Go to the landing page, open the sparkle launcher bottom-right, ask
the HLB question there, and tap a chip: the quote must stay **inside the widget**.

**Say plainly what is real and what is not:** every answer above comes from the live agent reading
real DART filings — nothing here is stubbed. The one card with a **side effect** is 「공시를 원문으로
확인할 수 있어 좋았어요…」: pressing it writes a real row to the 운영자 검토 대기열 (`/ops/feedback`)
every time. That is the capability, not a bug.

**Decisions the operator is being asked to take:**

1. **The quote panel's background changed** — this is the phase's one deliberate deviation from what
   R16 signed. The round drew the 인용 블록 on the translucent `--surface-inset` as an *in-flow* panel;
   as an overlay that let the sentence read straight through the reader's evidence, so it now uses the
   product's own overlay ground (`#0e1a15` + 1px border + glow). The 2px green edge and the 180px
   quote cap are unchanged, and no new colour was introduced. **Accept, or ask for it back.**
2. **The 연락처 card answers 「미정」** because `MIJUAL_OPERATOR_CONTACT` is unset. Configure a contact
   string before P4 — and which one? Publishing it is the operator's identity decision. *(Routed from
   `## Operator Questions`, P11.DECOMP.)*
3. **The ≤767 인용 칩 target is 14 × 16 px, not the 44px R16 §2.6 lists.** It never was: the round's own
   CSS never implemented it either, so the record's prose and its code disagree and the product follows
   the code. Closing it means either a 44px box drawn around a 10px number, or an invisible hit
   expander that makes each chip's target swallow its 3px-away neighbour on the very multi-chip
   sentence this phase fixed. **Close it (at which cost), or leave it as the round's CSS has it?**
   *(Routed from `## Operator Questions`, P11.S1.)*
4. **Seven rows now sit in `/ops/feedback`** — ids 4–6 from P11.S2's verification and **id 7 from this
   review's own walk**, all carrying the card sentence verbatim; your gate walk will add one more if
   you press card 5. **Clear ids 4–7 before the P4 demo, or keep them as evidence?** Deleting rows
   from the operator's own review queue is theirs to decide. *(Routed from `## Operator Questions`,
   P11.S2.)*

**Fresh-eyes findings — reported, not fixed, and explicitly not judged against the design record:**

- **The 의견 card looks exactly like the five questions but is not one.** Six identical boxes, and the
  fifth is a statement written in the reader's voice that gets *sent as if they wrote it* and lands in
  the review queue. Reading the start screen cold, nothing marks it as different in kind. It is
  defensible (first-person is what carries consent) but it is worth the operator's eye.
- **Card 2 opens with a bare 14-digit 접수번호**, which reads as machinery on a first screen where
  everything else reads as a sentence. It is deliberate — a bare number is the only shape that isolates
  `get_event` — and it is also a real reader's move (paste the number from DART).
- **At 390, a sentence's chips can land at the head of the next line**, ahead of the following
  sentence's text; and when a line wraps exactly at a sentence boundary, the signed `.25em`
  inter-sentence gap shows up as a small indent at the start of the wrapped line. Both are consequences
  of the chips being properly inline again; binding a chip to its sentence's last word would change
  `Answer.tsx`'s sentence structure, which is a typographic decision R16 does not make.
- **An API-tier chip's popover is a 380 × 47 panel holding one link** (「DART 원문 … ↗」) and, on a short
  answer, it lands over the 근거 footer line beneath it. It is legible and R14 signed the link-alone
  content; it just reads emptier than a quoted chip's.
- **The footer's 「AI 질문」 link measures 40 × 44 at 390** — the only interactive target under 44px I
  found on `/ask`. Pre-existing, not P11's, and listed as a deferred-job candidate below.

**Deferred jobs for the orchestrator to file** (`defer-job`; I do not file them):

1. **title:** 시작 카드의 회사가 늙는다 — a data-derived start-card set.
   **reason:** the six cards name live corpus companies (HLB · 로젠 · 에코프로비엠), and three of the
   *previous* four had already gone dead as their filings aged; `phase.md` Decision Q1 chose hard-coded
   strings deliberately and named this the follow-up.
   **trigger:** when a start card's question can no longer be answered from the corpus, or before the
   next demo after 2026-09-07.
2. **title:** 의견 카드가 다른 카드와 구별되지 않는다 — mark the one card with a side effect.
   **reason:** the fresh-eyes finding above; the reader cannot tell from the start screen that pressing
   it files a row in their name.
   **trigger:** any future work on the `/ask` start screen (it is a copy/affordance decision, not a bug).
3. **title:** 푸터 「AI 질문」 링크가 390에서 40 × 44 — sub-44px target in the footer.
   **reason:** measured at 390 on `/ask`; pre-existing since P8 and unrelated to P11.
   **trigger:** the next mobile-target pass, or any footer work.

## 8. Scope

No source file was touched by this review — the only edits are the three doc versions, the three
regenerated `docs/current` files, `docs/index.json`, `phase.md` and this file. No workflow
state-transition command was run (`review-phase`, `accept-gate`, `finish-slice`, `set-*-status` are the
orchestrator's), nothing was committed, and `defer-job` was not run — the three jobs are listed above
for the orchestrator to file. The CDP harness and its screenshots live in the session scratchpad,
outside the repo. The dev stack was restarted around the production run and is **back up** in the
manifest's own configuration (`make stack-status`: api pid 5283, web pid 21260, `http://127.0.0.1:3010`
→ 200).
