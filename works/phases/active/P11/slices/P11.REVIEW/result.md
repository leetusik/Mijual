# Result — P11.REVIEW (phase review — SECOND PASS, after the operator's gate rejection)

- **status:** `done`
- **summary:** P11 passes on the re-review. All three of the operator's gate reports are answered by
  `P11.F1` + `P11.F2`, and I re-verified every one of them **myself** in the manifest runtime **and**
  in a production build rather than on the fix slices' reports: a sentence resting on five 근거 renders
  on one line with its chips side by side (all three placements, page and widget, 1280 and 390), the
  start screen is four cards whose two 공시 companies are picked **per request** (proved in the
  production build — one running server, three requests, three different card sets), and the operator's
  contact answers in the agent and closes the footer's identity row on every reader page. The docs are
  consolidated into **six** new versions superseding the first pass's three; the gate is the
  orchestrator's to open again.
- **files_changed:**
  - `docs/versions/frontend/v0013_p11_fixes_the_ask_route_becomes_the_start_screen_s_data_boundary_with_a_per-card_static_fallback_and_the_global_footer_publishes_served_config_read_by_the_root_layout.md` (new)
  - `docs/versions/experience/v0011_p11_fixes_the_ask_start_screen_offers_four_cards_whose_two_companies_the_server_picks_from_the_live_corpus_per_request_and_the_card_answers_the_operator_s_configured_contact.md` (new)
  - `docs/versions/qa/v0014_p11_fixes_the_start-card_checklist_rows_are_re-cut_to_four_live-corpus_cards_the_footer_row_admits_the_operator_s_mono_phone_and_the_p11_block_gains_the_contact_fallback_and_dynamic-route_checks.md` (new)
  - `docs/versions/operations/v0013_p11.f2_mijual_operator_contact_is_set_and_must_travel_to_wherever_the_api_runs_it_now_feeds_the_global_footer_as_well_as_the_agent_s_answer_and_.env_carries_it_in_no_commit.md` (new)
  - `docs/versions/backend/v0008_p11_fixes_two_new_read_surfaces_get_ask_start-cards_picks_the_start_screen_s_companies_from_the_live_corpus_per_request_and_get_site_contact_serves_the_operator_s_one_contact_string_split_into_its_parts.md` (new)
  - `docs/versions/security/v0009_p11.f2_the_operator_contact_is_now_genuinely_published_set_answered_by_the_agent_and_printed_in_every_reader_page_s_footer_so_it_is_public_by_decision_and_appears_in_tracked_files_too.md` (new)
  - `docs/current/{frontend,experience,qa,operations,backend,security}.md` + `docs/index.json` (regenerated by `rebuild-docs`)
  - `works/phases/active/P11/phase.md` (compressed and rewritten — **186 lines / 16,250 B** of 200 / 16,384)
  - `works/phases/active/P11/slices/P11.REVIEW/result.md` (this file, rewritten from scratch for this pass)
- **validation:**
  | command | result |
  |---|---|
  | `.venv/bin/python -m pytest` | **pass — 158 passed**, 1 known deprecation warning |
  | `cd frontend && npm run typecheck` | **pass** (`tsc --noEmit`, no output) |
  | `cd frontend && npm run build` | **pass** — 19 route entries, **`ƒ /ask` (Dynamic)** |
  | `cd frontend && npm run smoke` | **pass — 22/22** |
  | `python3 scripts/workflow.py validate` | **pass** (before and after every edit) |
  | `python3 -m mijual.gates run` ×2 | **pass — byte-identical**; 1359 events judged, **710 field rows**, passed 618 / tbd 4 / failed 10 / n/a 78; exposable R1 50 · R2 422 · R3 16 = **488** |
  | exposure invariant re-derived (`gates.exposure.exposure_of_all`) | **pass** — 0 renderable fields outside `passed`/`tbd`, 0 `tbd` carrying a value; 414 value + 4 「추후결정」 = **418** renderable on exposable events (equals `scheduler`'s own line) |
  | `python3 -m mijual.estimate report --today 20260831` ×2 | **pass — byte-identical**, ▷ 718.1억원 unchanged |
  | `python3 -m mijual.scheduler once --offline` | **pass — six stages** at 0 requests / 0 calls / **$0.0000** |
  | `python3 -m mijual.extract recheck` ×2 · `python3 -m mijual.evalset refresh-recall` ×2 | **pass** — byte-identical, `rewritten 0`, 재현율 **88.70 %**, 「sample: unchanged — nothing written」 |
  | secret scan over all **1128** tracked files | **pass for the three keys — 0 hits**; the *published* contact string is a deliberate exception, see §6 |
  | real browser, **dev** (`make stack-up`, `http://127.0.0.1:3010`) | **pass** — §2–§4 |
  | real browser, **production** (`npm run build && npm run start`, same origin) | **pass — every measured number identical to dev** — §2–§5 |
- **instrument:** **not Aside.** `command -v aside` → nothing, no `/Applications` entry, no `aside mcp`
  tools in this session — the same finding all four earlier slices recorded. The documented fallback
  applies: the same sweep, at the same viewports, in the same manifest runtime, driven through the real
  **Google Chrome 152.0.7977.65** this machine has, over the DevTools protocol from a throwaway node
  harness (no Playwright). Every number below was read out of that live DOM or off the wire.
- **deviations:** none from `plan.md`. Two scope notes, stated rather than glossed: the authenticated
  `/ops` checklist boxes were again **not** re-run (they need operator credentials this session does not
  hold — §7), and the P10 round-3 wordmark forensics were re-run as **asset statistics + the trim box**
  rather than a re-derived splice hash (the binary is byte-unchanged since P10 and P11 touched no asset).
- **doc_impact:** n/a (this is the review; the phase's notes are consolidated below).
- **doc_versions:** `frontend v0013` · `experience v0011` · `qa v0014` · `operations v0013` ·
  `backend v0008` · `security v0009` — then `rebuild-docs`. §6.
- **review_verdict:** **pass** — with `acceptance.required: true` **reset**, so the gate must be opened
  again before `review-phase --verdict pass` is recordable.
- **walkthrough:** returned to the orchestrator; reproduced in §8.
- **explain:** `not written — run /explain for this phase`.

---

## 1. What I judged, and against what

This is a **re-review**. The first pass passed on the merits; the operator then walked the gate and
rejected it with three reports (`phase.json` `review.note`). Two `fix` slices landed. I judged the
phase **as it now stands** — against `intent.md`'s two items *and* against the three gate reports.

| the operator asked for | landed in | I verified |
|---|---|---|
| drop the 접수번호 and 의견 cards — four remain | `P11.F1` | §3 — four cards, and `conversation_feedback` gained **no** row from my whole sweep |
| the company-bearing cards must be caught in real time | `P11.F1` | §4 — three requests to one **production** server, three different card sets |
| set the contact and surface it in the footer on every page | `P11.F2` | §5 — agent answer + five routes, dev and production, 1280 and 390 |

`intent.md`'s item 1 (chips break the prose) and item 2 (one capability of seven) are both still met,
and item 1 is the thing that most needed re-checking under F1/F2 — the cards changed underneath it.
It has not regressed (§2). Server-side numbering was again not touched.

**Cross-check of `phase.md` against all five `result.md` files.** No decision recorded in a slice's log
is missing from `## Decisions`: S1's four deviations (opaque ground, the clip-box `fit()` + upward flip,
the three-close model without `×`, the 44px target raised as a question rather than invented), F1's card
set, picker rules and data-boundary/fallback contract, and F2's override of R8 §4 plus the one-string
split and the cache asymmetry are all there. The supersessions were done **in place** (F1's card
decision replaces S2's; DECOMP's 「hard-coded strings」 answer is gone because F1 superseded it), which is
what the notebook rule asks for. **No `## Operator Questions` entry is unrouted** (§8, decisions 2 and 3;
the contact question is closed by F2 and re-verified).

## 2. The headline defect, re-measured by me

**Dev, `/ask`, 1280×800@2** — pressed card 1 (`{served 회사} 전환사채 공시가 몇 건이나 있나요?`), a live
agent turn, no interception. The sentence rests on **five** 근거:

- chips `1 2 3 4 5` at `x = 594.8 / 611.8 / 628.8 / 645.8 / 662.8`, **all at `y = 249`**, 17px apart,
  each **14 × 16 px**; `<p class=prose>` carries **0 `<br>`**. In the **production** build the same
  answer put both sentences on **one** line (`.prose` 21px at 20.925px leading) with the five chips
  inline after the first period — the operator's `…입니다.[1] ⏎ [2] ⏎ [3]` is closed.
- **opening a chip moves nothing**: the document-coordinate snapshot of every `.prose`, `.sentence`,
  `.row`, `.rowLabel`, `.rowValue`, `.answer`, `.data`, `.calc` and chip is **byte-identical before and
  after the click** (`moved: 0`) in every placement, dev and production.
- **conditional mount**: `a[href*="dart"]` = **3** closed → **4** open → **3** after 칩 재탭. That is
  what `inert` used to buy, now bought by construction.
- **the three closes** all work: 칩 재탭 (`pops 0`, `aria-expanded=false`), a press outside (`1 → 0`),
  and **Esc with focus returned to the chip** (`document.activeElement` is the chip, text `2`).
- **ground**: `rgb(14, 26, 21)`, `border-left: 2px rgb(95, 208, 165)`, `border-top: 1px rgba(163, 196,
  180, .32)`, `z-index: 40`, quote `max-height: 180px`. The phase's one signed-surface deviation, still
  as recorded.

**All three signed placements**, on card 2's answer (which draws a 데이터 블록 *and* a 계산 블록 in one
turn) — identical in dev and production:

| placement | anchor | popover box | quote cap | moved |
|---|---|---|---|---|
| 데이터 행 값 | `.row` | `[274, 355, **732 × 76**]` — flush under the row, block-wide | `180px` | 0 |
| 계산 입력 | `.row` | `[274, 473.6, **732 × 47**]` | API-tier (link only) | 0 |
| 프로즈 | `.citationWrap` | `[662.2, 590.5, **380 × 47**]` (prod `[596.3, …]`, x follows the sentence) | API-tier | 0 |

and the data row's grid tracks stay `283.188px 391.812px 17px` with the popover open — **the value
column cannot collapse**.

**390×844@3, touch, production:** the four cards are one 358px column; the sentence's chips run
`[309.8, 326.8, 343.8] @ y 261` then `[32, 49] @ y 282` — a **wrap between chips**, which is ordinary
inline behaviour and not the defect (the defect was a break after *every* chip); `.prose` is 3 lines
with **0 `<br>`**. The popover is **340px**, `translateX(24px)`, fully inside the viewport; nothing
moved; no horizontal overflow (`scrollWidth === innerWidth === 390`).

**Widget, production, landing page:** widget `440 × 620`, thread `438 × 509` (`overflow-y: auto`);
opening it shifts `<main>` by **0px**; the same question answers there with chips inline; the popover
lands `[863, 527, 380 × 47]`, **inside the thread**, and the thread's `scrollHeight` is **509 before and
after** the open — the composer is not pushed.

## 3. Four cards, and what each one does — pressed by me, one at a time

Dev and production both, each press a fresh load, `/ask` at 1280 (grid: `316px 316px`, gap 8, boxes
`316×63.2 / 316×63.2 / 316×56 / 316×56`, **two even rows, no orphan**, nothing clipped):

| # | card | 도구 행 | answer |
|---|---|---|---|
| 1 | `{served} 전환사채 공시가 몇 건이나 있나요?` | `이벤트 검색 「빛과전자」 → 5건 · ② 전환사채 ×5` | 「…전환사채 공시는 총 5건입니다.」 + 5 chips inline |
| 2 | `{served} 유상증자, 1,000주 보유 시 배정 신주는 몇 주인가요?` | `이벤트 검색` → **`이벤트 읽기`** → `계산 → 배정 신주 · 1,000주 × 0.507594018주 = 507주` | 배정비율 cited `[2]`, 「배정 신주는 507주입니다」 |
| 3 | 내 포트폴리오에서 가장 급한 일정은…? | `내 포트폴리오 읽기 → 샘플 포트폴리오 · 4종목 (구성 예시)` | 「…**구성 예시** 기준으로 대동기어… 2026-10-24, D-54」 |
| 4 | 운영자에게 직접 연락하려면…? | `운영자 연락처 → 이메일 … · 전화 …` | 「운영자 연락처는 이메일 …, 전화 …입니다.」 |

- **`get_event` is demonstrated inside card 2's chain**, exactly as the operator's report intended — the
  three-row 검색 → 이벤트 읽기 → 계산 trace is visible on the turn.
- **The calc block shows exactly one 「입력」 marker beside exactly one cited chip**: `보유 주식수 1,000주
  [입력] | 1주당 신주배정비율 0.507594018 [2] | 1,000주 × 0.507594018 = 507주 | 결과 507주`.
- **No 「미확인」** anywhere in any of the four page bodies.
- **Nothing was written to the operator's queue.** `conversation_feedback` holds **8** rows; the newest
  (id 8) is stamped **12:17 UTC = 21:17 KST**, i.e. the operator's own gate walk, hours before this
  session. My four-card sweep in two runtimes added **zero**. That is the 의견 card's removal, measured.

## 4. The cards are caught in real time — proved in the production build

This is what the operator actually rejected, so I proved it where it bites rather than reading F1's
report:

1. **`next build`'s route table prints `ƒ /ask`** (was `○ Static`). A static render is the original
   defect returning, and the route kind is the tell.
2. **One running production server, three consecutive requests, three different card sets.** I ran a
   second production server against a stub upstream that alternates companies between requests:
   request 1 rendered 「가나전자 … / 다라화학 …」, request 2 「마바산업 … / 사아소재 …」, request 3 back to
   「가나전자 …」. No restart, no rebuild. The page is resolved **per request**.
3. **The live pick is the corpus's, not a constant.** `GET /ask/start-cards` answers
   `빛과전자 (5건)` / `아이에이 (20260818000250, D-45)` today, and both cards on the running page name
   exactly those. *(Not asserted as a permanent fact — it is data, and the walkthrough deliberately does
   not name them.)*
4. **The fallback draws four cards.** With the API killed, `/ask` returned **200 in 25 ms** and rendered
   four cards in two rows with **no spinner and no hole**; the same stub run with `contact: null` proved
   the footer's absent case in production (no `mailto:`, no `tel:`, no empty label — just the 자료 row).

## 5. The contact, in both places

- **Agent:** the 연락처 card answers 「운영자 연락처는 이메일 leetusik@gmail.com, 전화 010-3772-9916
  입니다.」 with the 도구 행 `운영자 연락처 → 이메일 … · 전화 …`, 근거 0건 — dev and production. The
  honest-unset line appears nowhere.
- **Footer, dev and production, five routes** (`/` `/ask` `/stocks` `/portfolio` `/events/…`):
  「자료: 금융감독원 DART 전자공시 · © 주주의관제탑 · leetusik@gmail.com · 010-3772-9916」. **`/ops`
  renders no footer** — but the two values *are* in that route's flight data (the root layout hands
  them to the client chrome on every route); nothing renders or links them there.
- **Type split measured:** email `notoSansKr` 12px `mailto:`, phone `plexMono` 11px `tel:` — R8 §4's
  override, as recorded. At **390** both links are **44 px** tall and hit-test to themselves; the footer
  「AI 질문」 link still measures **40 × 44** (D30, untouched, neither fixed nor worsened).
- **The footer's height, which F2 asked the review to look at.** Measured across 12 widths on `/ask`:
  **73px** at ≥~840, **118px** at 768–820, **132px** at 481–767, 207–231px at ≤480. `.inner` wraps its
  action row earlier than before because the identity line grew ~215px. I looked at it at 820 and 600:
  nothing overlaps, nothing overflows at any width, 「의견 보내기」 answers `elementFromPoint` at every
  width, and the two-row footer reads as a deliberate stacked footer rather than a broken one.
  **My judgment: acceptable** — but it is a visible change to a signed surface, so it is walkthrough
  item **E**, not a silent pass.

## 6. Doc consolidation (pass path) — six versions, three of them superseding the first pass

The first pass's `frontend v0012` / `experience v0010` / `qa v0013` were partly **wrong** after F1/F2
(most obviously the six-card set and the 「질문 카드 6장」 checklist row). Nothing under `docs/versions/`
was patched; six **new** versions were created and `rebuild-docs` run:

- **`experience v0011`** — the start screen is **four** cards, the two 공시 slots resolved per request,
  `get_event` demonstrated inside the 계산 chain, and **`save_feedback` is off the start screen by the
  operator's decision** (so a demo walk writes nothing). Adds why the companies are served rather than
  typed. Plus: the 연락처 card no longer demonstrates honest-unset — the contact is configured and
  published in two places, and honest-unset now governs only an *unset* value.
- **`frontend v0013`** — `app/ask/page.tsx` as the start screen's **data boundary** (`connection()` +
  `no-store` → `ƒ Dynamic`, the 2.5s timeout, the **per-card** fallback contract, no Korean on the
  wire), and the footer publishing **served** config (the read in the root layout, cached 600s, 2s
  timeout; absent → no contact line; the R8 §4 override; the measured height change; route kinds
  unmoved). The P11 chip section from v0012 stands unchanged — it is still true.
- **`qa v0014`** — three re-cuts and one new line: the **P9 start-screen row** (6장 → **4장**, four
  distinct 도구 행, and *pressing a start card now writes nothing* as the check); the **P8 푸터 row**
  (「no mono anywhere, one row」 is now untrue by the operator's own override — struck and re-cut, with
  what still holds); the whole **P11 block** re-cut to this product (five-chip measurements, the
  `ƒ /ask` teeth, the fallback, the contact in both places); and a new line recording the 600–620px
  phone split so the band is not rediscovered as new.
- **`operations v0013`** — the `MIJUAL_OPERATOR_CONTACT` row rewritten (set; feeds the footer as well as
  the agent; gitignored so it travels in no commit **as a file**; public by decision, not a secret) plus
  a new section, *The one setting a deploy silently gets wrong* — unset on the API means every reader
  page silently loses its contact line while the dev box looks correct, a change takes up to 10 minutes,
  and an unreachable API reads as unset for the chrome.
- **`backend v0008`** — the two new read surfaces: **`load_start_cards`** with its full ranking and the
  `find_corps` findability check, and **`mijual.web.site` + `routers/site.py`** with `split_contact`
  (server-side, once, so the two readouts cannot drift) and the three-nulls-with-a-200 contract.
- **`security v0009`** — the stale 「P6 left the value unset … still the operator's, at P4」 paragraph is
  struck and replaced: the contact is **published by the operator's decision**, so it is not a secret;
  **its literal text is therefore also in the repository** (`tests/test_web_site.py` pins it, the phase
  notebook records it) — `.env` being gitignored keeps the *file* out of commits, not the value; the
  real secret scan (`DART_API_KEY` · `GEMINI_API_KEY` · `MIJUAL_VOCKY_API_KEY`) is still **0 hits over
  1128 tracked files**; and `/ops`'s flight data carries the values though nothing renders them.

**Three corrections the phase's own `## Doc impact` list did not name**, added by this review and
recorded in the notebook: qa's P8 푸터 row, backend's `/ask/start-cards` endpoint (F1 logged frontend,
experience and qa lines but no backend line for its own new route), and security's stale paragraph. None
of them is a defect in the product; they are the gap between what a fix slice notices and what the phase
actually made true.

## 7. The cumulative `## Regression Checklist`, re-run

Beyond the deterministic commands in the verdict block, the browser blocks were re-run in the
**production** build across 8 reader routes (`/`, `/ask`, `/stocks`, `/stocks/00102618`, `/portfolio`,
`/auth/login`, an event detail, a 404) at 1280, plus 390 / 481–840 / 767 / 768 passes:

- **brand mark** — every route carries exactly one nav `img` at **90.75 × 27** and one footer at
  **80.66 × 24**, natural **1247 × 371**, `complete`, `alt="주주의관제탑"`, **including the 404**;
- **titles** — every reader page `주주의관제탑`, the `/ops` door `주주의관제탑 운영`;
- **retired spellings** — 미주알 / 미주얼 / MIJUAL / Mijual in **no** reader page's `innerText`;
  `/assets/mijual-*.png` **404**; a repo grep finds the old names only in prose and comments; the
  out-of-scope identifiers (`src/mijual`, `name = "mijual"`, `mijual-frontend`) are untouched;
- **favicon** — `link[rel*=icon]` = **3** on every reader page, `/icon.png` `/icon1.png`
  `/apple-icon.png` 200, `favicon.ico` **404**;
- **nav width reservation** — `header nav a` lefts identical **to the decimal** (`[218.75,
  279.484375]`) on all eight routes;
- **fonts / third-party** — `document.fonts` holds `notoSansKr` + `plexMono` and **no Pretendard**,
  exactly one `link[rel=preload][as=font]`, and **zero** foreign origins requested on any route;
- **widths** — `/stocks` **620px** and `/stocks/{corp_code}` **960px** in the production build;
- **motion** — under `prefers-reduced-motion: reduce` the whole document reports **0** animations on `/`
  and `/ask`; without it the ambient set is still there (`drift`, `twinkle`, `shoot`, `orbit`);
- **mono line breaks** — measured the P10 round-4 way (comparing rects' `top`, never
  `getClientRects().length`): **0 splits at every width the P8 box names** (1512 / 1440 / 1280 / 768 /
  767 / 481 / 390). One new observation at widths that box does not name — §9 finding 1;
- **AI 질문 blocks** — one `<p>` per answer with inline sentences and **0 `<br>`**; 근거 N건 equals the
  distinct chip count (5건 / 5 chips); the completed footer is 근거 · 접수번호 · KST + DART 원문 ↗ ·
  이벤트 상세 · 내 종목 조회 with no 「다시 질문」; the `/ask` 빈 상태 has 익명 줄 0, 새 대화 0, 「범위:」 칩 0;
  `rg "480|481"` over `frontend/components/ask` + `lib/ask.ts` returns **nothing**; no quota or
  storage-denial copy and no `localStorage.` call in the ask surfaces (only comments naming the ban);
- **AI 질문 경계** — no launcher and no widget at 390/767; the widget is `440 × 620` on the landing at
  1280 and opening it shifts `<main>` by 0px.

**Two boxes I did not re-run, and why.** (1) The authenticated `/ops` boxes (the six-tab strip's rects,
대화 로그, `/ops/feedback`, the AX tab names) need operator credentials this session does not hold; the
`/ops` **door** was checked (title, no footer, values not rendered) and the operator sees
`/ops/feedback` in the walkthrough anyway. (2) The P10 round-3 splice forensics were re-run as the trim
box and asset statistics rather than a re-derived alpha hash — the binary is byte-unchanged since P10
and P11 touched no asset.

## 8. The walkthrough returned to the orchestrator, and the routed questions

Reproduced here because the verdict block is ephemeral. Runtime: `make stack-up`, then
**`http://127.0.0.1:3010`** in Chrome (the tailnet URL from `make stack-status` for a real phone). The
stack was left **running in dev**, as the manifest describes.

**A — the defect that started the phase (2 min).** Open `/ask`. Press the **first** card. The answer's
sentence ends with a period and its chips sit **side by side after it, on the same line** — no line
break before or after any chip. Tap a chip: the quote **floats over** the text and nothing on the page
moves. Tap it again (or Esc, or click away) to close.

**B — four cards, and the two companies are today's (4 min).** Press 「새 대화」, then the cards **one at
a time**. Four distinct 도구 행: 이벤트 검색 · **계산** (whose trace shows 검색 → 이벤트 읽기 → 계산 —
that is where 이벤트 읽기 lives now that its own card is gone) · 내 포트폴리오 읽기 · 운영자 연락처.
**The first two cards name a company chosen when the page loaded** — reload the page a few times over
the coming days and the names should follow the corpus, never a fixed pair. Check that each answer is
about **that** company. Two closer looks: card 2's 검증된 계산 marks 보유 주식수 「입력」 (your number)
while the 배정비율 carries a 근거 chip; card 3 says 구성 예시.

**C — the row and calc placements (1 min).** In card 2's answer, tap the chip in the 공시에서 읽은 값 row
and the one in the 계산 block: the quote opens **under the row, across the block**, and the value column
does not move.

**D — the contact, both places (2 min).** Card 4's answer carries your email and phone. Then look at the
**footer of any page** — `/`, `/stocks`, `/portfolio`, an event detail: the same two values close the
identity row (email in the UI face, phone in mono, both tappable). `/ops` shows no footer by design.

**E — the footer got taller, and you should see it (1 min).** Narrow the window from full width to about
800px. Between roughly **481 and 840px** the footer is now **two rows** where it used to be one (73px →
118–132px), because the identity line grew by the two contact values. Nothing overlaps or overflows and
「의견 보내기」 still hits, but it is a visible change to a surface R8 signed. **Accept, or ask for it
back** (e.g. phone only, or the contact on its own row).

**F — the widget (1 min).** On the landing page open the sparkle launcher bottom-right, ask the same
question, tap a chip: the quote must stay **inside the widget**.

**What is real and what is not:** every answer above comes from the live agent reading real DART
filings — nothing is stubbed. **Nothing on the start screen writes anything any more**: the 의견 card
that filed a row on every press is gone, so pressing cards during this walk leaves your review queue
untouched.

**Decisions you are being asked to take:**

1. **The quote panel's background** is the phase's one deliberate deviation from what R16 signed —
   opaque `#0e1a15` instead of the translucent `--surface-inset`, because as an overlay the translucent
   ground let the sentence read through your evidence. The 2px green edge and the 180px cap are
   unchanged and no new colour was introduced. **Accept, or ask for it back.**
2. **The ≤767 인용 칩 target is 14 × 16 px, not the 44px R16 §2.6 lists** — and it never was: the round's
   own CSS never implemented it either, so the record's prose and its code disagree and the product
   follows the code. Closing it means either a 44px box drawn around a 10px number or an invisible hit
   expander that makes each chip's target swallow its 3px-away neighbour, on the very multi-chip
   sentence this phase fixed. **Close it (at which cost), or leave it as the round's CSS has it?**
   *(Routed from `## Operator Questions`, P11.S1 — still open; your gate report did not address it.)*
3. **`/ops/feedback` holds 8 rows.** Ids 1–3 are genuine reader feedback from 2026-08-22; **ids 4–8** all
   carry the 의견 card's sentence verbatim — 4–6 from `P11.S2`'s verification, 7 from the first review,
   and **8 from your own gate walk**. No more can arrive from the start screen. **Clear ids 4–8 before
   the P4 demo, or keep them as evidence that the capability works?** *(Routed from `## Operator
   Questions`, P11.S2 — corrected from the earlier 「ids 4–7」.)*
4. **Your email and phone are now in the repository as well as on the site** — a test pins them
   (`tests/test_web_site.py`) and the phase notebook records them. That follows from your instruction to
   publish them on every page, so nothing is disclosed that the site does not already print; but if you
   would rather the repo carried a fictional string, that is a one-line change. **Fine as is, or swap
   the test's fixture?**

**Fresh-eyes findings — reported, not fixed, and explicitly not judged against the design record:** §9.

## 9. Fresh-eyes walk (first-time reader), reported not fixed

1. **The phone number splits across two lines between 600 and 620px** — 「010-」 / 「3772-9916」. Measured
   at 22 widths: it is a ~40px band, and **none of the P8 mono box's widths (1512/1440/1280/768/767/481/
   390) hit it**, so that checklist row still passes as written. A phone number broken mid-number reads
   badly and could be mis-copied. Deferred job below; not fixed silently, because `white-space: nowrap`
   on a mono phone is a typographic decision on a surface R8 signed.
2. **The identity row is a dense run of unlabeled values** — 자료: … · © 주주의관제탑 · email · phone,
   four things separated by the same `·`, with the last two in different faces. It is legible and the
   label-less form is deliberate (no invented Korean copy), but on first read the phone looks like part
   of the copyright line.
3. **The two 공시 cards name companies most readers will not recognise**, and they **change on reload**.
   That is exactly what the operator asked for, and it is the right trade; it is worth knowing that a
   reader who reloads twice sees two different first screens.
4. **The 계산 card's sentence wraps to two lines while the other three fit one**, so the top row of the
   grid is 63px and the bottom 56px. Signed layout, no orphan, nothing clipped — just visibly uneven.
5. **With the API down the landing `/` returns 500** with an **English** error screen (「This page
   couldn't load … ERROR …」) on an otherwise all-Korean product, while `/ask` degrades gracefully to its
   four static cards. Pre-existing (`app/page.tsx` awaits its board reads with no `catch`, unlike
   `/stocks`) and not P11's, but I saw it and it is the worst thing a reader can hit if the API blinks.
6. **One React #418 (server/client text mismatch) fired once in the production build** during a
   multi-route sweep, and I could **not** reproduce it in 11 further attempts (8 isolated route loads and
   3 repeats of the exact sequence). No route throws it in isolation; the likeliest cause is a
   second-boundary difference in a rendered timestamp. Reported as an observation, not a defect —
   listed below so a second sighting is not treated as new.

## 10. Deferred jobs for the orchestrator to file (`defer-job`; I do not file them)

1. **title:** 푸터 전화번호가 600–620px에서 두 줄로 끊긴다
   **reason:** the mono phone splits as 「010-」 / 「3772-9916」 in a ~40px band; none of the P8 mono row's
   asserted widths cover it, so it passes the checklist while reading badly. Fixing it is a typographic
   decision on a surface R8 signed (nowrap, or a narrower separator), not a bug fix.
   **trigger:** the next footer or chrome work, or before the P4 demo if the operator browses that band.
2. **title:** API가 죽으면 랜딩(`/`)이 500 — 영문 오류 화면
   **reason:** `app/page.tsx` awaits `getBoardSummary()`/`getBoard()` with no `catch` (unlike
   `/stocks`), so an API blink turns the product's first page into Next's English error screen while
   `/ask` degrades gracefully. Pre-existing, unrelated to P11, and the only place the reader meets
   English.
   **trigger:** the next landing-page work, or P4 deployment hardening.
3. **title:** `tests/test_web_site.py`가 운영자의 실제 이메일·전화를 고정한다
   **reason:** the published contact is public by the operator's decision, so this is not a leak — but
   the literal value is now in git history via a test fixture, and a fictional string would test the
   same code. Raise it with the operator first (walkthrough decision 4); if they prefer, it is a
   one-line change.
   **trigger:** the operator's answer to walkthrough decision 4, or any future work on `mijual.web.site`.
4. **title:** 프로덕션에서 React #418(하이드레이션 텍스트 불일치)이 한 번 관측됨
   **reason:** §9 finding 6 — one occurrence in ~20 production loads, unreproducible in 11 attempts, no
   route reproducing it in isolation. Most likely a time-stamped string crossing a second boundary.
   Worth a watch rather than a hunt.
   **trigger:** a second sighting, or the next time a route renders a live clock value.

*(D28 is already promoted — it became `P11.F1`; D29 was dropped when the 의견 card was removed; **D30**
— 푸터 「AI 질문」 40 × 44 at 390 — is open and untouched, re-measured unchanged at this review.)*

## 11. Scope

No source file was touched by this review — the only edits are the six doc versions, the six regenerated
`docs/current` files, `docs/index.json`, `phase.md` and this file. No workflow state-transition command
was run (`review-phase`, `accept-gate`, `finish-slice`, `set-*-status` are the orchestrator's), nothing
was committed, and `defer-job` was not run — the four jobs are listed above for the orchestrator. The CDP
harness, the stub upstream and the screenshots lived in the session scratchpad, outside the repo, and the
two temporary servers (a production build on 3010 and a stub-backed one on 3011) are stopped. The dev
stack was restarted around the production run and is **back up** in the manifest's own configuration
(`make stack-status`: api pid 36968, web pid 37588, `http://127.0.0.1:3010` → 200).
