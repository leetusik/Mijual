# Phase P13: Email verification at signup (6-digit code)

_Intent: see [intent.md](intent.md)._

## Objective

Registration requires proving control of the address: signup creates the account unverified, mails a single-use 6-digit code and opens no session; the auth panel switches to a code-entry state on the same surface, and the right code verifies the account and logs the reader in. Logging in with the correct password on an unverified account routes to the same code step with a 재전송 option. Existing production accounts are grandfathered as verified by the migration. No design round: the phase extends the signed R5/R12 auth vocabulary and the operator approves the exact Korean strings at the acceptance gate. The verification mail is proven live on production.

## Slices

<!-- slices:begin -->
_Generated by `python3 scripts/workflow.py rebuild` from each slice's `slice.json`; never hand-edit this block._

| Slice | Name | Kind / risk | Status | Outcome | Result |
|---|---|---|---|---|---|
| `P13.DECOMP` | decompose phase | `decomposition / high` | `done` | Cut P13 into three bare middle slices (S1 backend, S2 frontend, S3 release + live proof) and seeded phase.md with the NULL-means-verified column, the code grant's shape, the full API contract, the three-kind mail rule, the copy route and the release gates | [result.md](slices/P13.DECOMP/result.md) |
| `P13.S1` | Backend: unverified account state, the 6-digit code grant, the third mail kind, gated signup/login, verify + resend, tests | `implementation / high` | `done` | Backend gate built to the notebook's contract — signup creates an unverified account, mails a single-use 6-digit code and opens no session; /auth/verify (password then code) verifies and logs in; login on an unverified account answers verification_required; resend supersedes under a 60 s cooldown; a completed reset also verifies; existing accounts verified by column shape, proven on dev Postgres (+1 columns, 0 pending), 171 tests green, curl-smoked on the running dev API | [result.md](slices/P13.S1/result.md) |
| `P13.S2` | Frontend: the code-entry state on the auth panel, the drafted Korean strings, real-browser verification | `implementation / high` | `done` | Auth panel gained its 인증번호 state — 계정 만들기 and 로그인 on an unverified account land on the same panel, the mailed code verifies and lands on 보유 종목, ten drafted Korean strings plus the re-drafted 계정 만들기 intro cited for literal approval at the gate; verified in Aside (u2) on dev and the local production build at 1280/390, which agreed on every measured value; found S1's 5-attempt cap inert over HTTP (the increment is rolled back with the error) — proposed P13.F1 | [result.md](slices/P13.S2/result.md) |
| `P13.F1` | Fix: the wrong-code attempt increment must survive the ApiError rollback so the 5-attempt cap is real over HTTP; make the test fixture roll back like production | `fix / high` | `done` | verify_code commits the failed attempt before the ApiError travels, so the increment survives get_write_session's rollback and the 5-attempt cap is real over HTTP (measured: 불일치 ×4 then 만료, row attempts = 5, the mailed code dies with the grant); the three test write-session fixtures now roll back like production — the cap test went red on the honest fixture alone and green on the fix; 171 passed | [result.md](slices/P13.F1/result.md) |
| `P13.S3` | Release to production and prove the verification mail live | `implementation / high` | `todo` | — | — |
| `P13.REVIEW` | phase review | `review / high` | `todo` | — | — |
<!-- slices:end -->

## Decisions

_Durable cross-slice decisions. Replace a superseded line; never stack versions._

**Schema — no Alembic, and NULL means verified.**

- The repo has **no Alembic and never did** (`intent.md` assumption 6 is wrong on that point; corrected here). Schema evolution is `python -m mijual.db ensure` — `create_all` for missing *tables*, then `mijual.db.schema_sync.ensure_columns` for declared-but-missing *columns*, which **raises** on any column that is `NOT NULL` or carries a `default`/`server_default`. Production runs it as the `mijual-schema` one-shot before the API starts. There is no data-migration step and none is invented.
- **`account.verification_pending_since: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))`** — nullable, **no `default=`**, so `ensure_columns` accepts it. **NULL means verified.** Every existing production row therefore arrives verified with zero data work: that *is* the grandfathering, and it is a property of the column's shape, not a step anyone runs. `create_account` sets it to `utcnow()` **explicitly in the function body** (never as a column default, which `ensure_columns` would refuse); verify/reset-confirm set it back to `NULL`. It doubles as the age stamp assumption 3 asks about.
- A `verified_at`/`verified: bool` column is **rejected**: either needs a backfill this schema path cannot run.
- `Account`'s docstring names the new column **deliberately** as account *state*, not PII — the list of what must stay absent (no name, no admin flag, no conversation join, no activity trail) is unchanged.

**The code grant — `email_verification`, modelled on `PasswordReset` with three deliberate differences.**

- New table beside `password_reset`, created by `create_all` on production (so its own columns may be `NOT NULL`/defaulted — `ensure_columns` skips tables it did not find): `id`, `account_id` FK `ondelete="CASCADE"` **plus** an ORM `cascade="all, delete-orphan"` relationship on `Account` (both halves — SQLite does not enforce FKs), `code_digest String(64)`, `created_at`, `expires_at`, `used_at`, `attempts Integer NOT NULL default 0`. Index on `account_id`.
- **Difference 1 — no `UniqueConstraint` on `code_digest`, and lookup is by `account_id`, never by digest.** A reset token is 256 bits and is addressed *by itself*; a 6-digit code has 10^6 values, so two accounts can hold the same digest under the same pepper and a unique constraint would turn that collision into a 500. The code is only meaningful *with* the address.
- **Difference 2 — an `attempts` counter, and it is real (P13.F1).** `VERIFICATION_MAX_ATTEMPTS = 5`: a wrong code increments it, and a row at the cap is **not live** (same lookup predicate as expired/spent). This is assumption 2's row counter, **not** the cross-process login rate limiting still parked in P4. **The counter only bites because the failed attempt commits before the error travels.** `verify_code`'s wrong-code branch calls `db.commit()` — capturing the reached-the-cap boolean first, since the test factory expires instances on commit — and *then* raises. It is the **one place the service layer commits**, because `get_write_session` rolls back on any exception: right for every other 4xx here (a rejected 가입 leaves nothing behind) and wrong for a counter whose whole job is to make failures expensive. Between S1 and F1 the cap was **inert** (S2 measured eight wrong codes over HTTP, the row still `attempts = 0`); from F1 it is proven live over HTTP — five wrong codes answer 불일치·불일치·불일치·불일치·**만료**, the row reads `attempts = 5`, and the genuinely mailed code dies with the grant. The suite was blind to it because all three write-session fixtures committed without ever rolling back; they now mirror `get_write_session`, so the existing cap test fails on a regression (it did, before the fix — `slices/P13.F1/result.md` § 1). After the raise the dependency's `rollback()`/`close()` run on a fresh, empty transaction and are harmless.
- **Difference 3 — a resend cooldown, `VERIFICATION_RESEND_COOLDOWN = timedelta(seconds=60)`**, read from the live row's `created_at`. Without it 재전송 (and re-signup) is a mail-bomb aimed at any address. Same class of protection as the attempt counter, decided here. **As built (S1) the clock is the *most recently issued* row's `created_at`, live or not, and the cooldown outranks every caller**: nothing mails twice inside 60 seconds, not 재전송, not re-signup, not login. One service function, `issue_verification(..., force=)`, is the only thing that ever mints a code — `force=False` (login) *ensures* one exists, `force=True` (signup, 재전송) *replaces* the live one — so the throttle cannot be forgotten by a later caller.
- Otherwise the `PasswordReset` rules are copied exactly: **digest, never the secret**, through `mijual.web.auth.token_digest` (keyed with `session_pepper`, so rotating `MIJUAL_SESSION_SECRET` kills every outstanding code — a property), `expires_at`, `used_at` for single use, supersede-on-repeat (`delete` unused rows, then add). Digest comparison uses `hmac.compare_digest`.
- **`VERIFICATION_LIFETIME = timedelta(minutes=10)`** (assumption 1). The code is `f"{secrets.randbelow(1_000_000):06d}"` — uniform, **leading zeros kept**, so it is a string everywhere: the field, the payload and the mail.

**API contract — fixed here so S1 and S2 build the same thing without talking.**

- `POST /auth/signup` `{email, password}` → **201, no session, no cookie**, `{"verification": {"email": <normalized>, "expires_at": <iso>}}`. Never the code, never a token. **As built (S1): `expires_at` is *absent*, never `null`, when no live code exists** — the contract's own "absent, never null" rule. It is only reachable in one corner (the attempt cap or a spend killed the grant *and* the 60-second cooldown has not elapsed, so nothing may be re-issued yet); `email` is always present. Every route carrying a `verification` block behaves this way.
  - Address held by an **unverified** account → **re-issue, not `email_taken`** (assumption 3): the password hash **is replaced** with the one just typed (nobody has proven the mailbox, and a reader who retyped a different password must end up with the one they typed), the live code is superseded and a fresh one mailed **if** the cooldown has elapsed, otherwise the live code stands and its `expires_at` is returned. Both branches answer the identical 201 shape, so signup still discloses nothing about whether the address exists.
  - Address held by a **verified** account → `409 email_taken`, unchanged.
- `POST /auth/login` `{email, password}` — wrong password and unknown address stay one `401 invalid_credentials` at one cost (R5, `_burn_a_hash`), unchanged.
  - Correct password + **verified** → unchanged: session + cookie + `{"account": …}`.
  - Correct password + **unverified** → **no session**, `200 {"verification_required": true, "verification": {"email", "expires_at"}}`. It **issues a fresh code only when no live one exists** (honouring the cooldown otherwise), so a reader returning an hour later gets a working code without pressing 재전송 and one logging in seconds after signup gets no second mail. Disclosure is fine: it fires only after a correct password.
- `POST /auth/verify` `{email, password, code}` — **password first, then code.** Checking the password (burning a hash on a miss, exactly as `authenticate` does) means the one who verifies is the one who chose the password, which closes the pending-signup race where a second signup inside the window swaps the hash under a code the mailbox owner then enters. The panel holds both fields in state on both routes into the code step.
  - Success → `used_at` set, `verification_pending_since = NULL`, **session started + cookie set** (the `reset/confirm` precedent), `200 {"account": …}`.
  - Wrong code, grant still live → `attempts += 1`, `400 verification_code_invalid`. **Decided in S1: any wrong value costs an attempt, including one that is not six digits** (and including an empty string). A value that cannot be the code is simply not the code; exempting malformed input would add a branch whose only observable effect is a cheaper guess. The field is `code: str` with `max_length=16`, `strip()`ed, so a bad length is never a 422 — it is the ordinary structural code.
  - No live grant — expired, spent, never issued, **or killed by that same increment reaching the cap** → `400 verification_code_expired`. Two codes, not one, because the panel must point at 재전송 for the second and must not for the first.
  - Correct password on an **already verified** account → treated as a login (session + `{"account": …}`). A correct password already proves everything; inventing a failure here would only strand a reader who pressed 확인 twice.
- `POST /auth/verify/resend` `{email, password}` — password required, so it cannot be pointed at a stranger's mailbox. `200 {"resent": <bool>, "verification": {"email", "expires_at"}}`; `resent: false` when the cooldown has not elapsed (or the account is already verified) — a **state, not an error**, so the panel has one honest line for "the code we already sent is still valid" instead of a timer. Wrong password / unknown address → `401 invalid_credentials`, uniform with login. **As built (S1): on an already-verified account the block is `{"email"}` alone** (there is no code and none is coming), which is the same absent-`expires_at` rule as everywhere else.
- `POST /auth/reset/confirm` **also clears `verification_pending_since`** (assumption 4) — mailbox proven. `POST /auth/reset/request` is **unchanged** on an unverified account: 가입 여부 비노출 forbids branching the answer at all.
- `/auth/me`, `account_payload`, `/auth/logout`, `PATCH`/`DELETE /auth/account`, the chrome and `/ops` are **unchanged** (out of scope per `intent.md`).
- **Stale unverified accounts (assumption 3): no new beat entry, no sweep.** The address is never blocked — a fresh signup re-takes it at any age — which is what the assumption actually asks for. A janitor that deletes long-dead unverified rows is a **`defer-job` candidate**, listed for the review, not a slice here.

**Mail — the two-kind rule becomes three, deliberately.**

- `mijual.mail` gains `SIGNUP_VERIFICATION = "signup_verification"` (data: `code`, `expires_at`) and a third `render()` branch; `mijual.mailcopy` gains `signup_verification_subject` / `signup_verification_body`. **Both docstrings' structural rule is rewritten to say three kinds** — 마감 알림 · 비밀번호 재설정 · 가입 인증 — never slipped in (assumption 5): `mail.py`'s "Two kinds, and there will not quietly be a third" and `mailcopy.py`'s "알림 외 메일 금지. Two kinds exist".
- The mail carries the **code itself** (not a link) and its KST validity through the existing `kst_stamp`. `mailcopy.py`'s three hard rules stand: no won amount anywhere, no sentence in `data`, no second spelling of a shared label.
- **Copy route (both mail and panel): drafted in this phase, cited `drafted P13 — approved literally at the P13 gate`** — the same route `mailcopy.py` used for `drafted P4.S2`. No design round, no invented sentence outside a cited constant, and every drafted string is **listed in the acceptance walkthrough** for the operator to approve literally.
- Dev has no SMTP: `ConsoleMailer` prints the code to `var/stack/api.log`, which is how S2 reads codes in the browser. Production's `SmtpMailer` logs `smtp mailer: sent signup_verification` — the line S3 greps for.

**Frontend — one panel, a third mode.**

- **Built and swept live in S2** (the inventory below is now description, not instruction): a third `mode` inside `AuthPanel` — no verify route, no modal, no overlay — with the origin mode remembered, `email` replaced by the **normalized** address from the response, `password` kept (both calls need it), `code` reset and focused on entry. One field (`id="auth-code"`, `type="text"`, `inputMode="numeric"`, `autoComplete="one-time-code"`, `maxLength=6`, string value), 확인 → `PENDING_KO` + disabled, a quiet row of 인증번호 재전송 + the way back (labelled with the **origin mode's own name**, so it needed no constant). Client gate before any request: not exactly six digits → the drafted line, no POST (proven — the two gated submits appear nowhere in the API log). `lib/api.ts` returns `AuthResult` from both `signup` and `login`, narrowed on the **key** (`"verification" in result`), plus `verifySignup` / `resendVerification`; `Verification` lives in `lib/types.ts` with the "absent, never null" rule. **One** CSS rule was added, `.code { font-variant-numeric: tabular-nums }` — no geometry touched (panel 432/358, submit 48, field 324×48 at 390, identical to the other modes in both runtimes).
- **`SIGNUP_INTRO_KO` was false and is re-drafted** — 「… 만들어지면 바로 로그인됩니다.」 → 「… 인증번호를 메일로 보내 드립니다.」, R5's first clause untouched. It goes to the operator with the rest of the drafted strings.
- **Two live behaviours the panel is built around, both confirmed in the browser:** routing in through 로그인 **mints a code when none is live**, so the 만료 state is unreachable by logging in again (the design working — the expired line was proven by expiring a grant under the reader instead); and `resent: false` inside the cooldown mails nothing and is answered as a 알림, never an error.
- The signed R5/R12 vocabulary binds unchanged: one `p role="status"` line slot, `--alert` never on this layer, the button's own text swapped and disabled, 로그인됨 is 보유 종목 (not a panel state), and `authErrorKo` returns **`null` for any unsigned code** so the panel renders no line — the two new structural codes (`verification_code_invalid`, `verification_code_expired`) must be **mapped there or they are silently invisible**.
- `frontend/lib/api.ts`: `signup` and `login` return types become unions (`{account}` | `{verification_required, verification}`), plus `verifySignup(email, password, code)` and `resendVerification(email, password)`. `X-Mijual-CSRF` on every mutation, unchanged.

**Tests — the repo's "core behaviour only" rule.**

- The hard gate **is** core auth behaviour, so S1 updates what breaks and adds the **minimum**: signup opens no session and the right code does; login on an unverified account opens no session and routes; N wrong codes kill the code; a reset completed on an unverified account verifies it. **Nothing for the panel** — that is verified live in the browser.
- **Instrument and runtime for every browser claim: Aside on the agent account `u2`** (profile 「claude2」), `aside repl --account u2 "<js>"` — never `u0`, never `aside account use`, never `aside profile list` (hangs). Runtime is `## Operator Runtime` (`docs/current/operations.md`): `make stack-up` (API `127.0.0.1:8010`, `next dev` `:3010`), viewports **1280 and 390**, plus the **local production build** on a spare port. That section's sentence saying Aside's daemon does not run here is **stale** — P4 owes the correcting note and P12 recorded it; P13 writes no new note for it.

**Release.**

- **No design round** (`intent.md`), so no `co-work` slice, no `DECOMP2`, no research slice: the shape is decided above from the code as it stands on 2026-09-05.
- The **acceptance gate is `--require`** (`intent.md`): the auth surface changes visibly and the operator approves every drafted Korean string at it.
- **Two release dependencies no executor may resolve**: `deploy/deploy.sh` releases `origin/main` on the box and **pushing `main` is the operator's**; and the **deploy freeze 2026-09-07 11:00 → 2026-09-11 23:59 KST** (`deploy/runbook.md`). S3 checks both at its start and returns `needs_operator` rather than working around either.

## Doc impact

_One line per durable-truth change: `- <doc>.md: <what changed> (<slice>)`. Append only; the review verifies this list and a later docs phase consolidates it into versions — never per slice, never at the review._

- api.md: `POST /auth/signup` no longer opens a session or sets a cookie — 201 `{"verification": {"email", "expires_at"?}}`; `POST /auth/login` gained a second success shape `{"verification_required": true, "verification": {…}}` (200, no cookie) for a correct password on an unverified account; two new routes `POST /auth/verify` (`{email, password, code}` → `{"account"}` + cookie) and `POST /auth/verify/resend` (`{email, password}` → `{"resent": bool, "verification": {…}}`); two new structural codes `verification_code_invalid` / `verification_code_expired` (both 400), with `invalid_credentials` (401) unchanged on both new routes; `expires_at` is absent — never null — when no live code exists (P13.S1)
- backend.md: the 가입 인증 flow in `mijual.web.auth` — `new_code()` (6 digits, string, leading zeros kept), `issue_verification(force=)` as the **only** minting path (`force=False` ensures, `force=True` replaces; the cooldown outranks both), `live_verification()`'s one liveness predicate (unspent · unexpired · under the attempt cap), `verify_code()` (password first through `authenticate`, then the code), `resend_verification()`, and the three constants `VERIFICATION_LIFETIME=10m` / `VERIFICATION_MAX_ATTEMPTS=5` / `VERIFICATION_RESEND_COOLDOWN=60s`; `start_session()` now **raises** on an unverified account; `confirm_reset()` also clears the pending column; `mijual.mail` carries a **third** kind `SIGNUP_VERIFICATION` with renderers in `mijual.mailcopy` and both modules' two-kind rule rewritten to three (P13.S1)
- security.md: the reader auth model gained a hard mailbox gate — 가입 creates an account and opens no session, 인증 requires the account's own password *and* the mailed code, the code is stored only as a `token_digest` (pepper-keyed, so rotating `MIJUAL_SESSION_SECRET` kills every outstanding code), a grant dies at 5 wrong attempts and after 10 minutes, and no mail is sent twice inside 60 seconds; `start_session` is the single structural enforcement point (it raises); pre-P13 accounts are verified by the column's shape, not by a backfill; the Account PII statement gains one **state** column (`verification_pending_since`) while the must-stay-absent list is unchanged; a wrong password on the two new routes answers `invalid_credentials`, byte-identical with login, and signup answers the identical 201 for a free address and for one held by an unverified account (P13.S1)
- data.md: table 20 — `email_verification` (`account_id` FK `ondelete=CASCADE` + ORM `delete-orphan`, `code_digest String(64)` **deliberately not unique**, `created_at`, `expires_at`, `used_at`, `attempts NOT NULL default 0`, index `ix_email_verification_account`); `account.verification_pending_since` (nullable, no default, **NULL means verified**) (P13.S1)
- architecture.md: no Alembic still — `python -m mijual.db ensure` adds the new column additively (`schema ok (+1 columns)` then idempotent) and `create_all` creates the new table; the grandfathering is the column's shape, so the phase ships no data-migration step (P13.S1)
- frontend.md: `AuthPanel` has a **third mode** — 이메일 인증, a state of the panel and not a route (title / one intro line naming the normalized address and the 10분 window / one `#auth-code` field, `type=text` + `inputMode=numeric` + `autoComplete=one-time-code` + `maxLength=6` + string value / 확인 with the `PENDING_KO` swap / a quiet row of 인증번호 재전송 + a way back labelled with the origin mode's own name), entered from both 가입 and 로그인-on-unverified with `email` replaced by the normalized address and `password` kept; `lib/api.ts`'s `signup` and `login` now return the `AuthResult` union branched on the **key** (`"verification" in result`), plus `verifySignup` / `resendVerification`, and `lib/types.ts` gained `Verification` (`expires_at` optional, never null); `authErrorKo` maps `verification_code_invalid` / `verification_code_expired` to two different lines; `Auth.module.css` gained one rule, `.code { font-variant-numeric: tabular-nums }` (P13.S2)
- experience.md: 인증 — 계정 만들기 now ends in a code step on the same panel instead of a session (「인증번호를 메일로 보내 드립니다」), 로그인 with the right password on an unverified account routes to that same step rather than failing, the step offers 인증번호 재전송 (a cooldown answers 「아직 유효합니다」 as a 알림, with no timer), an empty or short code is answered on the surface before any request, and a dead code points at 재전송 without saying whether it expired or was exhausted; the right code lands on 보유 종목 exactly as 로그인 does (P13.S2)
- product.md: 계정 만들기 now requires the 6-digit code mailed to the address — the account exists but cannot be used until it lands, and 로그인 with the right password on an unverified account routes to the same code step instead of failing; existing accounts are unaffected (P13.S1)
- backend.md: `verify_code`'s wrong-code branch **commits before it raises** — the one place `mijual.web.auth` commits, because the failed attempt is the write of record and `get_write_session` rolls back on any exception; the reached-the-cap boolean is read **before** the commit (the test session factory expires instances on commit, the app's does not); every other raise-after-write in the module was audited and each one intends its discard (the two `IntegrityError` branches roll back themselves); the three test write-session overrides now mirror `get_write_session` — commit on a normal return, roll back on exception — so the suite can see this class of bug (P13.F1)
- security.md: **amends P13.S1's claim** rather than repeating it — the 5-attempt cap was **inert between S1 and F1** (the increment was rolled back with the 400 it raised, so a 6-digit code was guessable without limit for its full 10 minutes) and is **real from F1**: measured over HTTP, five wrong codes end the grant, the fifth answers `verification_code_expired`, the genuinely mailed code dies with it, and the row persists `attempts = 5` (P13.F1)
- qa.md: regression — five wrong codes over HTTP kill the grant (1–4 `verification_code_invalid`, the 5th `verification_code_expired`, the mailed code then also expired, the row reads `attempts = 5`); a test fixture that commits without rolling back on exception cannot see it (P13.F1)

## Operator Questions

_Questions only the operator can answer. Append only; every entry is routed at the review — folded into the acceptance walkthrough or filed with `defer-job`. An unrouted entry is a review finding._

- **(from P13.DECOMP)** **The production proof needs a real mailbox.** S3 signs up for real on `https://jujutower.com` to prove the verification mail through the live SMTP transport, the way P4.S4 proved the reset mail. Proposed: a **plus-address of the operator's own inbox**, `leetusik+p13@gmail.com` — confirm that address or name another. Also confirm the disposal: the throwaway account is **deleted on the box afterwards** (proposed) or left standing for the operator to inspect at the gate.
- **(from P13.DECOMP)** **Every new Korean string is drafted, never signed** — the verification mail (subject + body), the panel's code-entry state (title, intro, field label, 확인, 재전송, the four line slots) and the **re-drafted `SIGNUP_INTRO_KO`**, whose current sentence 「만들어지면 바로 로그인됩니다」 becomes false under the gate. All of them need the operator's **literal** approval at the acceptance gate; the walkthrough must list them verbatim.
- **(from P13.S2)** **The 5-attempt cap does not exist in the running product, and the release waits on the answer.** S2's browser sweep found that a wrong code's `attempts` increment is rolled back with the error it raises, so a 6-digit code is guessable without limit for its full 10 minutes (measured: 8 wrong codes, all 불일치, row still `attempts = 0`). A backend fix slice `P13.F1` is proposed and is cheap; the decision is whether **P13 releases only after it lands** (recommended — S1's `security.md` note already claims the protection) or ships first with the cap knowingly inert.
- **(from P13.DECOMP)** **Should `/ops`'s 독자 계정 table (`GET /ops/users`) mark or hide unverified accounts?** Out of scope for P13 (`intent.md` excludes any `/ops` change), so nothing is built either way — but after this phase the table silently lists accounts that cannot log in, which an operator reading it would not know. Decide whether that becomes a later job.
- **(from P13.F1, answering the S2 entry above)** **Answered by the orchestrator: the release waits for `P13.F1`, and it has landed.** The cap is real over HTTP from this slice (five wrong codes end the grant, the row reads `attempts = 5`), so S3 releases with the protection `security.md` claims rather than without it. The review routes the S2 entry as **resolved**; no operator decision is outstanding on it.
- **(from P13.F1)** **One drafted line is untrue in the corner the working cap just made reachable.** A reader who burns all five attempts is told 「이 인증번호는 더 이상 사용할 수 없습니다 — 인증번호 재전송을 눌러 새 번호를 받아 주세요.」 and, pressing 재전송 inside the first 60 seconds, is answered `resent: false`, which the panel renders as 「조금 전 보낸 인증번호가 아직 유효합니다」 — contradicting the line above it, and false: that code is dead, not still valid. The flow still terminates correctly (wait out the cooldown, press 재전송, type the new number) and no backend behaviour is wrong — the cooldown outranking every caller is S1's anti-mail-bomb property. It is a **copy** decision, and P13's copy is approved literally at the gate: leave the line as drafted, or draft a third one for 재전송-under-cooldown-with-no-live-code (a frontend slice, not a backend one). Detail: `slices/P13.F1/result.md` § 5.

## Notes for later slices

_Gotchas and constraints the next slices need, each tagged `(from P<N>.Sk)`. A slice that consumes a note removes it; the detail stays in that slice's `result.md`._

- **(from P13.S1, for P13.S3)** **The schema step on the box is one line and its evidence is unrepeatable.** `python -m mijual.db ensure` printed `schema ok (+1 columns)` on the first dev run and `schema ok` on the second, and the new **table** came from `create_all` in the same run (both are in `mijual-schema`'s one-shot). Capture `SELECT count(*) FROM account WHERE verification_pending_since IS NOT NULL` = **0** *before* the test signup — on dev it read 0 over 2 accounts.
- **(from P13.DECOMP, for P13.S3)** **Two gates at the very start, both of which are a `needs_operator` and not a workaround.** (1) `git fetch origin && git merge-base --is-ancestor HEAD origin/main` — `deploy/deploy.sh` releases `origin/main` from the box, and **pushing `main` is the operator's** (P12.S2 stopped `pending` for exactly this). (2) The **deploy freeze 2026-09-07 11:00 → 2026-09-11 23:59 KST** (`deploy/runbook.md`, restated in `## Operator Runtime`) — at decomposition time that is **under two days away**, so this slice either lands before it or waits until 2026-09-12.
- **(from P13.DECOMP, for P13.S3)** **What the live proof consists of** (P4.S4's shape): the `mijual-schema` one-shot reporting the added column; `SELECT count(*) FROM account WHERE verification_pending_since IS NOT NULL` reading **0** *before* the test signup (every existing account grandfathered — capture it, it is the grandfathering evidence and it is unrepeatable once you sign up); a real signup at `https://jujutower.com` delivering a real code to the operator-confirmed address; `smtp mailer: sent signup_verification` in `docker compose -f compose.prod.yml logs mijual-api`; the code completing and opening the session; `make smoke-prod` green. Logs and the `.env.prod` credential are read **on the box, by path** — the box clock is GMT, the app containers log KST.
- **(from P13.DECOMP, for P13.REVIEW)** **Two things to route, beyond the `## Operator Questions` list.** (a) A **`defer-job` candidate**: a janitor that deletes long-dead unverified accounts (`verification_pending_since` older than N days) — P13 deliberately builds no sweep because re-signup already re-takes the address, so the only thing missing is housekeeping. (b) The gate walkthrough must carry **every drafted Korean string verbatim** (mail subject + body, the code-entry state's lines, the re-drafted 계정 만들기 intro) — literal approval is the whole copy route for this phase.


- **(from P13.S2, for P13.REVIEW)** **Every drafted P13 Korean string, verbatim — the gate walkthrough carries all of them for the operator's *literal* approval.** Cited in code as `drafted P13 — approved literally at the P13 gate`.
  - **The mail** (`src/mijual/mailcopy.py`, S1): 제목 「[주주의관제탑] 가입 인증번호」 · 본문 「가입 인증번호입니다. 아래 6자리 숫자를 입력해 주세요.」 / (the 6 digits on their own line) / 「이 번호는 {KST 시각}까지 사용할 수 있습니다.」 / 「요청하지 않으셨다면 이 메일을 무시해 주세요. 인증하지 않으면 계정은 사용되지 않습니다.」
  - **The panel** (`frontend/components/auth/copy.ts`, S2): 제목 「이메일 인증」 · 본문 「{email} 주소로 6자리 인증번호를 보냈습니다 — 10분 안에 입력해 주세요.」 · 필드 라벨 「인증번호」 · 버튼 「확인」 (in flight: 「확인 중…」) · 재전송 「인증번호 재전송」 · 재전송됨 「인증번호를 다시 보냈습니다 — 메일함을 확인해 주세요.」 · 쿨다운 「조금 전 보낸 인증번호가 아직 유효합니다 — 메일함을 확인해 주세요.」 · 형식 「인증번호 6자리를 입력해 주세요.」 · 불일치 「인증번호가 일치하지 않습니다.」 · 만료/소진 「이 인증번호는 더 이상 사용할 수 없습니다 — 인증번호 재전송을 눌러 새 번호를 받아 주세요.」
  - **The one re-drafted signed string:** `SIGNUP_INTRO_KO` 「이메일과 비밀번호만으로 만듭니다 — **인증번호를 메일로 보내 드립니다.**」 (was 「… 만들어지면 바로 로그인됩니다.」, which the gate made false). The way back out of 인증 wears the origin mode's own name (계정 만들기 / 로그인) and is **not** new copy.
- **(from P13.S2, for P13.S3)** **Two things the release should know.** (1) The local production build was clean at S2's commit (16 routes, no new warning) and dev and that build agreed on **every** measured value at 1280 and 390 (panel 432/358, submit 48, code field 324×48, the P12.F5 flash reservation one value at 298.52 px over 192 frames in both), so nothing frontend-side is expected to differ on the box — and P13.F1 touched no frontend file, so that still holds. (2) **Cookies ignore ports:** an `mj_session` set on `127.0.0.1:3010` is sent to `127.0.0.1:3014`, so a second local origin must be signed out first or `/auth/login` redirects to `/portfolio` and looks broken.

## Now

- **P13.F1 is done: the 5-attempt cap is real over HTTP.** `verify_code`'s wrong-code branch now commits the failed attempt before the `ApiError` travels, so `get_write_session`'s rollback no longer eats the increment. Measured live on the restarted dev stack: 불일치 ×4 then **만료**, the row persists `attempts = 5`, and the genuinely mailed code is dead with the grant. Backend only — no frontend file was touched, and none needed to be.
- **The suite can now see this class of bug.** All three write-session fixtures (`test_web_auth`, `test_web_portfolio`, `test_web_ops`) mirror `get_write_session` — commit on a normal return, roll back on exception. With that fixture alone and `verify_code` untouched, the existing cap test **failed** (`assert 'verification_code_invalid' == 'verification_code_expired'`); it passes on the fix. `171 passed`, `validate` clean.
- **Every other `raise ApiError` after a write in `mijual.web.auth` was audited and left alone** — each one intends its discard, and the two `IntegrityError` branches already roll back themselves. The table is in `slices/P13.F1/result.md` § 3.
- **Next: P13.S3 (release + the live production proof).** The blocker S2 raised is cleared: nothing about the cap holds the release now.
- **S3's two gates are unchanged and neither is workaroundable:** the operator must have pushed `main` (`deploy.sh` releases `origin/main` from the box), and the **deploy freeze 2026-09-07 11:00 → 2026-09-11 23:59 KST** is now roughly a day out — S3 lands before it or waits until 2026-09-12. Both are a `needs_operator`.
- **Open for the operator, all at the gate:** the plus-address for the production proof, the **literal** approval of every drafted Korean string (all quoted verbatim in the note tagged `for P13.REVIEW`), whether `/ops` should mark unverified accounts, and now one new copy question — 재전송 inside the cooldown on a cap-killed grant renders 「아직 유효합니다」, which that reader's dead code makes untrue.
