# Plan — P13.S2 (implementation, `slice-executor-high`)

**The auth panel's code-entry state, the drafted Korean strings, and the real-browser sweep.** After this slice, 계정 만들기 on `/auth/login` lands the reader in a code-entry state **on the same panel** (no route, no modal, no overlay), the mailed 6-digit code verifies the account and lands on 보유 종목 exactly as a login does, 로그인 with the correct password on an unverified account lands in the same state with a 재전송 option, and every new Korean string is drafted inside the signed R5/R12 auth vocabulary for the operator's literal approval at the gate. Frontend only — the backend landed in S1 and is running on the dev stack.

**Read first:** `works/phases/active/P13/phase.md` — `## Decisions` (*Frontend — one panel, a third mode* is the build inventory; *API contract* is what you call) and `## Notes for later slices` tagged `for P13.S2` (the JSON shapes as built, the log line the code is read from, the two live-behaviour traps, the `authErrorKo` rule, the Aside sharp edges, the production-build recipe) — consume them. `works/phases/active/P13/slices/P13.S1/result.md` § *Step 4* has the exact request/response bodies. `intent.md` only if unsure. Then the code: `frontend/components/auth/AuthPanel.tsx` (its header comment is the panel's contract — read it whole), `copy.ts`, `Auth.module.css`, `ResetConfirmPanel.tsx` (the one-field-state model), `frontend/lib/api.ts` (auth calls ~line 205, `ApiError` ~line 85), `frontend/lib/types.ts` (`Account`, `AuthState` ~line 388).

## RESPECT THE DESIGN — the vocabulary binds, the new state is drafted inside it

R5-1 / R12 signed the panel: one panel, mode names as title + submit verb + 전환 링크 label, one `p role="status"` line slot under the form where 오류 (`--ink-1`) and 알림 (`.soft`, `--ink-2`) differ by weight and **`--alert` never appears**, the submit's **own text** swapped to `PENDING_KO` and disabled while a request is in flight (no spinner, the quiet row disabled with it), `noValidate` with the gating order empty → email shape → 8자 (계정 만들기 only) → POST, 로그인됨 = 보유 종목 (a navigation, not a state), the 로그아웃 band above the title with P12.F5's pre-hydration reservation. **Change none of it.** The new state reuses the existing classes (`.head/.title/.intro`, `.field/.labelRow/.label/.input`, `.submit`, `.line/.soft`, `.quietRow/.quiet`); add CSS only if the numeric field genuinely needs one rule (e.g. `font-variant-numeric: tabular-nums` / letter-spacing on the code input), in the panel's existing tokens, and say so. No new route, no new component file unless the panel's own file becomes unreadable — a third `mode` inside `AuthPanel` is the expected shape.

## Build

- **`frontend/lib/api.ts`** — `signup` and `login` return a union (`{ account: Account } | { verification_required?: true; verification: Verification }` — signup never carries `account`; branch on the **key**, never the status), new `verifySignup(email, password, code)` → `{ account }`, `resendVerification(email, password)` → `{ resent: boolean; verification: Verification }`. `Verification = { email: string; expires_at?: string }` in `lib/types.ts` (`expires_at` optional, never null). CSRF header unchanged (it is set for every unsafe method already).
- **`frontend/components/auth/copy.ts`** — a `## P13` section in the header in the R12 pattern, every new constant cited `drafted P13 — approved literally at the P13 gate`: the state's title, its intro line (it names the **normalized** address from the response and the ≈10분 window), the field label (인증번호), the submit verb (확인), the 재전송 label, the way-back label if it is not simply the origin mode's own name, and the line slots: 재전송됨 (soft), the cooldown answer for `resent: false` (soft — "the code already sent is still the one to type", no timer), 인증번호 불일치 (`verification_code_invalid`), 만료·시도 초과 → 재전송 안내 (`verification_code_expired`), and one client-side line for an empty / non-6-digit code before any request (R12's own grammar: 빈 입력 answers in the slot). Add both new codes to `authErrorKo` (an unmapped code renders nothing — that is the trap). **Re-draft `SIGNUP_INTRO_KO`**: 「… 만들어지면 바로 로그인됩니다.」 is false under the gate; keep it one sentence in the round's register and cite the reason. Do not touch any signed string other than that one.
- **`frontend/components/auth/AuthPanel.tsx`** — `mode: "login" | "signup" | "verify"` with the origin mode remembered (so the way back and the intro read right); on a signup 201 or a login `verification_required` → `verify` mode, **keep `email` (set it to the normalized address from the response) and `password` in state** (the verify and resend calls need both), reset `code` to `""`, clear the line, focus the code field. In `verify` mode the form renders one field (`id="auth-code"`, `type="text"`, `inputMode="numeric"`, `autoComplete="one-time-code"`, `maxLength={6}`, string value so leading zeros survive), the submit 확인 (→ `PENDING_KO` while pending), the quiet row with 재전송 and the way back (both `disabled` while pending). Submit: client gate (empty / not 6 digits → the drafted line, no request) → `verifySignup` → on success the existing 로그인됨 path (`router.push(ROUTES.portfolio); router.refresh()`) untouched; on `ApiError` → `authErrorKo(code)` (so `invalid_credentials` still renders 불일치 if the password stopped matching, e.g. a re-signup elsewhere). 재전송: `resendVerification` → `resent` true → 재전송됨 (soft); false → the cooldown line (soft); on `ApiError` → `authErrorKo`. The way back returns to the origin mode with fields kept and the line cleared. Update the header comment: add a section for the third mode in the file's own voice — what the state is made of, why the password travels with the code (S1's reason), why it is not a route — without rewriting R12's sections.
- **`frontend/app/auth/login/page.tsx`** — unchanged (an authenticated visit still redirects; an unverified account never has a cookie, so it never fires for one).
- Nothing in `components/chrome`, `lib/session*.ts`, the reset panel, `/ops`, or the backend.

## Validate — typecheck, smoke tests, then the real browser in both runtimes

1. `cd frontend && npm run typecheck && npm run smoke`.
2. **Real browser: Aside on the agent account `u2`** — `aside repl --account u2 "<js>"`; never `u0`, never `aside account use`, never `aside profile list`. Read the sharp-edges note before the first call. Runtime per `## Operator Runtime`: the dev stack (`make stack-status` — API `http://127.0.0.1:8010`, `next dev` `http://127.0.0.1:3010`), viewports **1280×800 and 390×844**, **and** the local production build on **3014** (recipe in the notes; `MIJUAL_API_ORIGIN=http://127.0.0.1:8010`; stop it when done). Codes are read from `var/stack/api.log` (`[mail:signup_verification] to=<address> code=<6 digits> …` — the last line for that address). Use throwaway addresses on a fake domain (e.g. `p13-s2-<n>@mijual.test`) and **delete every account you create** at the end: verified ones through `DELETE /auth/account` with their cookie, unverified ones directly in dev Postgres (`docker exec mijual-postgres psql …`, credentials from the app's `DATABASE_URL`, never hard-coded). The sweep, in each runtime, at both viewports where layout matters:
   - **Signup → code state.** `/auth/login` → 계정 만들기 → address + password → submit → the panel is in the code state: title, intro naming the **normalized** address, one field focused, 확인, 재전송 and the way back; **no cookie was set** (`document.cookie` shows nothing and `GET /auth/me` says `authenticated: false`); the page did not navigate.
   - **Wrong code** → the 불일치 line in `--ink-1`, no navigation; **empty / 3-digit code** → the client line, no request (watch the network or the API log).
   - **재전송 inside the cooldown** → the soft cooldown line (`resent: false`), nothing mailed (the log shows no new line). If time allows, back-date the live grant's `created_at` by 61 s in dev Postgres and press 재전송 again → 재전송됨 and a new code in the log; otherwise say the past-cooldown branch is covered by S1's test only.
   - **Right code** → lands on 보유 종목 (`/portfolio`), the chrome's account slot shows the address, `GET /auth/me` authenticated. Then 로그아웃 → the 로그아웃되었습니다 band renders with P12.F5's reservation intact (no layout jump on reload — the band's slot is pre-reserved; check `data-mj-auth-flash` behaviour did not change). Then 로그인 with the same credentials → straight to 보유 종목 (a verified account never sees the code state).
   - **Login route in.** Sign up a second address and do **not** verify; reload `/auth/login` (state is lost — expected); 로그인 with the right password → the code state again with 재전송; wrong password → 불일치 unchanged. Enter wrong codes until the cap: the **5th** wrong code renders the 만료·시도 초과 line, not 불일치, and 재전송 (past the cooldown by then, or back-dated) issues a new code that then verifies.
   - **Unchanged paths hold**: 로그인 with a wrong password → 불일치; 비밀번호 재설정 → 「재설정 링크를 보냈습니다 …」; the empty-fields and bad-address lines; the sample entry below the panel; nothing about the reset page.
   - **390**: the code state fits the panel, no horizontal scroll, the field and button geometry match the other two modes (measure the panel width and the submit height against 로그인 mode — same values).
   - **Production build (3014)**: the signup → code → verify → 보유 종목 flow once at 1280, plus the 390 code state; dev and production must agree (the P7 rule: same behaviour, only speed differs).
   Screenshots of the code state at both viewports go under `var/p13/` (outside the slice folder), referenced by path in `result.md`. Name the instrument you used, and never claim a run you did not make.
3. `python3 scripts/workflow.py validate`.

## `phase.md`

Consume every `for P13.S2` note. Append **Doc impact** lines: `frontend.md` (the panel's third mode and what each state is made of; `lib/api.ts`'s union returns and two new calls; the code field's attributes), `experience.md` (인증: 계정 만들기 now ends in the code step; 로그인 on an unverified account routes there; the 재전송 option), `security.md` only if you changed a security-relevant client behaviour beyond S1's note. Add a note **`for P13.REVIEW`** listing **every drafted P13 Korean string verbatim** — the mail's four constants from `src/mijual/mailcopy.py` and every panel constant you added or re-drafted — so the walkthrough can carry them without re-collecting. Add a note `for P13.S3` only if you learned something about the production build that the release must know. Rewrite `## Now` last.

## `result.md`

Verdict block first, then: the drafted strings verbatim with one line of reasoning each, the sweep log per runtime × viewport (what you did, what you saw, measured values), deviations, dead ends, the cleanup you performed. Do not commit; do not run any state command.
