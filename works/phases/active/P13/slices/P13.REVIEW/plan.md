# P13.REVIEW — review of P13 "Email verification at signup (6-digit code)" (gated: `acceptance.required: true`, never opened)

You are `slice-executor-high` executing the phase review. The contract is `.claude/skills/review-phase/SKILL.md` — read it first and follow it to the letter; this plan adds the P13-specific facts, order, budgets and hard rules. You are not the last word: on a `pass` you return a **`walkthrough`** and the orchestrator opens the operator acceptance gate. You never run `accept-gate`, `defer-job`, `review-phase`, `docs-consolidated`, `git commit` or `git push`. Written by the orchestrator in `auto` mode after `P13.S3`.

## What P13 set out to do (judge against this)

`works/phases/active/P13/intent.md`, confirmed 2026-09-05: registration is a **hard gate by a 6-digit code** — signup creates the account unverified, mails a single-use code and opens no session; the panel switches to a code-entry state **on the same surface**; the right code verifies and logs in; login with the correct password on an unverified account routes to the same step with 재전송; existing production accounts are **grandfathered**; **no design round** — the new Korean is drafted in the R5/R12 vocabulary and the operator approves every string **literally** at the gate; the verification mail is **proven live on production**. Six recorded assumptions (10-minute code, a handful of attempts then dead, re-signup re-sends rather than 중복 가입, a completed reset verifies, three mail kinds stated deliberately, the schema path — corrected by DECOMP: no Alembic, `ensure` adds a nullable column and NULL means verified). Out of scope: `/ops`, verifying existing accounts at next login, soft gating, a design round.

Slices `done`: `P13.DECOMP`, `P13.S1` (backend), `P13.S2` (frontend + the Aside sweep), `P13.F1` (the attempt cap was inert over HTTP — the increment was rolled back with the 400 — fixed by committing the failed attempt before the error travels; the three test fixtures now roll back like production), `P13.S3` (release + live proof). Production runs `origin/main` = **`1899111`** (released by `P13.S3` 2026-09-06 01:47→01:49 KST; rollback point per its `result.md`). The deploy freeze opens **2026-09-07 11:00 KST** — nothing in this slice deploys, and a fix slice you propose must say whether it needs a deploy before 09-07 11:00 or waits for 09-12.

## Read (in this order, just in time)

1. `CLAUDE.md`; `.claude/skills/review-phase/SKILL.md`.
2. `python3 scripts/workflow.py next`; `works/phases/active/P13/phase.json` (`acceptance.required: true`, never opened).
3. `works/phases/active/P13/intent.md`; `works/phases/active/P13/phase.md` **whole**: `## Decisions` (the schema, the grant with its numbers, the API contract as built, the three-kind mail rule, the copy route, the instrument and runtime, the release), `## Doc impact` (append-only — **this is the list you verify**; count the lines and name each doc), `## Operator Questions` (six entries, one of them F1's answer to S2's), `## Notes for later slices` (**the notes tagged `for P13.REVIEW`** — DECOMP's routing note, S2's verbatim list of every drafted Korean string, S3's production-state note — were written for you), `## Now`.
4. Every slice's `slice.json` and `result.md`, **head-first** (verdict block), whole where the detail matters: S1 § *Step 4* (the HTTP bodies), S2 § *Finding 1* and § *The drafted Korean, verbatim* and both sweep sections, F1 § the red run and the write-path audit, S3 § the release table and the proof.
5. `docs/current/operations.md` `## Operator Runtime` (present and filled; stale on one point — it says Aside's daemon does not run here; since 2026-09-03 the instrument is Aside on `u2` and P4 owes that note — not a halt); `docs/current/qa.md` `## Regression Checklist` (**154 `- [ ]` lines** through the P12 block) — read it whole, you re-run all of it. `docs/current/security.md` § *Auth Model* and `docs/current/experience.md` (인증 bullets) only to judge the doc-impact coverage; every doc P13 touched is behind the code **by design** (the docs-phase deferral).

## Hard rules (all of them, no exceptions)

- **Production: read-only plus exactly the writes this plan names.** You may read production (HTTPS GETs, browser loads, `ssh oracle-cloud` for `docker compose -f compose.prod.yml ps/logs`, `docker inspect`, `git rev-parse`, and **read-only** `psql` selects). You may **not** deploy, rebuild, restart or stop any container; touch `edge-nginx`; edit anything under `/home/opc` or `.env.prod`; log in to `/ops`; create an account on production **other than** re-using the proof address S3 already registered (see Stage C — a re-signup on that unverified address is the one production write allowed, and only if the cooldown reasoning in S1's contract makes it harmless); send feedback. Signed-in and cap-exhausting flows run **on dev only**, with throwaway accounts on a fake domain, deleted afterwards (verified ones through `DELETE /auth/account`; unverified ones in dev Postgres).
- **Secrets.** Never print, quote or store a secret value. The repo `leetusik/Mijual` is public: `result.md`, `phase.md`, the walkthrough and the doc sections are published the moment they are committed. The proof address is the operator's own plus-address and is already in the notebook — fine to repeat; the throwaway password S3 used is not yours to know or to print.
- **Browser instrument: Aside on the agent account `u2`** (profile 「claude2」) — every invocation `aside repl --account u2 "<js>"`; never `u0`, never `aside account use`, never `aside profile list`. The sharp edges are in `works/phases/active/P12/phase.md` `## Decisions` and P13's S2 result (tabs die between invocations, cookies persist **and ignore ports**, module scripts end with `console.log`, one `evaluate` argument, ~80 s per invocation, one `screenshot()` per invocation). Viewports **1280** and **390**, plus **412×915 @ DPR 2.625 / 4× CPU / ≈1.6 Mbps / 150 ms** for the checklist's cold-cache lines. Say in `result.md` which instrument you used; never report a walk you did not make.
- **Model-call budget:** the checklist's AI 질문 lines need real turns — cap **6** on dev, **1** on production; state the count spent.
- **No source code edits, no fixes, no `docs/current` hand-edits, no `docs/versions` patches.** The only files you write: `result.md`, `phase.md`, and — on a pass only — the two named sections via `doc-new-version`. Notebook-only findings are the one thing you may close yourself.
- Leave the machine as you found it: the operator's dev stack **up** (`make stack-status` as found), every server you started stopped, no stray build copies serving, every throwaway account gone.

## Runtime: three environments

| environment | how | what runs there |
|---|---|---|
| **dev** | `make stack-status` → API `127.0.0.1:8010` + `next dev` `127.0.0.1:3010`. Confirm it serves the current tree (restart with `make stack-down && make stack-up` if the API pid predates F1's commit `12fbdbe`). Codes: `var/stack/api.log` (`[mail:signup_verification] to=… code=…`). | every checklist line; every signed-in line; the whole verification flow including the 5-attempt cap, the cooldown, the login route in, the reset-verifies path (a reset link is also in that log). |
| **production build** on **3014** | a copy of `frontend/` outside the repo (never build into `frontend/.next`), `NEXT_PUBLIC_SITE_URL=https://jujutower.com MIJUAL_API_ORIGIN=http://127.0.0.1:8010 npm run build`, stage `.next/static` + `public/` into `.next/standalone/`, `node .next/standalone/server.js`. Sign out of 3010 first — cookies ignore ports. | the code state at 1280 and 390 once, and any line where dev and production could differ. |
| **production** `https://jujutower.com` (`1899111`) | read-only plus the one named write. | the headline claims (Stage C), every anonymous checklist line, the R7 no-harm assertions. |

## Stage A — validate all slices together

Re-run each slice's validation from its verdict block (`plan.md` as fallback), collapsed into one run each; record every command and outcome:

- `.venv/bin/pytest -q` → **171 passed** (S1 + F1's count). `cd <frontend copy> && npm run build && npm run typecheck && npm run smoke` → green (23/23 per S2).
- `.venv/bin/python -m mijual.db ensure` on dev → `schema ok` (idempotent, no column added).
- The S1 / F1 HTTP flow once over `127.0.0.1:8010` with the CSRF header: signup 201 with no `Set-Cookie` and no code in the body; login → `verification_required` and no cookie; wrong code ×4 → `verification_code_invalid`, the 5th → `verification_code_expired`, the row `attempts = 5`; the real code afterwards → expired; resend inside the cooldown → `resent: false`; back-date `created_at` 61 s → `resent: true`, new code, verifies → cookie; `GET /auth/me` authenticated; a completed reset on an unverified account verifies it (request a reset, read the link from the log, confirm, then `/auth/me`). Delete the accounts.
- `make smoke-prod` → **17/17** (a red `www` line from this Mac can be MagicDNS — re-check with `--resolve` and count it as a known local false FAIL; any other red line is a finding).
- On the box (read-only): six services up, `mijual-schema` exited 0 with `schema ok (+1 columns)` in its log; `/home/opc/Mijual` at `1899111`; `select count(*) from pg_stat_user_tables` = **20**; `mail transport: smtp …` in the API log; the `smtp mailer: sent signup_verification` line S3 recorded; the proof account's row as S3 left it (`verification_pending_since` not null, `attempts` = what S3 recorded).
- `python3 scripts/workflow.py validate` (the `consolidation_owed=P4, P12`, `stale_docs=…`, `oversized_doc_sections` warnings are pre-existing).

## Stage B — judgment, cross-check, doc-impact coverage

- Did each slice meet its brief and plan, with deviations explained? Judge in particular: **DECOMP**'s NULL-means-verified column against `ensure_columns`' refusal rule and against assumption 6's wording; the **API contract** (password-first `verify`, the re-signup-replaces-the-hash rule and the race it closes, the cooldown outranking every caller, `expires_at` absent-never-null, malformed code costing an attempt) — is each decision argued from the code and does the built product match `## Decisions` line by line?; **S1**'s `start_session` refusing an unverified account (grep every caller), the `_stored_utc` SQLite/Postgres seam, the three-kind rule stated in **both** docstrings, `Account`'s docstring naming the column as state; **S2**'s drafting discipline (every new string cited `drafted P13 — approved literally at the P13 gate`; the way-back label borrowed from the mode names; `--alert` absent; the P12.F5 reservation intact; the client six-digit gate sending nothing), and whether `SIGNUP_INTRO_KO`'s re-draft keeps R5's first clause verbatim; **F1**'s one service-layer commit (is the docstring's justification right? does the audit table cover every raise-after-write in the module? is the fixture change applied to all three files?); **S3**'s release (both gates checked, the schema line, the grandfathering count captured **before** the signup, the SMTP line, the state production was left in, the freeze respected, no edit on the box).
- **Cross-check the notebook against the logs:** a decision or constraint recorded in any `result.md` that appears nowhere in `phase.md` is a finding; so is a durable-truth change a log describes with no `## Doc impact` line. Candidates: `_stored_utc` (backend), the `start_session` guard (backend/security), the `.code` CSS rule and the `AuthResult` union (frontend), `authErrorKo`'s two new codes (frontend), the three fixtures' rollback shape (backend/qa), the box's table count 19 → 20 and the first additive column on production (operations/data), the `## Operator Runtime` staleness (confirm it is on **P4's** list, not silently missing everywhere), the F1 copy corner (the cooldown line on a cap-killed grant — an `## Operator Questions` entry, and the frontend note should say the line is drafted and may change).
- **Notebook-only findings you close yourself** (append to `phase.md`, tag `(P13.REVIEW)`, report as closed). Product/code/deploy findings become numbered findings with a proposed fix slice (`P13.F2`, …) and `changes_requested`.
- Orphaned design routes: none expected (no design round) — confirm (`app/**/mock*`).
- **The six `## Operator Questions` + the review notes** — build the routing table (Stage D consumes it): each entry is **walkthrough decision** / **deferred job (title · reason · trigger)** / **answered — nothing outstanding**. The last deferred id is **D49**; new ones are yours to list, the orchestrator files them. Starting points, yours to confirm: the proof address and the account's disposal → walkthrough (the operator finishes the signup on production with the mailed code, then decides delete/keep); the literal approval of every drafted string → walkthrough, verbatim, numbered; S2's cap entry → answered by F1 (one confirming line); `/ops` marking unverified accounts → deferred job candidate (trigger: the next `/ops` phase, or the first time the operator reads the table and wonders); F1's cooldown-line corner → walkthrough decision (accept as is / change the line / make the panel hide 재전송 until the cooldown elapses — each a later slice); DECOMP's janitor for long-dead unverified rows → deferred job.

## Stage C — gate stages 1–4 (the phase is gated)

1. **Manifest** — present and filled; stale on the instrument only, not a halt.
2. **Spot-check the headline claims yourself on production** (`1899111`), 1280 and 390: `/auth/login` → 계정 만들기 renders the re-drafted intro; the code state's look on production is a **dev/production-build** check (Stage A) unless the one allowed write applies — re-submitting 계정 만들기 with the **proof address** S3 registered (unverified on production) answers the same 201 and lands the code state on production without creating a second account; do it **once**, only if S3's note says the account still stands unverified, and record whether it mailed (past the cooldown it supersedes the code — say so in the walkthrough, because the operator's mailbox then holds two mails and only the newest code works). Then `GET /api/auth/me` → `authenticated: false`; the served HTML of `/auth/login` carries no code and no verification state; `/api/auth/login` with the proof address and a wrong password → `401 invalid_credentials` (one attempt, discloses nothing). Every other reader route still answers 200 uncookied (`/board`, `/stocks`, one event page) — the gate touched no other surface.
3. **Fresh-eyes walkthrough** as a first-time Korean reader, on **dev** for the signup flow (production would create an account): land → 로그인 → 계정 만들기 → read the intro → submit → the code state → mistype → 재전송 → the code from the log → 보유 종목 → 로그아웃 → 로그인 again; then on production the anonymous half (land → search → an event → the login page, no submit) at both viewports. Report everything dead, confusing or annoying, **not** judged against the design record — especially: does the intro make the reader look at their mailbox? is the way back's label understandable? what does a reader who reloads mid-code see, and is it recoverable without help? These go into the walkthrough as decisions, never into fixes.
4. **Re-run the whole `## Regression Checklist`** — all **154** lines, per *Runtime*; results as a table in `result.md` (line → dev / prod-build / production, one-word result, a note where not a clean pass; a line whose precondition no longer exists is recorded as such). Account-bound lines run on dev with a throwaway account **that you verify through the new flow** — the checklist's signed-in lines are now themselves a test of the gate. Then compose the **P13 block** to append, in the shipped shape `- [ ] <surface>: <one observable behaviour> (P13)`: signup opens no session and mails a code (the code never in a response body); the panel's code state on the same surface with 확인 / 재전송 / the way back; five wrong codes → the 5th answers 만료 and the mailed code is dead; 재전송 inside 60 s answers the 알림 line and mails nothing; login on an unverified account routes to the code state; a completed reset verifies; every pre-existing account logs in unchanged (the grandfathering count); the production SMTP line — with the counts and the runtime you ran them in, plus the instrument note in the P12 block's shape if anything new about Aside was learned.

## Stage D — route every operator question; build the walkthrough

Write the routing table into `result.md`. Then the **walkthrough** — the script the operator runs. Constraints: English prose, Korean product strings verbatim; **≤ ~80 lines**; no secret values; numbered so the operator can reply "1 ok, 3c change X". Shape:

0. **Already verified by the reviewer today** — one paragraph: tests, the HTTP flow, production's schema line and grandfathering count, the SMTP line, `make smoke-prod`, what you opened on production.
1. **Finish the live proof yourself (≈3 min).** S3 left the proof address `leetusik+p13@gmail.com` registered on production **unverified, with all five attempts unspent**, and its throwaway password deliberately outside this public repo. The operator therefore finishes it without any secret: at `https://jujutower.com/auth/login` → 계정 만들기 with that address and **a password of their own** (the re-issue branch replaces the stored hash with the one just typed and, past the 60 s cooldown, mails a fresh code — the **newest** mail in the inbox is the one that works; the earlier ones are superseded), enter the code → 보유 종목. Say how many mails the inbox will hold (S3's sends plus any you made in Stage C) so the operator is not confused by the older ones. Then 계정 삭제 on `/portfolio/notifications`, or keep it — their decision (item 4).
2. **Open it and look (≈5 min)** — the code state at desktop and on a phone: 계정 만들기 intro, the state's title and intro, 확인 → wrong code → the line, 재전송 → the line, the way back.
3. **Approve the strings, literally** — every drafted P13 string numbered, verbatim (from S2's `for P13.REVIEW` note: the mail's subject and three body lines, the panel's constants, the re-drafted 계정 만들기 intro), each with "ok / change to: …".
4. **Decisions, to take literally** — the proof account's disposal; F1's cooldown-line corner; `/ops` marking unverified accounts (deferred unless they say now); the janitor (deferred unless they say now); anything Stage 3 surfaced.

## Stage E — verdict, and the two named writes on a pass only

Form the verdict from the complete picture, then branch. On **`pass`**: (a) verify the `## Doc impact` list covers every durable-truth change (Stage B's candidates) and report `doc_versions: none — deferred to a docs phase`; (b) write the two named sections: `python3 scripts/workflow.py doc-new-version --doc qa --summary "P13: the signup-verification regression block" --source P13.REVIEW`, edit **only** `## Regression Checklist` in the returned `edit_path` (append the P13 block), and — only if this phase changed how the operator runs or views the product — `--doc operations` for `## Operator Runtime` (it did not: the runtime is unchanged; the Aside staleness is P4's note — so expect **no** operations version); then `python3 scripts/workflow.py rebuild-docs`. On **`changes_requested` / `blocked`**: stop before (a) and (b); return the verdict, numbered findings and proposed fix slices (`P13.F2` …, one line of scope each, and whether each needs a deploy before the freeze). Every verdict carries `explain: not written — run /explain for this phase`.

## `result.md` / `phase.md`

`result.md` verdict-block-first (`review_verdict`, `walkthrough`, `doc_versions`, `explain`, the deferred jobs to file with title · reason · trigger, `files_changed`, validation, deviations), then Stage A's command table, Stage B's judgment and cross-check, the checklist table, the routing table, the walkthrough verbatim. `phase.md`: consume the `for P13.REVIEW` notes, append your `## Doc impact` line for the qa version you wrote, close notebook-only findings in place, rewrite `## Now` (≤ 15 lines) as the handoff for the operator's gate.
