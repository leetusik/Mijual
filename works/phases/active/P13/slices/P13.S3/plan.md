# Plan — P13.S3 (implementation, `slice-executor-high`)

**Release P13 to production and prove the verification mail live.** After this slice, `https://jujutower.com` runs the P13 gate: the schema one-shot has added `account.verification_pending_since` and created `email_verification`, every pre-existing production account is verified (the grandfathering count, captured **before** anything else writes), a real signup on production delivered a real 6-digit code through `mail.privateemail.com:587`, that code opened a session on production, and `make smoke-prod` is green. Same shape as P4.S4 / P4.S9 / P4.S10 / P12.S2 — read `works/phases/active/P4/slices/P4.S10/result.md` § the release table and P4.S4's § D16 (the reset-mail proof) for the exact commands that worked.

**Read first:** `works/phases/active/P13/phase.md` — `## Decisions` (*Release*), `## Notes for later slices` tagged `for P13.S3` (the two gates, the proof's parts, the schema evidence) and `## Operator Questions` (the plus-address proposal). `docs/current/operations.md` § *Operator Runtime* → *Production* (origin, access path, logs, the `.env.prod` rule, the freeze). `deploy/runbook.md` R3 (the release command) and R6 (post-deploy checks) and R7 (rollback). `deploy/deploy.sh`'s header for the `REF` / `PROJECT` knobs.

## Two gates at the very start — each is a `needs_operator`, never a workaround

1. **The push.** `git fetch origin && git merge-base --is-ancestor HEAD origin/main`. `deploy/deploy.sh` releases `origin/main` from the box's clone; **pushing `main` is the operator's** and you may not push, bundle, scp, or otherwise put unpushed commits on the box (a box checkout that is not on `origin/main` is a rollback waiting to happen at the next release). If HEAD is not on `origin/main`, return `needs_operator` — say exactly which commits are unpushed (`git log --oneline origin/main..HEAD`) and that a `git push origin main` from the operator resumes this slice — and stop.
2. **The freeze.** `TZ=Asia/Seoul date`. Nothing deploys **2026-09-07 11:00 → 2026-09-11 23:59 KST** (`deploy/runbook.md`; `rollback.sh` excepted). Inside the window, return `needs_operator` naming 2026-09-12 as the earliest release and stop. Before it: proceed, and note the time you started.

If both gates are open, everything below runs without a further stop.

## Steps (P4.S10's shape; every box command over `ssh oracle-cloud`, never an edit on the box)

1. **Pre-release evidence.** `make smoke-prod` green **before** the deploy (so a red check afterwards is attributable). On the box: `git -C /home/opc/Mijual rev-parse --short HEAD` (expected: the P12 release `004d936` or later), `docker compose -f compose.prod.yml ps`, and the table count (`select count(*) from pg_stat_user_tables` — expected 19).
2. **Release.** `ssh oracle-cloud 'cd /home/opc/Mijual && nohup deploy/deploy.sh > var/deploy-<UTC stamp>.log 2>&1 < /dev/null &'`, then poll the log (`tail`) until `DONE — released at ref origin/main` or a `ROLLBACK`; `grep -c ROLLBACK` must be 0. This release rebuilds **both** images (backend changed), so expect the API and worker/beat to recreate, not only the web container. Then `docker compose -f compose.prod.yml ps` — six services up, `mijual-schema` exited 0 — and `docker compose -f compose.prod.yml logs mijual-schema` must show **`schema ok (+1 columns)`** (the table came from `create_all` in the same run; the count of user tables is now **20**).
3. **The grandfathering count, before anything else writes:** `docker compose -f compose.prod.yml exec -T mijual-postgres psql -U mijual -d mijual -tAc "select count(*), count(*) filter (where verification_pending_since is not null) from account;"` — the second number must be **0**. Capture it verbatim; it is unrepeatable once you sign up. Also `logs mijual-api | grep 'mail transport:'` must still read `smtp mail.privateemail.com:587 …`, never `console`.
4. **The live proof.** From this Mac, through the public origin, with the CSRF header: `POST https://jujutower.com/api/auth/signup` with the operator-confirmed proof address — the `## Operator Questions` proposal is **`leetusik+p13@gmail.com`**, unconfirmed; the operator asked for no stop before the review, so **use that address** and record in `result.md` that the choice is theirs to overrule at the gate — and a throwaway password you keep only in your session. Expect `201 {"verification": {...}}` and **no `Set-Cookie`**. On the box: `logs mijual-api | grep 'smtp mailer: sent signup_verification'` — the line that proves the mail left (per the secrets rule the log carries no address and no subject; do not go looking for one). **You cannot read the operator's inbox, so the code itself is the operator's to enter** — leave the throwaway account **unverified and standing**: the review's gate walkthrough will ask the operator to open the mail and finish the signup on `https://jujutower.com/auth/login` themselves, which is a better proof than any you could make. Then prove the rest of the gate without the code: `POST /api/auth/login` with the right password → `200 {"verification_required": true, ...}` and no cookie; with a wrong password → `401 invalid_credentials`; `POST /api/auth/verify` with `code: "000000"` → `400 verification_code_invalid`; `POST /api/auth/verify/resend` inside the cooldown → `{"resent": false, ...}`. (One wrong attempt is spent — say so; four remain for the operator.)
5. **Existing accounts still work.** You may not log in as the operator. Prove it structurally instead: the count from step 3, and one **read-only** check that `POST /api/auth/login` for a **nonexistent** address is still the uniform `401` — nothing more is provable without a credential you do not hold. Say so plainly.
6. **Post-release smoke.** `make smoke-prod` — 17/17 (or whatever the suite's current count is) — and the R7 no-harm assertions the suite carries. Open `https://jujutower.com/auth/login` once in Aside on account `u2` (`aside repl --account u2 …`, 1280 and 390): 계정 만들기 → the code state renders on production **without** creating a second account (use the same proof address — re-signup on an unverified address answers the same 201 and, inside 60 s, mails nothing; outside it, supersedes the code — either is fine and you say which happened). Screenshot to `var/p13/`.
7. Record the release in `result.md`: box HEAD before/after, the deploy log path, the schema line, the grandfathering count, the SMTP log line (verbatim, it carries no PII), the four API bodies from step 4, the smoke result, the Aside observation — and the exact state you left production in (one unverified account at the proof address, N attempts spent).

## Never

- push, bundle, or scp code to the box; edit anything on the box; touch `.env.prod` (read it by path only if a value is needed, never paste it); run `rollback.sh` unless the deploy itself demands it (then say so and stop); log in to `/ops`; read or guess the operator's password; create any account other than the proof address.

## Validate (verdict block)

- The two gates' outputs; `make smoke-prod` before and after; `deploy.sh` `DONE` + `ROLLBACK` count 0; `mijual-schema` `+1 columns`; the account count/pending count; the `smtp mailer: sent signup_verification` line; the step-4 bodies; the Aside observation; `python3 scripts/workflow.py validate`.

## `phase.md`

Consume the `for P13.S3` notes. **Doc impact:** `operations.md` (Deployment: P13 released at `<sha>` on `<KST time>`, both images rebuilt, the schema one-shot's first additive column on production and the table count 19 → 20; the freeze respected), `qa.md` (the production evidence for the gate; a regression line the review should carry — signup on production opens no session and mails a code), `security.md` only if production revealed something S1 did not record. Add a note `for P13.REVIEW`: the proof address, that its account stands **unverified** on production with N attempts spent so the operator's walkthrough must include opening the mail and entering the code (and what to do with the account afterwards — delete via `DELETE /api/auth/account` once verified, or leave it), plus the exact production URLs to open. Rewrite `## Now` last.

## Reconciled after P13.F1 (added by the orchestrator at planning time)

- **P13.F1 landed before this slice**: the 5-attempt cap is real over HTTP, so the release carries no known defect. Step 4's one wrong attempt therefore spends **1 of 5** on the proof account — say so in the walkthrough note (four remain for the operator).
- **Cookies ignore ports** (S2's note): if you open a second local origin during any check, sign out of the first or `/auth/login` will redirect to `/portfolio` and look broken. On production this does not arise.
- **Status at planning, 2026-09-06 00:53 KST:** the push gate was **closed** — HEAD `12fbdbe` is 8 commits ahead of `origin/main` `004d936` (production still runs the P12 release). The orchestrator set this slice `pending` on that fact without dispatching; **on resume, re-run both gates first** — the operator's push may have landed, and the clock may have crossed the freeze.
