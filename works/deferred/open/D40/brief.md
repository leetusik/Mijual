# Deferred: D40 Decide the 정정 해석 thinking preset (D-4)

## Context

## Why Deferred

The backlog prices at 69 calls against extract_max_calls=60 per run and the level is undecided; production is now the first unattended worker.

## Trigger to Promote

The first production run whose extract stage hits the call ceiling, or before any backfill.

## Notes

