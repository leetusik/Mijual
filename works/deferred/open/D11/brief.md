# Deferred: D11 Serve the 집계 범위 dates on a stockless read (Q32b)

## Context

## Why Deferred

lapse.coverage rides GET /stocks/{corp_code} only, so /stocks with no query states the boundary sentence with no dated rows under it.

## Trigger to Promote

When the entry page should date its own boundary

## Notes

