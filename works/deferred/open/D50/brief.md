# Deferred: D50 /ops 독자 계정 table — mark or hide unverified accounts

## Context

## Why Deferred

After P13 the /ops 독자 계정 table silently lists accounts that cannot log in (unverified signups) and nothing on the surface says so; every /ops change was excluded by P13's intent.

## Trigger to Promote

The next /ops phase, or the first time the operator reads that table and cannot tell a pending signup from a real reader.

## Notes

