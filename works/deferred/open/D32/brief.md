# Deferred: D32 API가 죽으면 랜딩(/)이 500 — 영문 오류 화면

## Context

## Why Deferred

With the API down the landing route returns a 500 rendered in English, while /ask degrades gracefully to its static fallback. Pre-existing, not introduced by P11, but it is the first page a visitor meets.

## Trigger to Promote

Next landing work, or P4 deployment hardening

## Notes

