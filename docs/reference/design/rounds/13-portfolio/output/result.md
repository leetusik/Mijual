# R13 result — 보유 종목 + 알림 설정 (P8.S12)

Round **R13** · slice `P8.S12` · apply slice `P8.S13` · group `⏳ P8.S12 · Portfolio`.
**No token changed** (`foundations/tokens.css` stays signed/frozen, R8).
Walk provenance as stated in the handoff: SSR markup + module source + P7's measured
geometry. Account-only states were **drawn from the states §2.14 lists**, not walked —
`P8.S13` verifies them in the operator runtime.

## Outputs

`portfolio/r13-portfolio.css` (geometry canon, tokens only) · `portfolio/r13-parts.jsx`
(parts + every string) · cards `Home.html` · `Sample.html` · `Notifications.html` ·
`Mobile.html` · this file · `build-prompt.md`.

## §2b decisions, as taken

| | decision |
|---|---|
| **Q-A** | 챙겼습니다 행은 **지나간 마감에 남는다** (기본값). R5-8이 같은 행·같은 숫자에 라벨+색만 서명했으므로 행을 없애면 서명된 라운드를 덮어쓴다. |
| **Q-B** | ~~유지~~ → **체크되면 링크도 사라진다** (이번 세션 운영자 개정, 아래 §7). |
| **Q-C** | `carryOverKo`의 「이 세션에 남아 있습니다」 **그대로**. 계정 캡션도 **「본인 표시 · 계정에 저장」 유지** (폼에서 미선택 → 기본값 적용; 표시가 계정을 따라간다는 것은 독자 자신의 사실이다). 샘플은 P7이 다듬은 「본인 표시」. |
| **Q-D** | **리셋/종료 컨트롤은 돌아오지 않는다.** 영구 브라우저 편집을 수용한다 — R5-4의 「종료」 조항은 이로써 공식 회수, `clearSample()`은 계정 이전 전용. 결과(샘플에서 지운 행은 그 브라우저에서 복구 불가)는 `build-prompt.md` §회귀에 사실로 남긴다. |
| **Q-E** | **R12 밴드는 익명 샘플 보유 종목에 렌더한다** — 자리는 지나간 마감 **다음**, R12 서열 규칙 하에서 (게이트 아님 · 닫기 · 세션당 1회 · 숫자 위 금지). |

## §6 열린 질문에 대한 답

1. **D-day 행이 1440에서 「정리됨」을 얻는 방법 (finding 1 — 표제).** 표가 아니라 **내용과
   무관한 네 트랙**: `84px · minmax(0,1fr) · 212px · 208px`, 두 섹션의 모든 행이 공유,
   열 머리글 없음·세로선 없음. 행 **본문**도 같은 트랙에 앉는다 — 모든 블록은 열 2에서
   시작하고 모든 금액은 마지막 트랙에서 끝나므로, 소멸 금액의 오른쪽 edge가 위
   카운트다운과 같은 선에 선다. 표면 폭은 제품의 960(R11 `.page`)으로 고정: 1440에서
   측정된 584.6–761.3px의 빈 가운데 중 ~480px는 이것만으로 사라지고, 남은 폭은 「두 블록
   사이의 간격」이 아니라 종목명의 열이 된다. 144.7px 들쭉날쭉한 왼쪽 edge는 트랙이
   고정이므로 구조적으로 불가능해진다.
2. **빈 권리 셀 (2).** 파선 하이라인 56px — R10/R11이 이미 쓰는 「값 없음」 표시. 파선
   *상자*는 이 시스템에서 컨트롤(`.prompt`/`.restore`)이므로 쓰지 않았다. **새 카피 0건**,
   행 리듬 유지.
3. **앵커 줄 (3).** **페이지(=블록) 레벨, 한 번.** 지나간 D+n까지 전부 같은 서버
   `reference`로 계산된 값이다. 다가오는 마감 아래에 두면 그 섹션의 속성으로 읽히고, 두 번
   쓰면 기준이 둘인 것처럼 읽힌다.
7. **종료/리셋의 집** — Q-D에 따라 없다 (위 표).
8. **샘플이 전환 사다리에 합류하는가** — 합류한다 (Q-E). 다만 R12의 리드 줄 「이 보유량은
   탭을 닫으면 사라집니다」는 **이 표면에서 거짓**이므로 쓰지 않는다: 샘플 편집은
   `localStorage`에 남는다. 밴드는 리드 없이 R12의 본문 + CTA + 닫기로 선다.
12. **알림 페이지의 프레임** — 레일 한 줄 「← 보유 종목」(화살표 + 서명된 층 이름) +
    `h2` → **`h1` 승격**. 둘 다 새 카피 없이.

## 나머지 findings

- **4 (Q-B)** 유지 — 위. **5** 캡션은 **항상** 렌더 (조건부로 만들면 클릭마다 22.6px 점프).
- **6 (Q-A)** 유지 — 위. **7 (Q-D)** 수용 — 위. **9** 로그인 페이지 「실제 공시 4건」은
  **변경하지 않음**: 4는 공시 건수이고 사실이다 (4건 → 다가오는 2 + 지나간 3 = 5행).
  카피를 새로 만들 값이 아니며 `P8.S13` 회귀 항목으로만 남긴다.
- **10 (Q-C)** 유지 — 위. **11** 480px 두 블록 **회수**, 단일 767 경계 + `.m390` 미러.
- **13** R12의 `invalid_email` 매핑 **물림을 원한다**: 이전에는 조용한 실패였다. 규칙은
  R12 그대로 — 본문 잉크 한 줄, 필드 테두리 무변, 빨강 금지.
- **14** 전부 그렸다: 행 편집 · 8초 되돌리기(되돌린 행은 새 보유이므로 목록 끝) · 빈
  보유 종목 · 세션 이월 · 계정 이전 · `?add=` 프리셋 · 시점 칩(빈 선택 포함) ·
  KakaoTalk 무컨트롤 · 계정 삭제 제자리 무장 · busy/disabled.

## Copy — 신규 0건

이 라운드는 **한국어를 하나도 만들지 않았다.** 모든 문자열은
`frontend/components/portfolio/copy.ts`(및 그것이 re-export하는 R4/lookup/event/chrome
copy)와 R12의 `account/r12-parts.jsx`에서 전사했다. 조합 두 건은 카피가 아니라 기존 토큰의
배치다: 레일 `← ` + `PORTFOLIO_LABEL_KO`(R11/R12가 `← 관제 현황판`으로 이미 한 것), 그리고
`「」` + `PLANNED_CHIP_KO`(P5.S10 노트 4a의 인용 표기). 밴드에서 R12의 리드 한 줄은 **쓰지
않았다**(삭제 아님 — 이 표면에서 거짓이라 미사용).

## Departures / 기록해 둘 것

- `Sample.html`은 `../account/r12-auth.css`를 함께 싣고 `.aoffer`를 **그대로** 쓴다 —
  밴드가 R12의 티어라는 사실을 시트 재사용으로 증명한다(R13 시트가 마지막에 로드).
- 종목코드(`stock_code`)는 walk가 싣지 않아 **렌더하지 않았다**; 값이 없는 자리는 R11의
  `⋯` 관례(② 전환 시 주식수, ① 확정 예정일).
- 대동기어 소멸 446,720원은 배정비율·단가가 walk에 없어 캡션(`배정 n주 × 「추정」단가`)을
  쓰지 않았다 — 한화솔루션 행에만 있다. 지어내지 않았다.

## 이번 세션 운영자 개정 (2026-08-24, 게이트 이후)

1. **보유 종목 행의 카운트다운 정렬.** 진행 중인 권리 셀도 트랙을 쓴다 —
   `52px minmax(0,1fr) auto`: 칩 폭(유증/CB/매수청구)이 달라도 라벨의 왼쪽 edge와
   D-day·날짜의 오른쪽 edge가 모든 행에서 한 선에 선다. D-day 행에 쓴 규칙과 같은 규칙.
2. **지나간 마감의 줄 간격 축소.** 행 row-gap 8 → 4px, 그리고 「놓친 돈 상세 →」와
   체크박스는 같은 티어의 32px 컨트롤 둘이므로 **한 줄에 나란히** 선다(`.pdfoot`,
   gap 20px). 캡션만 그 아래. 지나간 행이 4줄 → 3줄이 되고 리듬이 조여진다.
3. **1·2는 390에서도 같다** — `.pdfoot`/`.pdcap`은 ≤767에서 `grid-column:1/-1`,
   보유 종목 카드의 카운트다운은 카드 오른쪽 edge에 정렬.
4. **용어 변경: 「포트폴리오」를 쓰지 않는다.** 독자 표면의 층 이름은 **보유 종목**(R8 nav
   slot의 이름)이다. 영향: 빈 상태 제목 「보유 종목이 비어 있습니다」, 샘플 배너 「샘플
   보유 종목 — …」, 알림 페이지 레일 「← 보유 종목」. 코드 경로·파일명·카드 그룹
   (`portfolio/`, `⏳ P8.S12 · Portfolio`)은 그대로 — 바뀐 것은 독자가 읽는 한국어다.
   `copy.ts`의 `EMPTY_TITLE_KO`·`SAMPLE_BANNER_KO`가 이로써 개정 대상이 된다
   (R5 서명 문자열의 운영자 개정 — `P8.S13`에서 함께 반영).
5. **로그아웃 · 계정 삭제 · 취소는 같은 상자 크기** (`.pact.wide`, 104px 밑바닥,
   가운데 정렬) — 같은 열의 단독 액션이 서로 다른 무게로 보이면 안 된다.
6. **계정 삭제 문장은 상시가 아니다.** 「계정을 삭제하면 이메일 주소를 즉시 지웁니다 —
   남는 것이 없습니다.」는 **첫 누름(=무장) 다음에만** 렌더한다. 지울 생각이 없는 독자가
   삭제의 결과를 상시로 읽을 이유가 없고, 무장한 독자는 돌이킬 수 없는 두 번째 누름
   **전에** 이것을 읽는다. R5의 「상시」 조항은 이로써 회수.

7. **Q-B 재결정 — 체크된 행에서 「놓친 돈 상세 →」를 없앤다.** 게이트에서는 「유지」였으나
   운영자가 이 세션에서 뒤집었다: 체크된 행은 이미 **놓친 돈이 아니라고** 말하고 있으므로
   「놓친 돈」을 부르는 링크가 그 행에 남아 있을 자리가 없다. 체크를 풀면 링크가 돌아온다
   자리도 옮겼다: 링크는 **금액 줄 안**, 라벨·기준 바로 뒤다(`.pmlead`) — 부르는 대상이 그
   줄의 숫자다. 컨트롤 줄에는 체크박스만 남는다. **깜빡임 0px**: 링크의 32px 히트 영역이
   줄의 높이를 만들고 있었으므로(사라지면 줄이 접혔다) 그 높이를 줄 자신이 갖게 했다 —
   `.pmlead{min-height:32px}`, ≤767에서 44px. 측정: 체크 → 해제 → 재체크에서 행 161px ·
   금액 줄 32px · 체크박스 top 1095 불변 (390: 249px · y 973 불변). 카피는 만들지도 옮기지도 않았다 —
   `MISSED_DETAIL_KO`는 그대로 있고, **체크된 행에서만** 렌더되지 않는다. 이로써 P7 Q8-D는
   「링크가 행의 상태와 어긋난다」는 findings 목록에서 완전히 닫힌다.

8. **지나간 행의 칩 + 날짜는 한 줄이다.** 「기간 지남 · D+45」와 「2026-07-10」이 두 줄로
   쌓이면 지배 라벨 아래에 그만큼 반칸 구멍이 생긴다(390에서 행 3의 높이 34.5 → 18.6px로
   측정). 그래서 마지막 두 트랙을 **212px / 208px**로 다시 잡아 세 종류의 칩(기간 지남 ·
   통지 마감 지남)이 모두 날짜와 한 줄에 서게 했다. 오른쪽 edge는 그대로 — 다섯 행의
   카운트다운과 두 행의 금액이 모두 같은 선(879)에서 끝난다.
