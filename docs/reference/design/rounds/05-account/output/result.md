# R5 Round Record — 개인화 2층 (`P3.S6`)

Round 5 of 7 · designed 2026-08-21 (KST) · cards under `⏳ P3.S6 · Account` (account/):
`Auth`, `Convert`, `Portfolio`, `DDayList`, `Notify`, `SampleLoad`, `ChromeSession` (added split), `AccountMobile`.

## §6 open questions → session decisions (operator: "do all as your recommendations")

- **R5-1 (개정) · 가입/로그인 = 이메일+비밀번호** — 인증 코드 방식은 operator 개정으로
  폐기. 비밀번호 8자 이상(다른 규칙 없음), 재설정은 이메일 링크. PII 패널 유지:
  "미주알이 받는 것: 이메일 주소와 비밀번호" + "저장하지 않는 것은 유출되지 않습니다".
- **R5-2 · 전환 제안 위치** — ① 조회 결과 아래(값 계산 직후, 별도 패널, 닫기 가능,
  세션당 1회) ② 상세 D-day 아래 한 줄 링크 "이 마감 알림 받기 →". nav 로그인은 R2
  서명대로 조용히 유지 — 강조 없음. 어떤 지점에서도 익명 경로 비차단.
- **R5-3 · 세션 보유량 이월** — 제안으로만: 빈 포트폴리오에 inset 행 "조회에서 입력한
  계양전기 500주가 이 세션에 남아 있습니다" + 담기/담지 않기. 자동 저장 없음 (R4-6
  restore-chip 패턴 재사용).
- **R5-4 · 샘플 진입** — 로그인 페이지 하단 + 랜딩 푸터 한 줄. 로드 상태 = 2층 표면
  상단 inset 배너 + nav 「샘플」 칩 + 샘플 종료 버튼(로그인 슬롯 대체). 익명 표면은
  샘플 중에도 평소 그대로.
- **R5-5 · KakaoTalk** — 행은 보이되 컨트롤 없음: 「예정」 칩 + "준비되면 이 자리에서
  켤 수 있습니다". 동작하지 않는 스위치 없음.
- **R5-6 (개정) · 이름 = 내 포트폴리오, 진입은 계정 메뉴 첫 행** — nav 4번째 링크 철회
  (operator 개정); links는 R2 삼분할 그대로 (내 종목 조회 · 관제 현황판 · 해설).
  메뉴 구성: 내 포트폴리오 / 알림 설정 / 로그아웃.
- **R5-7 · 알림 메일 미리보기 — 이번 라운드에 포함** (Notify 카드 우측). 메일은 라이트
  표면(클라이언트는 토큰 밖) — 라이트 값 직접 지정.

## Sample portfolio (session composition)

실제 pinned 공시 4건, 4개 상태 커버 — 보유량은 구성 예시로 명시:

| 종목 | 보유 | 상태 | 근거 |
|---|---|---|---|
| 계양전기 | 500주 | ① live, D-5 (2026-08-25), 발행가 확정 전 → 배정 115주 +23주, 금액 없음 | `r1-live-healthy` `20260724000546` |
| 대동기어 | 300주 | ② 전환청구 개시 D-65 (2026-10-24), 오버행 6.68% — 금액·환산 없음 | `r2-option-schedule` `20251016000315` |
| 한화솔루션 | 500주 | ① 소멸 — 500주 기준 「추정」679,575원 (R4 MissedMoney와 동일 수) | `r1-money-chain` `20260720000067` |
| 세기상사 | 100주 | ③ 통지 마감 지남 D+45 (2026-07-06) — 2단계 절차 경고 | `r3-version-split` `20260713000345` |

D-day 앵커: board-snapshot 측정일 **2026-08-20 (KST)**, 카드에 명기. 브라우저 계산 없음.

## Proposed copy (all new — copy-inventory에 auth 계열 없음)

Auth: "가입한 이메일과 비밀번호로 로그인합니다." · "이메일과 비밀번호만으로 만듭니다 —
만들어지면 바로 로그인됩니다." · "이메일 또는 비밀번호가 일치하지 않습니다." · "이미
가입된 이메일입니다 — 로그인해 주세요." · "비밀번호는 8자 이상이어야 합니다." ·
"재설정 링크를 보냈습니다 — 메일함을 확인해 주세요." · "확인 중…" · "로그아웃되었습니다" · PII 패널 3행 (위 R5-1 개정).
Convert: "이 보유량은 탭을 닫으면 사라집니다" · "계정에 저장하면 마감이 다가올 때
이메일로 알립니다 — 계정은 이메일과 비밀번호뿐입니다." · "저장하고 알림 받기" ·
"지금처럼 로그인 없이 계속 쓸 수 있습니다" · "이 마감 알림 받기 →" / 로그인 시
"내 포트폴리오에 담기 →". Portfolio: "포트폴리오가 비어 있습니다" · "종목과 보유량을
등록하면, 진행 중인 권리와 마감을 여기서 지켜보고 이메일로 알립니다." · 이월 행 (R5-3).
Notify: "마감 임박 이메일" · 시점 칩 7일 전/3일 전/1일 전/당일 (기본 7일+1일 — 제안) ·
KakaoTalk 행 (R5-5) · "계정을 삭제하면 이메일 주소를 즉시 지웁니다 — 남는 것이
없습니다." · 메일 제목 "[미주알] {종목} — {마감명} D-{n} ({date})" + 푸터 "이 메일은
회원님이 설정한 마감 알림입니다 — 알림 설정에서 끌 수 있습니다."
Sample: "샘플 포트폴리오로 둘러보기" · "가입 없이, 실제 공시 4건으로 구성된 예시
포트폴리오를 엽니다 — 클릭 한 번." · 배너 "샘플 포트폴리오 — 구성 예시입니다.
종목·공시·마감은 실제, 계정·보유량은 예시입니다." · "샘플 종료" · 랜딩 푸터
"내 포트폴리오는 어떻게 보이나 — 샘플로 열어보기 →".

## Departures & notes

- **P3.S6 개정 (operator, 2026-08-21)**: ① Auth 이메일+비밀번호 (R5-1 대체 — 인증 코드
  폐기, 재설정 링크 추가); ② 샘플/익명 포트폴리오 편집 + localStorage 저장 (로그인 없이
  유지, 로그인 시 이전 제안만); ③ 본문 대제목 제거 — header nav가 위치 표시; ④ 행 편집
  확정 = 액션 열 저장/취소 가로 배치; ⑤ 내 포트폴리오 진입 = 계정 메뉴(스택 리스트)
  첫 행 — nav links 아님 (R5-6 진입 위치 대체).

- **R5-8 · 챙긴 돈 체크 (operator, post-gate)** — 지나간 ① 소멸 행에 본인 표시 체크박스
  "청약·매도로 챙겼습니다": 체크 시 라벨 놓친 돈 → 챙긴 돈, 금액 동일(「추정」 유지),
  색 alert → live, 카러션 "본인 표시 · 계정에 저장". 공시 데이터가 아닌 사용자
  주장 — 계정에만 저장. 익명 조회(R4)의 조건문 프레임("청약도 매도도 하지
  않았다면")은 불변 — 체크는 포트폴리오 층 전용.
- **Card split**: `ChromeSession.html` added beyond the required 7 (handoff allows) —
  logged-in chrome은 R2 nav의 *확장*이라 자기 카드가 맞음. Footer 불변.
- **삭제 = 즉시 + 8초 되돌리기** (모달 없음) — proposed interaction, Portfolio 카드 명기.
- **알림 정책 제안**: 계정 존재 이유(마감 알림) 외의 메일 없음 — 마케팅·다이제스트 없음.
- **샘플에서 알림 설정 숨김** — 보낼 주소가 없으므로; 가짜 이메일·알림 이력 없음 (Finding 8).
- 이메일 mock은 토큰 밖 라이트 값 하드코딩 (이메일 클라이언트는 cosmos 스코프가 닿지
  않는 외부 표면) — readme의 "light :root remains for light/print contexts"와 일치.
- Token delta: **없음**.

## Hard rules honored

익명 경로 비차단 · 확정발행가 전 금액 없음(메일 포함) · ②/③ 금액 없음 · D-day KST
상류 계산 · ② 과거 개시 = 진행 중 · 「추정」 규칙 · 사용자 정체성 연출 없음.
